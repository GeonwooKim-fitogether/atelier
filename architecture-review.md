# Atelier 발전 리뷰 — 개인 공방에서 회사 상품기획 시스템, 그리고 SaaS로

> 한 줄 요지: **Atelier를 SaaS까지 끌고 가는 길에서 가장 먼저 고칠 것은 코드가 아니라 헌장이다.** 현행 헌장 v0.2 §18은 "Atelier ≠ 외부 platform, 다른 사람에게 안 팜"을 명시하고 있어 새 방향과 헌법 수준에서 충돌한다. 그 충돌을 개정으로 해소한 뒤, 지금 마크다운 파일과 스킬 문서(prose) 안에 흩어져 있는 워크플로를 "척추 불변식이 데이터베이스에서 강제되는 애플리케이션"으로 옮기는 것이 Hardware Team System(이하 HTS) 수준으로 가는 길이다. 이 문서는 그 격차를 실측으로 재고, 클린 아키텍처 4겹 위의 설계와 3단계 로드맵을 제안한다.

작성일: 2026-08-26. 검토 대상: `atelier` 저장소 전체(main 기준), 비교 기준: `Hardware-Team-System`.

---

## 1. 결론 요약 — 다섯 문장

1. **헌장이 새 방향과 충돌한다.** §18(1인용 도구, 안 팜), §2(사용자 = 김건우 1인), §4.2(100% 동기 대화, CEO 부재 시 진행 0), §8(대화 모드만), §9.3(영구 단일 repo·복제 0)은 모두 "회사 시스템 → SaaS" 방향과 정면으로 어긋난다. 헌법 변경은 CEO 권한이므로 이 문서는 개정안을 제안만 한다(§3).
2. **지금의 Atelier는 제품이 아니라 방법론 문서다.** 시스템의 실체가 `SKILL.md`의 산문과 마크다운 파일 6개에 있고, 실행 엔진은 Claude Code 자체다. Claude Code 위에서만 도는 것은 사내 도구로는 훌륭하지만 팔 수 있는 물건이 아니다 — SaaS가 되려면 워크플로 규칙이 코드(도메인 계층)로, LLM 호출이 교체 가능한 어댑터 뒤로 옮겨져야 한다(§5).
3. **HTS 수준의 본질은 기능 개수가 아니라 "규칙이 문서가 아닌 시스템에 산다"는 것이다.** HTS는 척추 한 줄(Rev·Phase는 오직 ECO Effective로만)을 데이터베이스 트리거가 강제하고, 그 위에 검증·마이그레이션·문서 거버넌스 규율을 쌓았다. Atelier도 같은 구조가 가능하다 — 척추 후보는 "아이디어의 단계는 오직 근거가 딸린 판정(Pass/Hold/Kill/Freeze)으로만 바뀌고, Freeze된 PRD는 새 리비전으로만 변경된다"이다(§4, §5.2).
4. **SaaS를 염두에 둔다면 지금 싸게 사 둘 것이 딱 두 가지 있다.** 첫 마이그레이션부터 모든 표에 워크스페이스 식별자를 넣는 것과, LLM 호출을 포트(인터페이스) 뒤에 두는 것이다. 나머지 SaaS 요소(과금·온보딩·외부 인증)는 나중에 붙여도 비싸지지 않지만, 이 둘은 나중에 고치면 전면 재작업이 된다(§6).
5. **순서는 "헌장 개정 → 사내 시스템(1단계 고객 = 우리 회사) → SaaS 분사"다.** 그리고 Atelier 스스로가 자기 깔때기의 첫 실전 안건이 되어야 한다 — "상품기획 SaaS"라는 아이디어에 Pain 3축(Reality/Depth/Owner)을 그대로 적용하는 것이 헌장이 스스로에게 요구하는 일이고, 비어 있는 idea-funnel에 첫 실데이터를 만드는 일이기도 하다(§7).

---

## 2. 현재 상태 실측 — Atelier에 지금 무엇이 있나

저장소를 전수로 열어 확인한 결과다. 추정이 아니라 파일 목록 그대로다.

| 자산 | 내용 | 상태 |
|---|---|---|
| `charter.md` (433줄) | 헌법 v0.2. 정체성·프로세스·안티패턴이 정밀하게 정의됨 | 완성도 높음. 단 §18이 새 방향과 충돌 |
| `.claude/skills/atelier-npi/SKILL.md` | Stage 1(Ideation) → Stage 2(PRD freeze) 워크플로 전체 | 실체는 산문(prose). 실행 엔진 = Claude Code |
| `idea-inbox.md` · `idea-funnel.md` · `decisions.md` · `lessons-learned.md` | 운영 데이터 저장소 역할의 마크다운 4개 | **전부 헤더만 있고 실데이터 0건** — 아직 한 사이클도 돌지 않음 |
| `workflows/type1-external-service.html` | 워크플로 시각화 1건 | 산출물 |
| `Reference/` | 상품기획 책 PDF 5장 | 에이전트 참고 자료 |
| 애플리케이션 코드 | 없음 | — |
| 데이터베이스·마이그레이션 | 없음 | — |
| 테스트·CI | 없음 | — |
| `.claude/rules/` · 훅 · `.github/workflows/` | 없음 | **창고(Template-repository) 동기화 밖에 있음** |

두 가지를 정확히 읽어야 한다.

첫째, **이것은 "뒤처진" 상태가 아니라 "기획 단계" 상태다.** HTS도 구현 전에 헌장·데이터 모델·디자인 정본을 먼저 만들었고(`docs/HANDOFF.md`가 그 기록이다), Atelier는 지금 그중 헌장만 있는 지점이다. 격차표(§4)는 "부끄러운 목록"이 아니라 "구현 단계로 넘어갈 때 채울 목록"으로 읽는 것이 맞다.

둘째, **아직 한 사이클도 돌지 않았다는 사실이 SaaS 계획의 순서를 정한다.** idea-funnel과 lessons-learned가 비어 있다는 것은, 이 방법론이 실전에서 검증된 횟수가 0회라는 뜻이다. 검증 0회의 방법론을 제품으로 파는 것은 헌장 §5.1의 Kill 안전장치("내가 이 niche의 흔적을 진짜로 찾아봤나?")에 스스로 걸린다. 그래서 로드맵(§7)은 "사내에서 실사이클을 돌려 데이터를 쌓는 단계"를 SaaS 앞에 둔다.

---

## 3. 헌장 충돌 — 개발을 시작하기 전에 CEO가 결정할 것

새 방향(회사 상품기획 시스템 → SaaS 자회사)과 충돌하는 헌장 조항을 전수로 뽑았다. 헌장 변경은 CEO 권한(§7 "CEO 검토 + Agent 정정 — 헌법 변경")이므로, 아래는 개정 **제안**이다.

| 조항 | 현행 | 충돌 지점 | 개정 방향 제안 |
|---|---|---|---|
| §18 | "Atelier ≠ 외부 platform. 당신 1인용 도구. **다른 사람에게 안 팜**" | SaaS와 정면 충돌 | 삭제하고, "단계적 개방" 조항으로 대체: 1인 도구 → 사내 시스템 → 외부 제품의 3단계를 명시 |
| §18 | "Atelier ≠ 개발 환경. 코드 안 씀" | 시스템이 되는 순간 코드가 생김 | "Atelier **사용자**는 코드를 안 씀"으로 정밀화. 시스템 자체의 개발은 Foundry 사이클로 진행 |
| §2 | 사용자 = 김건우 1인 | 사내 시스템은 복수 사용자, SaaS는 외부 고객 | 퍼소나를 계층화: 공방장(운영자) / 사내 기획 참여자 / (3단계) 외부 고객 |
| §4.2 | "100% 동기적 대화. CEO 부재 시 작업 진행 0" | 복수 사용자 시스템에서는 성립 불가 | "판정(Pass/Hold/Kill/Freeze)은 사람만 내린다"로 본질을 보존하고, 진행 방식은 자유화 |
| §8 | 상품기획 Agent = 유일한 agent, 대화 모드만 | 제품이 되면 워크플로 엔진·수집 봇 등이 필요 | "판정에 개입하는 agent는 하나"로 좁혀 보존 |
| §9.3 | Atelier 복제 0, 영구 단일 repo | SaaS는 테넌트(고객사)마다 자기 데이터 공간이 필요 | "복제 0"의 본질(경험 축적의 단일성)을 "워크스페이스 분리 + 방법론 자산은 중앙 버전 관리"로 재정의 |

한 가지는 **보존을 강하게 권한다.** §18의 마지막 줄 — "대화 자체가 본질. 자동화하면 사라짐" — 은 충돌 조항이 아니라 **이 제품의 차별점 정의**다. 시중의 기획 도구(Notion 템플릿, Productboard 류)는 양식(form)을 채우게 하지만, Atelier의 가치는 cross-examine 대화가 사람의 사고를 깊게 만드는 데 있다. 제품화할 때 이것이 "채팅이 붙은 양식 도구"로 퇴화하면 팔 이유가 없는 물건이 된다. 그래서 §5의 아키텍처 제안은 대화 세션을 1급 개체(first-class entity)로 둔다.

---

## 4. "HTS 수준"이란 정확히 무엇인가 — 그리고 Atelier와의 격차

"HTS 수준의 시스템"을 감이 아니라 목록으로 만들기 위해, HTS 저장소에서 실제로 확인되는 능력 일곱 개로 분해했다. 각 행의 근거는 HTS의 실물 파일·사건이다.

| # | 능력 | HTS에서의 실물 근거 | Atelier 현재 | 격차의 성격 |
|---|---|---|---|---|
| 1 | **척추 불변식을 데이터베이스가 강제** | 트리거 `guard_revision_spine`, RLS 96개 정책. "Rev는 ECO Effective로만"이 문서가 아니라 DB에서 막힘 | 규칙이 SKILL.md 산문에만 존재. 어기는 것을 막는 장치 0 | 설계 필요 (§5.2에 척추 후보 제안) |
| 2 | **클린 아키텍처 4겹 코드 구조** | `app/src/lib/domain`(순수 판정) / `lib/data`·`lib/actions`(어댑터) / `app/`(UI). 판정 함수를 화면과 검사가 공유해 둘이 갈리지 않음 | 코드 없음 | 신규 구축 |
| 3 | **마이그레이션 규율** | 시각 기반 파일명 규칙, 적용 전 push 강제 훅, 함수 지문 잠금, 드리프트 판정 스크립트 — 전부 실제 사고에서 나옴 | 해당 없음(DB 없음). 단 창고 동기화 밖이라 규칙·훅이 안 내려옴 | 창고 동기화 편입으로 대부분 무상 획득 가능 |
| 4 | **검증 규율** | Playwright e2e + 사고를 그대로 인코딩한 단위 테스트(예: `attrs-merge` 11개) + 노출 증명(배선·적용·보는 법) | 없음 | 신규 구축. 단 LLM 산출물 검증은 HTS에도 없는 새 규율(§6.3) |
| 5 | **문서 거버넌스** | STATUS.md, doc-governance 매트릭스(개발→문서 갱신 트리거), 디자인 토큰 + 정본 프로토타입 | charter 하나가 전부. 갱신 트리거 없음 | 구현 단계 진입 시 같은 패턴 복제 |
| 6 | **운영 규율의 동기화** | `.claude/rules/` 4종 + 훅이 창고에서 내려옴. 커뮤니케이션·원본우선·DB쓰기 규칙이 세션마다 자동 적용 | `.claude/`에 스킬 하나뿐. rules 0, 훅 0 | **즉시 조치 가능** — 동기화 봇 대상에 atelier 추가 |
| 7 | **실측 문화** | "느리다"를 감이 아니라 라이브 쿼리 통계로 규명(100ms→3ms, 33배)하고 근본원인 문서로 남김 | 실측할 대상이 아직 없음 | 문화는 사람이 가져감 — 이 문서 자체가 그 방식(전수 실측)을 따름 |

격차를 한 문장으로 압축하면: **HTS는 "규칙을 시스템으로 격상"하는 데 3개월치의 사고와 수리를 지불했고, Atelier는 그 교훈을 창고 동기화와 설계 차용으로 거의 무상으로 물려받을 수 있는 위치에 있다.** 이것이 지금 시작하는 것의 가장 큰 이점이다.

---

## 5. 클린 아키텍처 설계 제안 — 상품기획 도메인의 4겹

### 5.1 가장 중요한 구조 전환 — 시스템이 지금 어디에 살고 있나

현재 구조를 정직하게 그리면 이렇다.

```
지금:  [Claude Code 런타임] ──읽음──> SKILL.md (워크플로 산문)
                          ──읽고 씀──> *.md 파일 4개 (데이터)
       → 워크플로 규칙도, 데이터도, 실행 엔진도 전부 프레임워크(Claude Code) 층에 있음
```

클린 아키텍처의 언어로 말하면, **지금은 안쪽 계층(도메인)이 비어 있고 모든 것이 가장 바깥 계층에 있다.** 사내 1인 도구로는 이게 최적이었다(헌장의 minimal-first가 옳았다). 그러나 회사 시스템·SaaS가 되려면 의존성 방향을 뒤집어야 한다.

```
목표:  [프레임워크/UI]  Next.js + antd 화면, Claude/LLM API, Supabase, GitHub
              ↓ 의존
       [어댑터]        LLMPort 구현체, 저장소(repo) 구현체, Foundry 인계 어댑터
              ↓ 의존
       [유스케이스]     runStage1, crossExamine, freezePRD, handoffToFoundry, recordDecision
              ↓ 의존
       [도메인]        Idea, StageDecision, PainAssessment, PRDRevision, KillSignal
                       + 순수 규칙: 단계 전이표, freeze 조건, Kill 정의(§3.2)
```

이 전환의 실무적 의미 두 가지:

1. **SKILL.md에 있는 것이 사실은 이 회사의 지적 자산이다.** Pain 3축, cross-examine 3축(구조·논리·본질), Moat & Wall, Kill Signal, freeze 조건 — 이것이 제품의 moat다. 이것들을 산문이 아니라 **버전 관리되는 도메인 데이터**(질문 세트, 판정 기준, 프롬프트 템플릿)로 옮겨야, 방법론을 개선할 때마다 스킬 파일을 고치는 게 아니라 제품이 진화한다.
2. **LLM은 반드시 포트 뒤에 둔다.** cross-examine을 수행하는 것이 Claude인지, 어떤 모델인지, 테넌트별로 어떤 키를 쓰는지는 어댑터의 사정이다. 도메인은 "cross-examine 요청 → 3축 검토 결과"라는 계약만 안다. 이것이 SaaS에서 비용 계량·모델 교체·고객사 키 지원을 가능하게 하는 유일한 구조다.

### 5.2 척추 제안 — Atelier의 "그 한 줄"

HTS가 "Rev·Phase는 오직 Change(ECO)의 Effective로만 바뀐다" 한 줄로 전체 정합을 잡았듯, Atelier의 척추를 다음과 같이 제안한다.

> **아이디어의 단계(Stage)는 오직 근거가 딸린 판정(Decision)으로만 바뀐다. Freeze된 PRD는 불변이며, 변경은 새 리비전으로만 한다.**

풀어 쓰면 세 개의 불변식이다.

1. **단계 전이는 판정을 통해서만.** `idea.stage`를 직접 UPDATE하는 것은 금지되고, `stage_decision` 행(판정 종류 Pass/Hold/Kill/Freeze + 판정자 + 근거 참조)을 넣는 것만이 단계를 바꾼다. HTS의 "Rev 직접 변경 금지"와 정확히 같은 모양이고, 같은 방식(데이터베이스 트리거)으로 강제한다.
2. **판정에는 근거가 반드시 딸린다.** Pass 판정은 Pain 3축 평가 없이 존재할 수 없고, Kill 판정은 "통증이 가짜"라는 근거 없이 존재할 수 없다(헌장 §3.2의 Kill 정의가 여기서 시스템이 된다). Hold와 Kill을 시스템이 구분해 주는 것 — "자원 부족은 Kill 사유가 아니다"를 데이터 제약으로 만드는 것 — 이 이 제품이 Notion 템플릿과 다른 지점이다.
3. **Freeze 후 PRD는 ECO 패턴으로만 변경.** freeze된 `prd_revision`은 불변이고, 수정은 새 리비전을 열어 다시 freeze하는 것뿐이다. Foundry에 인계된 것이 어느 리비전인지가 영원히 추적된다. HTS의 Item Revision 모델을 그대로 차용한다.

### 5.3 핵심 데이터 모델 (개념 수준)

```
workspace (테넌트 경계 — §6.1)
 └─ idea ──── stage_decision (Pass/Hold/Kill/Freeze + 판정자 + 시각)
        │            └─ evidence (Pain 3축 평가, 외부 흔적 수집물, cross-examine 기록)
        ├─── conversation_session (Stage 1/2의 대화 자체 — 1급 개체, §3 마지막 문단)
        ├─── prd_revision (freeze 시 불변) ── prototype_asset (바이트는 스토리지, DB엔 메타만)
        │            └─ kill_signal (정량 1개 + 이후 실측값 추적)
        └─── handoff (Foundry repo 링크 + 인계된 prd_revision 고정)

methodology_asset (질문 세트·판정 기준·프롬프트 — 버전 관리, 워크스페이스 밖 중앙 자산)
lesson (Flywheel 회전 종료 후 정제 — charter §11의 시스템화)
```

파일(목업·프로토타입·PDF)은 HTS의 원칙 그대로 — **바이트는 스토리지, DB엔 식별자와 메타데이터만.** 사내 단계에서는 HTS의 drive-broker 패턴을 참고하되, SaaS를 생각하면 회사 Google Drive가 아니라 Supabase Storage를 쓰는 것이 분리(분사) 시 이관을 없앤다.

### 5.4 스택 권장

**HTS와 동일 스택(Next.js App Router + antd + TypeScript + Supabase)을 권장한다.** 이유는 세 가지다. 첫째, 창고 동기화·마이그레이션 규율·훅이 그대로 적용된다. 둘째, HTS에서 검증된 패턴(순수 판정 함수를 화면과 검사가 공유, 트리거로 척추 강제, RLS)을 설계 차용이 아니라 코드 수준 참조로 가져올 수 있다. 셋째, 운영자가 한 사람이므로 스택을 하나로 유지하는 것 자체가 속도다. 단 디자인 토큰은 HTS 것을 출발점으로만 쓰고, 제품이 되는 시점(3단계)에 자기 정체성을 갖는 것이 맞다.

---

## 6. SaaS 관점에서 지금 결정해야 하는 것 vs 미뤄도 되는 것

과잉 설계(헌장 §13 안티패턴 2·3·6)를 경계하면서, "나중에 고치면 전면 재작업이 되는 것"만 지금 산다.

### 6.1 지금 산다 (나중에 사면 비싸짐)

| 항목 | 왜 지금인가 | 지금의 비용 |
|---|---|---|
| **모든 표에 `workspace_id`** | 멀티테넌시(한 시스템이 여러 고객사 데이터를 격리 수용)를 나중에 넣으려면 전 테이블 개조 + 전 RLS 재작성 + 데이터 이관이다. HTS가 단일 테넌트 전제로 지은 것을 지금 뜯을 수 없는 것과 같은 이유 | 첫 마이그레이션에 열 하나씩. 사내 단계에서는 워크스페이스가 1개("Fitogether")로 돌 뿐 |
| **RLS를 워크스페이스 멤버십 기준으로 작성** | 정책을 "로그인했으면 다 보임"으로 시작하면 96개 정책을 나중에 전부 다시 씀 | 정책 문구가 조금 길어짐 |
| **LLM 호출을 포트 뒤에** (§5.1) | 도메인 곳곳에 특정 SDK 호출이 박히면 모델 교체·비용 계량·고객 키 지원이 전면 수술이 됨 | 인터페이스 하나 + 구현체 하나 |
| **방법론 자산의 버전 관리** (§5.3 `methodology_asset`) | 프롬프트·판정 기준이 코드에 하드코딩되면 "우리 방법론 v3"을 팔 수 없음 | 표 하나 |

### 6.2 미룬다 (붙일 때 비싸지 않음)

과금·구독 관리, 외부 고객 온보딩, 회사 밖 인증(사내 단계는 HTS의 Google SSO 패턴 그대로), 다국어, 사용량 대시보드, 감사 로그의 규정 준수(compliance) 수준 강화. 전부 3단계(분사 준비)에서 붙여도 구조를 바꾸지 않는다.

### 6.3 HTS에도 없는 새 숙제 — LLM 산출물의 품질 게이트

정직하게 적어 둘 것이 하나 있다. HTS는 결정론적 시스템이라 "테스트 통과 = 동작 보장"이 성립하지만, Atelier의 핵심 기능(cross-examine, 외부 흔적 수집, PRD 초안)은 LLM 산출물이라 **같은 입력에 같은 출력이 보장되지 않는다.** HTS 수준의 검증 규율을 이식하는 것만으로는 부족하고, 다음이 추가로 필요하다:

- **평가 세트(eval set)**: "좋은 cross-examine"의 예시·반례를 누적해 프롬프트/모델 변경 때마다 회귀 검사. 사내 실사이클(§7의 1단계)이 이 세트의 원천이 된다.
- **판정의 인간 전속**: Pass/Hold/Kill/Freeze는 시스템이 절대 자동으로 내리지 않는다(헌장 §2 "User=판단 / AI=조작"의 시스템화). 이것은 품질 장치이면서 동시에 제품 철학이다.

---

## 7. 로드맵 — 3단계, 각 단계의 완료 판정 포함

### 0단계 — 헌장 개정 + 자기 자신을 깔때기에 넣기 (지금, 개발 전)

- §3의 충돌 조항을 CEO가 심의해 charter v0.3(또는 v1)으로 개정한다.
- **"상품기획 SaaS"라는 아이디어 자체를 `/atelier-npi` Stage 1에 넣는다.** Pain Reality(다른 회사들이 이 통증의 흔적을 남기는가 — 기획 도구 커뮤니티, PLM/PM 도구 불만 글), Pain Depth, Pain Owner(첫 고객사 1곳을 머릿속에 그릴 수 있는가)를 평가한다. 이것이 비어 있는 idea-funnel의 첫 실데이터가 되고, SaaS 가설의 Kill Signal(정량 1개)도 여기서 나온다.
- atelier 저장소를 창고 동기화 대상에 편입한다(§4 격차 #6). 규칙 4종과 훅이 내려오면 이후 모든 세션이 HTS와 같은 규율 위에서 돈다.
- 완료 판정: 개정 헌장 freeze + Stage 1 판정 기록 존재 + `.claude/rules/`가 동기화로 채워짐.

### 1단계 — 사내 상품기획 시스템 (첫 고객 = 우리 회사)

- §5의 설계로 앱을 짓는다. 범위는 깔때기의 척추만: idea 등록 → Stage 1(판정+근거) → Stage 2(PRD 리비전 + 프로토타입 메타) → freeze → Foundry 인계 기록. 대시보드 요구사항(decisions.md에 이미 누적된 것 — Phase 2 대기 가시화, Hold 재검토)이 첫 화면이 된다.
- 데이터의 원본이 마크다운 파일에서 데이터베이스로 옮겨간다. 기존 md 4개는 읽기 전용 유물로 보존.
- HTS 규율을 처음부터 적용: 시각 기반 마이그레이션 파일명, 적용 전 push, e2e(Playwright), 노출 증명, STATUS.md.
- **이 단계에서 실제 NPI를 최소 2사이클 돌린다.** 도구를 만들면서 도구로 기획한다(dogfooding). 여기서 나온 마찰·교훈이 lessons-learned의 첫 실데이터이자 6.3의 평가 세트 원천이다.
- 완료 판정: 사내 구성원이 CEO 개입 없이 idea 등록→Stage 1을 화면에서 완주(노출 증명) + 실사이클 2회분의 판정·근거가 DB에 존재.

### 2단계 — 회사 표준화 + 제품 완성도

- 사내 복수 사용자 운영(하드웨어팀 기획 흐름과의 접점 포함 — 단, HTS와의 직접 통합은 요청 밖이므로 착수하지 않고 접점 설계만 기록).
- 방법론 자산의 버전 관리 가동, LLM 평가 세트 회귀 검사 CI 편입, 디자인 자기 정체성.
- 완료 판정: CEO 아닌 사용자의 실사용 데이터 + 방법론 v버전이 데이터로 관리됨.

### 3단계 — SaaS 분사 준비

- 멀티테넌시 실가동(워크스페이스 2개 이상), 외부 인증, 과금, 첫 외부 고객(디자인 파트너) 1곳.
- 인프라 분리: Fitogether 계정(Supabase·Vercel·Drive)에서 자회사 계정으로. 6.1을 지켰다면 이관은 데이터 복사이지 개조가 아니다.
- 완료 판정: 0단계에서 정한 Kill Signal의 실측값이 생존 쪽 — 이것이 분사 결정의 근거가 된다.

---

## 8. 하지 말아야 할 것 — 이 계획 자체에 적용한 헌장 §13

1. **깔때기 실데이터 0건 상태에서 SaaS 기능(과금·온보딩)부터 짓지 않는다.** 안티패턴 2(추정으로 fleet 만들기)·6(premature optimization)의 재판이다.
2. **HTS 기능 목록을 이식 대상으로 삼지 않는다.** BOM·ECO·Quality는 하드웨어 PLM의 도메인이지 상품기획의 도메인이 아니다. 가져올 것은 기능이 아니라 **구조 원칙**(척추의 DB 강제, 4겹, 판정 함수 공유, 규율 문서)이다.
3. **대화를 양식으로 대체하지 않는다.** §3 마지막 문단. cross-examine이 선택 메뉴(A/B/C)로 퇴화하는 것은 안티패턴 7이 제품에서 재발하는 것이다.
4. **Foundry와의 경계를 흐리지 않는다.** Atelier 시스템 자체의 개발은 Foundry 사이클(PRD 패키지 → 새 repo → MVP)로 진행하는 것이 헌장 정합이다 — 즉 이 리뷰의 다음 단계는 "Atelier 시스템의 PRD 패키지"를 Atelier 방식으로 만드는 것이다.

---

## 부록 — 이 문서가 근거로 삼은 실측

- `atelier` 전 파일 열람(문서 6, 스킬 1, HTML 1, PDF 5). idea-funnel·lessons-learned 실데이터 0건 확인.
- `atelier` git 이력 7커밋 전수(v0.1 → v0.2 → 구조 평탄화 → 작성 주체 원칙).
- `Hardware-Team-System` 구조 실측: `app/src/lib/{domain,data,actions,...}` 계층, `docs/` 거버넌스 문서군, STATUS.md의 사고·수리 기록(트리거 `guard_revision_spine`, RLS 96정책, 성능 33배 수리 등).
- `Template-repository` 규칙 4종(communication·upstream-first·db-write-permission·migration-naming)과 atelier의 `.claude/rules/` 부재 대조.
- `foundry` README(4-Layer Frame, v0.3.0 frozen) — 인계 대상 시스템의 현황 확인.
