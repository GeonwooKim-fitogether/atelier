# Atelier 전체 진행 계획

> 한 줄 요지: **첫 실사이클을 돌리는 것이 시스템을 짓는 것보다 먼저다. 그리고 그 첫 사이클의 안건을 "사내 상품기획 시스템" 자체로 잡으면, 방법론 검증 1회차와 2단계 시스템의 PRD 패키지를 한 번에 얻는다.** 이 문서는 헌장 v0.3(단계적 개방)과 `architecture-review.md`를 실행 순서로 옮긴 계획이며, 각 국면(Phase)에 완료 판정과 게이트 질문을 붙였다.

작성일: 2026-08-26. 전제: charter v0.3 개정안(CEO ACCEPT 대기)과 `architecture-review.md`.

---

## 0. 왜 이 순서인가 — 계획의 두 원칙

**원칙 1 — 사이클이 시스템보다 먼저다.** Atelier의 깔때기는 아직 한 번도 돌지 않았다(idea-funnel 실데이터 0건). 검증 0회의 방법론 위에 시스템부터 지으면, 시스템이 굳혀 버리는 것이 검증되지 않은 절차다. 반대로 지금의 문서+Skill 형태로 한 사이클을 완주하면 세 가지가 한꺼번에 나온다: 방법론의 실전 마찰(Skill 진화의 재료), 깔때기의 첫 실데이터, 그리고 헌장 §16이 예정해 둔 "첫 실작업에서 발견"들.

**원칙 2 — 첫 사이클의 안건은 시스템 자신이다.** 어차피 2단계(사내 시스템)를 지으려면 PRD 패키지가 필요하고, 헌장 정합상 그 PRD는 Atelier 방식(Stage 1 → Stage 2 → freeze)으로 만들어야 한다. 그렇다면 첫 사이클의 안건을 "사내 상품기획 시스템"으로 잡는 것이 가장 경제적이다 — 검증용 모의 안건이 아니라 진짜 결과물이 걸린 안건으로 방법론을 시험하게 되고, 사이클의 산출물이 곧 다음 국면의 입력이 된다.

한 가지를 정직하게 구분해 둔다. 이 첫 사이클의 Stage 1이 검증하는 통증은 **사내 통증**(우리 회사의 상품기획이 개인 기억과 산발 문서에 흩어져 있다는 통증)이다. **SaaS 가설의 통증**(다른 회사들도 이 통증을 돈 내고 해결하려 하는가)은 별개의 검증이고, 그것은 3단계 진입 게이트(Phase 5)에서 따로 Stage 1을 돈다. 둘을 한 번에 검증하려 하면 둘 다 흐려진다.

---

## 1. 전체 지도

```
Phase 0  헌장 발효 + 기반 정비          ← 지금 여기. CEO의 ACCEPT가 방아쇠
   ↓
Phase 1  첫 실사이클 ⭐                 안건 = "사내 상품기획 시스템"
         (Stage 1 → Stage 2 → PRD freeze)   문서+Skill 형태 그대로, 동기 대화로
   ↓
Phase 2  Foundry 인계 + 시스템 구축      PRD 패키지 → 새 repo → 실코드 MVP
         (= 2단계 진입)                  클린 아키텍처 4겹 + HTS 규율
   ↓
Phase 3  실사이클 2회차+                 진짜 사업 아이디어를 시스템 위에서 완주
         (flywheel 1회전)               lessons-learned 첫 기록, 평가 세트 시작
   ↓
Phase 4  사내 표준화                    복수 사용자, 방법론 자산 버전 관리
   ↓
Phase 5  SaaS 게이트 → 분사 준비 (3단계)  "상품기획 SaaS" Stage 1 + Kill Signal 실측
```

각 화살표는 게이트다 — 앞 국면의 완료 판정이 나와야 다음으로 간다. 단, 헌장 §3.3(속도 원칙)에 따라 시간 유예는 없다: 판정이 나오면 즉시 다음 국면.

---

## 2. 국면별 상세

### Phase 0 — 헌장 발효 + 기반 정비

작다. 한두 세션이면 끝난다.

| 일 | 누가 | 내용 |
|---|---|---|
| charter v0.3 검토·ACCEPT | CEO | 개정안 대조표를 보고 수정 지시 → PR 머지 = 발효 |
| README 갱신 | Agent | "1인 CEO의 공방" 서술을 §1.4 단계적 개방에 맞춤 (발효 후) |
| 창고 동기화 편입 | Agent | Template-repository의 동기화 봇 대상에 atelier 추가 — 원본 우선 규칙대로 **창고 쪽에서** 고친다. 규칙 4종·훅이 내려오면 이후 세션이 HTS와 같은 규율 위에서 돈다 |

**완료 판정**: v0.3이 main에 있고, atelier에 `.claude/rules/`가 동기화로 채워짐.
**게이트 질문**: 없음 — ACCEPT 자체가 게이트다.

### Phase 1 — 첫 실사이클 ⭐ (이 계획의 핵심)

안건: **"사내 상품기획 시스템"** — 회사의 상품기획을 담당하는 시스템이 필요하다는 통증.

지금의 Atelier(문서 + `atelier-npi` Skill)를 **그대로** 쓴다. 시스템을 먼저 짓지 않는 것이 이 국면의 정의다. 헌장 §4.1대로 CEO의 평일 저녁/주말, 동기 대화로 진행한다.

**Stage 1 — Ideation** (약 1세션)

- CEO가 Pain 3축 초안을 말로 작성한다 (작성 권한 = CEO, 헌장 §5.1):
  - *Reality*: 우리 회사에서 이 통증의 흔적이 실제로 어디 남아 있나? (기획이 흩어진 실사례, 잊힌 아이디어, HTS 기획 단계에서 겪은 마찰)
  - *Depth*: 얼마나 자주, 얼마나 아프게?
  - *Owner*: 누가 가장 강하게 느끼나 — CEO 본인인가, 다른 구성원도인가?
- Agent가 구조·논리·본질 3축으로 cross-examine + 외부 흔적 수집 보조.
- 판정: Pass / Hold / Kill. **Kill이 나와도 이 계획의 실패가 아니다** — 그 자체가 깔때기가 작동한 첫 증거이고, 그 경우 Phase 2는 착수하지 않고 계획을 재검토한다.

**Stage 2 — PRD 패키지** (2~4세션, 반복 횟수에 따라)

- 도구 세트 확정 → 인사이트 정리 → mockups(깔때기 대시보드·판정 화면·PRD 리비전 화면 등) → **HTML 프로토타입** → PRD.md 초안.
- PRD.md에는 `architecture-review.md` §5의 설계(척추 불변식, 핵심 데이터 모델, 4겹)가 기술 제약으로 들어간다 — 기획과 설계가 여기서 합류한다.
- CEO가 프로토를 직접 사용 → 발견 → PRD 보완 → 재생성, 반복. 양쪽 OK → **freeze**.

**부산물 (똑같이 중요)**: Skill 실전 마찰 기록 → `atelier-npi` v0.2 진화. idea-funnel 첫 실데이터. Stage 산출물 형식의 정밀화(헌장 §16 예정 항목).

**완료 판정**: `stage-2/internal-planning-system/`에 PRD.md + mockups/ + prototype/이 freeze 상태로 존재하고, funnel에 판정 이력이 기록됨.
**게이트 질문**: "프로토를 사용해도 이상한 흐름·디테일 누락이 없는가?" (헌장 freeze 조건 그대로)

### Phase 2 — Foundry 인계 + 시스템 구축 (2단계 진입)

- foundry Template로 새 repo 생성 → PRD 패키지를 `01-product/`로 복사 (헌장 §9.5 워크플로 그대로 — Atelier 시스템도 예외 없이 이 길로 간다).
- 구현 원칙은 `architecture-review.md` §5·§6이 정본이다. 요지만 옮기면:
  - 첫 마이그레이션부터 전 테이블에 `workspace_id` + 워크스페이스 멤버십 기준 RLS.
  - 척추(단계는 판정으로만, freeze PRD 불변)를 데이터베이스 트리거로 강제.
  - LLM 호출은 포트 뒤에. 방법론 자산(질문 세트·판정 기준)은 데이터로.
  - HTS 규율 그대로: 시각 기반 마이그레이션 파일명, 적용 전 push, Playwright e2e, 노출 증명, STATUS.md.
- 기존 md 4개(inbox·funnel·decisions·lessons)의 데이터를 시스템으로 이관하고 파일은 읽기 전용 유물로 보존.

**완료 판정**: 사내 구성원이 메뉴에서 출발해 **클릭만으로** idea 등록 → Stage 1 판정까지 완주(노출 증명), 척추 위반 시도가 DB에서 거부되는 것이 테스트로 확인됨.
**게이트 질문**: "CEO가 아닌 사람이 안내 없이 첫 아이디어를 넣을 수 있는가?"

### Phase 3 — 실사이클 2회차+ (flywheel 1회전)

- **진짜 사업 아이디어**로 시스템 위에서 NPI를 완주한다. 현재 idea-inbox가 비어 있으므로, 이 국면의 첫 입력은 CEO가 채운다.
- 사이클 종료 후 헌장 §11 flywheel: lessons-learned 첫 정제 기록, 변경 크기 관찰, 필요 시 charter·Skill·시스템에 역반영.
- 이 사이클부터 "좋은 cross-examine"의 예시·반례를 모아 **LLM 평가 세트**를 시작한다(`architecture-review.md` §6.3) — 이것이 3단계 제품 품질의 기반이 된다.

**완료 판정**: flywheel 1회전 완료(사업 아이디어 1건이 freeze→Foundry 인계 또는 Hold/Kill로 종결) + lessons-learned 실데이터 존재.
**게이트 질문**: "시스템이 대화를 방해했는가, 도왔는가?" — 방해했다면 Phase 4 전에 고친다(§13-11 대화의 양식화 감시).

### Phase 4 — 사내 표준화

- 복수 사용자 실운영(기획 참여자 계층 활성화), 방법론 자산 버전 관리 가동, 평가 세트의 CI 회귀 검사 편입, 디자인 자기 정체성.

**완료 판정**: CEO 아닌 사용자의 실사용 데이터가 쌓이고, 방법론이 v버전으로 데이터에서 관리됨.

### Phase 5 — SaaS 게이트 → 분사 준비 (3단계)

- **"상품기획 SaaS" 아이디어를 자기 깔때기의 Stage 1에 투입한다** (헌장 §16 신설 항목). 이번에는 외부 통증이다: 다른 회사들의 흔적(커뮤니티 불만, 기존 도구 이탈 패턴, 잠재 고객 인터뷰)을 수집하고, Kill Signal(정량 1개)을 정한다.
- Pass 시: 멀티테넌시 실가동(워크스페이스 2개+), 외부 인증, 과금, 디자인 파트너 고객 1곳, 인프라의 자회사 계정 분리.
- 분사 판정의 근거 = Kill Signal 실측값. 헌장 §1.4가 정한 대로, 이 전환 자체가 근거 딸린 판정이다.

**완료 판정**: 외부 고객 1곳의 실사용 + Kill Signal 실측이 생존 쪽.

---

## 3. 지금 바로 움직이는 것 — 다음 세 수

1. **CEO**: charter v0.3 개정안 검토 → 수정 지시 또는 ACCEPT(PR 머지 지시).
2. **Agent** (ACCEPT 후): README 갱신 + 창고 동기화 편입.
3. **CEO + Agent** (다음 저녁/주말 세션): `/atelier-npi` 호출 — 안건 "사내 상품기획 시스템"으로 Stage 1 시작. CEO가 준비할 것은 하나다: **이 통증을 실제로 겪은 순간들**(흩어진 기획, 잊힌 아이디어, 되풀이된 설명)을 떠올려 오는 것. 그것이 Pain Reality의 재료다.

---

## 4. 이 계획이 스스로 지키는 것

- **판정은 전부 CEO 몫으로 남긴다.** 계획은 순서를 제안할 뿐, Pass/Hold/Kill/Freeze와 ACCEPT는 계획이 대신하지 않는다.
- **Kill이 나올 수 있음을 계획 안에 인정한다.** Phase 1이나 Phase 5의 Stage 1에서 Kill이 나오면 뒤 국면은 착수하지 않는다. 그것은 계획의 실패가 아니라 깔때기의 성공이다.
- **국면을 건너뛰지 않는다.** 특히 "시스템부터 짓고 싶은 충동"(Phase 1 생략)과 "SaaS 기능부터 붙이고 싶은 충동"(Phase 5 선착수)은 헌장 §13-2·6·12가 금지하는 바로 그것이다.
