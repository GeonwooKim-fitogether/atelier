# AGENTS.md — 프리랜서 브랜드 디자이너 포트폴리오 랜딩페이지

> 한 줄 요지: **Next.js로 만드는 1페이지 포트폴리오 랜딩이며, 이 페이지의 유일한 성공은 방문자가 '프로젝트 문의' 버튼을 눌러 폼을 제출하는 것이다.** 아래의 규격과 금지 사항은 취향이 아니라 요구사항이므로, 판단이 갈릴 때는 이 문서가 이긴다.

---

## 0. 이 문서를 어떻게 쓰나

이 파일은 코드를 작성하는 에이전트가 작업을 시작하기 전에 처음부터 끝까지 한 번 읽는 명세서다. 세 가지 원칙이 있다.

1. **여기에 적힌 수치는 그대로 쓴다.** 여백 80px, 컨테이너 1280px 같은 값은 협의된 규격이므로 "더 나아 보인다"는 이유로 바꾸지 않는다.
2. **여기에 없는 기능은 만들지 않는다.** 예를 들어 작업물 상세 페이지, 블로그, 다국어, 다크 모드 토글은 요청 범위 밖이다. 필요해 보이면 만들지 말고 §12의 형식으로 기록만 남긴다.
3. **§9 금지 사항은 예외가 없다.** 특히 이모지는 화면·코드·주석·커밋 메시지 어디에도 넣지 않는다.

---

## 1. 무엇을 만드나

### 1.1 한 문장 정의

브랜드 로고와 패키지 디자인을 6년째 해 온 프리랜서 디자이너 한 명의 개인 포트폴리오 랜딩페이지이며, 화면 하나에 일곱 개 섹션이 세로로 이어지는 단일 페이지 구조다.

### 1.2 사이트의 주인

| 항목 | 내용 |
|---|---|
| 직군 | 프리랜서 브랜드 디자이너 |
| 전문 분야 | 브랜드 로고 디자인, 패키지 디자인 |
| 경력 | 6년 |
| 보여줄 작업물 | 12개 |
| 주요 클라이언트 | 소규모 브랜드, 카페, 코스메틱 |

### 1.3 방문자와 목표 행동

방문자는 대체로 소규모 브랜드를 운영하거나 준비하는 사람이며, 여러 디자이너를 비교하다가 이 페이지에 들어온다. 이 페이지가 방문자에게 시켜야 하는 행동은 단 하나, **'프로젝트 문의' 버튼을 눌러 문의 폼을 제출하는 것**이다.

따라서 모든 섹션은 "이 사람에게 맡겨도 되겠다"는 확신을 쌓는 방향으로 설계하고, 문의로 가는 통로는 페이지 어디에서도 한 번의 조작으로 닿게 만든다. 구체적으로 세 곳에 통로를 둔다.

1. 상단 헤더에 상주하는 '프로젝트 문의' 버튼. 스크롤해도 계속 보인다.
2. 히어로 섹션 본문의 기본 버튼.
3. 6번 CTA 섹션의 문의 폼 자체. 위의 두 버튼은 모두 이 폼으로 스크롤 이동한다.

모바일에서는 헤더 버튼 대신 화면 하단에 고정 바를 두고 그 안에 같은 버튼을 넣는다.

### 1.4 완성된 사이트가 만족해야 하는 것

- 데스크톱과 모바일 어디서 열어도 레이아웃이 깨지지 않는다.
- 문의 폼이 실제로 제출되고, 잘못된 입력에는 어떤 항목이 왜 잘못됐는지 한국어로 알려 준다.
- 12개 작업물이 모두 보이고, 이미지가 없더라도 자리와 비율이 유지된다.
- §9의 금지 사항을 한 건도 어기지 않는다.

---

## 2. 기술 스택과 실행 명령

### 2.1 스택

| 항목 | 선택 | 이유 |
|---|---|---|
| 프레임워크 | Next.js 15, App Router | 요청된 기술이며, 폼 처리를 서버 액션으로 별도 백엔드 없이 끝낼 수 있다 |
| 언어 | TypeScript (strict) | 콘텐츠 데이터 구조를 타입으로 고정해 12개 작업물의 필드 누락을 컴파일 단계에서 잡는다 |
| 스타일 | Tailwind CSS v4 + CSS 변수 토큰 | 규격 수치를 토큰 한 곳에 모아 두고, 화면에서는 토큰만 참조하게 한다 |
| 폼 검증 | zod | 서버와 클라이언트가 같은 스키마를 공유한다 |
| 이미지 | `next/image` | 12개 이미지가 들어가는 페이지라 지연 로딩과 크기 최적화가 필요하다 |
| 폰트 | 아래 참조 | |
| 패키지 매니저 | npm | |
| Node | 20 이상 | |

폰트는 Pretendard를 `public/fonts/PretendardVariable.woff2`에 두고 `next/font/local`로 불러오는 것을 기본으로 한다. 그 파일을 구할 수 없는 환경이라면 `next/font/google`의 `Noto_Sans_KR`로 대체하고, 대체했다는 사실을 README에 한 줄 남긴다. 어느 쪽이든 폰트는 `next/font`를 거쳐야 하며 `<link>` 태그로 직접 불러오지 않는다.

### 2.2 명령

```bash
npm install
npm run dev        # 개발 서버, http://localhost:3000
npm run build      # 프로덕션 빌드. 이 명령이 통과해야 작업이 끝난 것이다
npm run start      # 빌드 결과 실행
npm run lint       # ESLint
npm run typecheck  # tsc --noEmit
```

`npm run typecheck` 스크립트가 없으면 `package.json`에 추가한다. 작업을 마치기 전에 `build`, `lint`, `typecheck` 세 개가 모두 통과해야 한다.

---

## 3. 디렉터리 구조

```
app/
  layout.tsx              루트 레이아웃. 폰트, 메타데이터, 전역 스타일
  page.tsx                랜딩 페이지 하나. 일곱 섹션을 순서대로 배치
  globals.css             Tailwind 진입점 + 디자인 토큰 정의
  actions/
    inquiry.ts            문의 폼 서버 액션
components/
  layout/
    Header.tsx            상단 고정 헤더 (로고 텍스트 + 문의 버튼)
    MobileCtaBar.tsx      모바일 하단 고정 문의 바
    Container.tsx         §4의 컨테이너 규격을 구현한 유일한 래퍼
    Section.tsx           섹션 세로 여백과 제목 슬롯을 담당
  sections/
    Hero.tsx              1) 히어로
    About.tsx             2) 소개
    WorkGrid.tsx          3) 작업물 그리드
    Process.tsx           4) 작업 프로세스
    Testimonials.tsx      5) 클라이언트 후기
    InquiryCta.tsx        6) 프로젝트 문의 CTA (폼 포함)
    Footer.tsx            7) 푸터
  ui/
    Button.tsx            버튼 한 종류. variant는 primary와 ghost 둘뿐
    Field.tsx             라벨 + 입력 + 오류 메시지를 묶은 폼 필드
content/
  site.ts                 이름, 소개 문장, 연락처 등 사이트 전역 문구
  works.ts                작업물 12개
  process.ts              프로세스 4단계
  testimonials.ts         후기
lib/
  inquiry/
    schema.ts             zod 스키마 (서버·클라이언트 공용)
    sink.ts               문의 내용을 어디로 보낼지 결정하는 어댑터
public/
  works/                  작업물 이미지 12장
  fonts/                  Pretendard woff2
```

콘텐츠를 `content/` 아래 타입이 붙은 데이터 파일로 분리하는 이유는, 나중에 사이트 주인이 문구와 작업물을 바꿀 때 화면 컴포넌트를 열지 않아도 되게 하기 위해서다. **화면 컴포넌트 안에 한국어 문구를 직접 적지 않는다.**

---

## 4. 레이아웃 규격

### 4.1 기준 수치

- 디자인 기준 화면 폭은 **1920px**이다. 즉 1920px에서 가장 보기 좋아야 한다.
- 콘텐츠 컨테이너의 최대 폭은 **1280px**이며, 화면이 아무리 넓어도 이보다 넓어지지 않고 가운데 정렬된다.
- 컨테이너 안쪽 좌우 여백은 **80px**이다. 따라서 1280px 컨테이너 안에서 실제 콘텐츠가 놓이는 폭은 **1120px**이다.

이 세 줄은 `Container.tsx` 하나에만 구현하고, 다른 곳에서 좌우 여백을 다시 주지 않는다. 여백 값이 여러 파일에 흩어지면 반드시 어긋난다.

```tsx
// components/layout/Container.tsx 의 규격
// width: 100%
// max-width: 1280px
// margin-inline: auto
// padding-inline: 20px  (화면 폭 768px 미만)
//                 40px  (768px 이상 1280px 미만)
//                 80px  (1280px 이상)
```

전체 폭 배경색을 깔아야 하는 섹션(6번 CTA)은 배경만 화면 끝까지 늘리고, 그 안의 내용은 반드시 같은 `Container`에 넣는다.

### 4.2 반응형 기준점

| 이름 | 폭 | 이 구간에서 달라지는 것 |
|---|---|---|
| mobile | 768px 미만 | 모든 그리드가 1열. 좌우 여백 20px. 하단 고정 문의 바가 나타난다 |
| tablet | 768px 이상 1280px 미만 | 작업물 2열, 프로세스 2열. 좌우 여백 40px |
| desktop | 1280px 이상 | 작업물 3열, 프로세스 4열. 좌우 여백 80px. 컨테이너 1280px에서 고정 |

모바일 대응은 선택이 아니라 필수 요구사항이다. 360px 폭에서 가로 스크롤이 생기면 실패로 본다.

### 4.3 세로 여백과 그리드

- 섹션 위아래 여백은 데스크톱 160px, 태블릿 120px, 모바일 80px이다.
- 간격 값은 4의 배수만 쓴다. 4, 8, 12, 16, 24, 32, 48, 64, 80, 120, 160.
- 그리드 열 사이 간격(gap)은 데스크톱 32px, 태블릿 24px, 모바일 16px이다.

---

## 5. 타이포그래피와 색

### 5.1 글자 크기

"과하게 큰 텍스트"는 금지 사항이므로 상한을 못 박는다. **데스크톱에서 어떤 글자도 64px를 넘지 않는다.**

| 역할 | 데스크톱 | 모바일 | 행간 | 비고 |
|---|---|---|---|---|
| 히어로 이름 (h1) | 64px | 34px | 1.1 | 자간 -0.02em |
| 섹션 제목 (h2) | 32px | 24px | 1.25 | |
| 소제목 (h3) | 20px | 18px | 1.4 | |
| 리드 문장 | 20px | 17px | 1.6 | 히어로 한 줄 소개, 섹션 도입부 |
| 본문 | 16px | 16px | 1.7 | |
| 라벨·캡션 | 13px | 13px | 1.5 | 자간 0.06em |

굵기는 400(본문), 500(라벨·버튼), 600(제목) 세 가지만 쓴다. 700 이상은 쓰지 않는다.

### 5.2 색

카페와 코스메틱 브랜드를 상대하는 포트폴리오이므로, 색은 작업물 이미지를 방해하지 않는 따뜻한 무채색을 바탕으로 하고 강조색 하나만 둔다.

```css
/* app/globals.css */
:root {
  --color-bg: #FBFAF8;        /* 페이지 배경. 순백이 아닌 따뜻한 오프화이트 */
  --color-surface: #FFFFFF;   /* 카드 배경 */
  --color-ink: #1A1A18;       /* 본문·제목 */
  --color-ink-muted: #6B6862; /* 보조 설명, 캡션 */
  --color-line: #E4E0D9;      /* 구분선, 카드 테두리 */
  --color-accent: #2E4B3F;    /* 버튼, 링크. 이 색 하나만 강조색으로 쓴다 */
  --color-inverse-bg: #1A1A18;/* 6번 CTA 섹션 배경 */
  --color-inverse-ink: #FBFAF8;
}
```

색은 여기서만 정의하고 화면에서는 토큰 이름으로만 참조한다. 컴포넌트 안에 `#` 색상값을 직접 적지 않는다. 그라디언트와 그림자는 쓰지 않으며, 면을 구분해야 하면 `--color-line` 1px 선이나 배경색 차이로 해결한다.

---

## 6. 섹션 명세

일곱 개 섹션을 아래 순서대로 배치한다. **각 섹션은 서로 다른 레이아웃 형태를 갖는다.** 같은 형태를 반복하면 페이지 전체가 템플릿처럼 보이고, 특히 좌우 2단을 번갈아 뒤집는 배치는 금지 사항(§9)이다.

| 순서 | 섹션 | 레이아웃 형태 | 이 섹션이 방문자에게 주는 것 |
|---|---|---|---|
| 1 | 히어로 | 좌측 정렬 단일 컬럼 | 누구인지, 무엇을 하는지 3초 안에 |
| 2 | 소개 | 좁은 라벨 열 + 넓은 본문 열 | 6년이라는 경력과 일하는 방식의 신뢰 |
| 3 | 작업물 그리드 | 3열 카드 그리드 | 실제 결과물의 양과 결 |
| 4 | 프로세스 | 가로 4열 스텝 레일 | 맡기면 어떻게 진행되는지의 예측 가능성 |
| 5 | 후기 | 중앙 정렬 좁은 단일 컬럼 | 다른 사람이 이미 만족했다는 증거 |
| 6 | 문의 CTA | 배경 반전 + 단일 컬럼 폼 | 행동. 이 페이지의 목적지 |
| 7 | 푸터 | 3열 정보 열 | 마무리와 대체 연락 수단 |

### 6.1 히어로

화면 높이의 80% 정도를 쓰되 100vh로 꽉 채우지 않는다. 아래에 내용이 더 있다는 사실이 보여야 스크롤이 이어진다.

- 배치: 컨테이너 안에서 왼쪽 정렬 단일 컬럼. 텍스트 블록의 최대 폭은 720px로 제한한다.
- 내용: 이름(h1), 한 줄 소개(리드 문장), '프로젝트 문의' 버튼 하나.
- 한 줄 소개는 직군과 대상을 함께 담는다. 예: "소규모 브랜드의 로고와 패키지를 만듭니다. 6년째, 90여 개의 브랜드와 함께했습니다."
- 배경 이미지나 장식 도형을 쓰지 않는다. 여백이 곧 배경이다.

### 6.2 소개

- 배치: 왼쪽에 220px 고정 폭 라벨 열, 오른쪽에 나머지 폭의 본문 열을 둔다. 이것은 정의 목록 형태이며 50:50 2단 배치가 아니다. 모바일에서는 라벨이 본문 위로 올라가 세로로 쌓인다.
- 라벨은 세 개다. "경력", "작업 방식", "함께한 곳".
- "경력" 항목에는 6년이라는 기간과 그 안에서 다룬 분야를 문장으로 적는다.
- "작업 방식" 항목에는 어떻게 일하는지를 두세 문장으로 적는다. 여기서 방문자가 "연락해도 부담 없겠다"고 느껴야 한다.
- "함께한 곳" 항목에는 소규모 브랜드, 카페, 코스메틱이라는 세 갈래를 적는다. 클라이언트 로고를 나열하지 않는다. 실제 로고 사용 허락 여부를 알 수 없기 때문이다.

### 6.3 작업물 그리드

- 개수는 정확히 12개다. 더 적거나 많으면 안 된다.
- 배치: 데스크톱 3열, 태블릿 2열, 모바일 1열. 카드 이미지 비율은 전부 4:3으로 통일한다. 비율이 섞이면 그리드가 어수선해진다.
- 카드에 담는 것은 썸네일 이미지, 클라이언트명(또는 프로젝트명), 카테고리, 연도까지 넷이다. 설명 문장을 넣지 않는다.
- **카드를 클릭해도 이동하지 않는다.** 작업물 상세 페이지는 요청 범위 밖이므로 만들지 않는다. 마우스를 올렸을 때 이미지가 아주 약하게 밝아지는 정도의 반응만 준다.
- 이미지가 아직 없더라도 `--color-line` 색의 4:3 자리표시 블록으로 자리와 비율을 유지한다. 레이아웃이 흔들리면 안 된다.
- 12개를 처음부터 다 불러오지 말고 `next/image`의 지연 로딩을 쓰되, 첫 3개는 `priority`를 준다.

### 6.4 작업 프로세스 4단계

- 배치: 데스크톱에서 가로 4열, 태블릿 2행 2열, 모바일 세로 스택. 세로로 쌓일 때는 단계 사이를 1px 선으로 잇는다.
- 각 단계는 번호(01~04), 제목, 한두 문장 설명으로 구성한다.
- 번호는 크게 키우지 않는다. 24px을 넘기지 않으며, `--color-ink-muted` 색으로 눌러 둔다.
- 네 단계의 기본 내용은 `content/process.ts`에 아래 뼈대로 넣는다.

| 번호 | 제목 | 설명의 요지 |
|---|---|---|
| 01 | 문의와 상담 | 브랜드의 현재 상태와 목표를 듣고 작업 범위, 일정, 비용을 함께 정리한다 |
| 02 | 리서치와 방향 설정 | 시장과 경쟁 브랜드를 살펴본 뒤 두세 가지 방향을 제안하고 하나를 고른다 |
| 03 | 디자인 전개 | 선택한 방향을 로고와 패키지 적용안으로 전개하고, 두 차례 수정을 거친다 |
| 04 | 최종 납품 | 인쇄용 원본 파일과 사용 가이드를 정리해 전달하고, 제작 감리를 돕는다 |

### 6.5 클라이언트 후기

- 배치: 컨테이너 안에서 최대 880px 폭의 단일 컬럼을 가운데 두고, 후기를 세로로 쌓는다. 카드 그리드로 만들지 않는다. 3번 섹션이 이미 그리드이므로 형태가 겹친다.
- 후기 개수는 3개로 시작한다.
- 각 후기는 인용문(리드 문장 크기), 그 아래 작은 글씨로 브랜드명과 역할, 작업 종류를 적는다.
- 큰 따옴표 장식이나 별점 아이콘을 넣지 않는다. 인용문과 출처의 글자 크기 차이만으로 구분한다.
- 실제 후기 문구가 아직 없으므로 자리표시 문구를 넣되, **의미 없는 로렘 입숨을 쓰지 않는다.** 실제로 있을 법한 한국어 문장을 쓰고, 각 항목에 `// TODO: 실제 후기로 교체` 주석을 단다.

### 6.6 프로젝트 문의 CTA

이 섹션이 페이지의 목적지다.

- 배치: 섹션 배경만 화면 끝까지 `--color-inverse-bg`로 반전하고, 그 안의 내용은 컨테이너 안 640px 폭 단일 컬럼에 왼쪽 정렬로 넣는다.
- 구성 순서는 섹션 제목, 한 줄 안내 문장, 문의 폼이다. 폼은 별도 페이지나 모달로 빼지 않고 이 자리에 그대로 둔다. 클릭 한 번을 더 요구할수록 제출률이 떨어진다.
- 이 섹션의 `id`는 `inquiry`로 한다. 헤더 버튼과 히어로 버튼, 모바일 하단 바가 모두 `#inquiry`로 부드럽게 스크롤한다.
- 반전 배경 위에서는 본문을 `--color-inverse-ink`로, 입력 필드는 배경 반전에 맞춰 테두리 대비를 다시 확인한다. 명도 대비 4.5:1을 반드시 지킨다.

### 6.7 푸터

- 배치: 3열. 왼쪽에 이름과 한 줄 소개, 가운데에 이메일과 전화 등 직접 연락 수단, 오른쪽에 인스타그램 등 외부 링크를 둔다. 모바일에서는 세로로 쌓는다.
- 저작권 한 줄을 맨 아래에 둔다.
- 푸터에 문의 폼을 한 번 더 넣지 않는다. 폼은 6번 섹션 하나뿐이다.

---

## 7. 문의 폼 명세

### 7.1 입력 항목

폼에 들어가는 항목은 아래 네 개이며, **이 넷 외의 항목을 임의로 추가하지 않는다.** 항목이 늘어날수록 제출률이 떨어지고, 넷은 협의된 개수다.

| 항목 | 이름(name) | 형태 | 필수 | 검증 규칙 |
|---|---|---|---|---|
| 이름 | `name` | 한 줄 텍스트 | 필수 | 앞뒤 공백을 제거한 뒤 1자 이상 40자 이하 |
| 연락처 | `contact` | 한 줄 텍스트 | 필수 | 이메일 형식이거나, 숫자와 하이픈으로 이루어진 9~20자 전화번호. 둘 중 하나면 통과 |
| 프로젝트 종류 | `projectType` | 선택 목록 | 필수 | 아래 다섯 값 중 하나 |
| 예산 범위 | `budget` | 선택 목록 | 필수 | 아래 다섯 값 중 하나 |

프로젝트 종류의 선택지는 "브랜드 로고", "패키지 디자인", "로고와 패키지 함께", "브랜드 리뉴얼", "기타"다.

예산 범위의 선택지는 "300만원 미만", "300만원~600만원", "600만원~1000만원", "1000만원 이상", "아직 정하지 못함"이다. 마지막 항목은 반드시 남긴다. 예산을 정하지 못한 사람이 폼을 포기하지 않게 하는 장치다.

연락처를 이메일과 전화로 나누지 않고 한 칸으로 받는 이유는, 방문자가 편한 수단 하나만 적으면 되게 해서 입력 부담을 줄이기 위해서다.

### 7.2 동작

- 제출은 Next.js 서버 액션(`app/actions/inquiry.ts`)으로 처리한다. 별도 API 라우트를 만들지 않는다.
- 검증 스키마는 `lib/inquiry/schema.ts`에 zod로 한 번만 정의하고, 클라이언트 즉시 검증과 서버 재검증이 같은 스키마를 쓴다. **서버 검증을 생략하지 않는다.** 클라이언트 검증은 편의이고 서버 검증이 진짜 방어선이다.
- 폼은 네 가지 상태를 갖는다. 입력 대기, 전송 중, 성공, 실패다. 전송 중에는 제출 버튼을 비활성화하고 "보내는 중"으로 라벨을 바꾼다. `useFormStatus`를 쓴다.
- 성공하면 폼 자리를 감사 메시지로 바꾸고, 언제쯤 답장이 가는지 적는다. 예: "문의가 접수되었습니다. 영업일 기준 2일 안에 답장드리겠습니다."
- 실패하면 폼 내용을 지우지 않은 채 상단에 오류 메시지를 띄운다. 사용자가 다시 입력하게 만들면 그 사람은 떠난다.
- 스팸 방지는 화면에 보이지 않는 허니팟 필드 하나로 한다. 이름은 `company`로 하고, 값이 채워져 들어오면 성공한 척 응답하되 저장하지 않는다. 캡차는 넣지 않는다. 문턱이 높아진다.

### 7.3 문의 내용이 가는 곳

문의를 어디로 받을지는 아직 정해지지 않았다. 그래서 `lib/inquiry/sink.ts`에 어댑터 하나를 두고 그 뒤를 바꿔 끼울 수 있게 만든다.

```ts
export type Inquiry = { name: string; contact: string; projectType: string; budget: string }
export type InquirySink = (inquiry: Inquiry) => Promise<void>
```

기본 구현은 두 가지다. 환경 변수 `INQUIRY_WEBHOOK_URL`이 있으면 그 주소로 JSON을 POST하고, 없으면 서버 콘솔에 기록한다. 어느 쪽이든 실패해도 사용자에게는 명확한 실패 메시지를 보여 준다. 조용히 성공한 척하지 않는다.

### 7.4 접근성

- 모든 입력에 `<label>`을 연결한다. 라벨을 없애고 플레이스홀더로 대신하지 않는다. 입력을 시작하면 사라져서 무엇을 적는 칸인지 알 수 없게 된다.
- 오류 메시지는 해당 필드 바로 아래에 두고 `aria-describedby`로 연결하며, 잘못된 필드에는 `aria-invalid="true"`를 준다.
- 제출 결과 메시지 영역에 `aria-live="polite"`를 준다.
- 키보드만으로 폼 처음부터 제출까지 갈 수 있어야 하고, 포커스 표시가 눈에 보여야 한다. `outline: none`만 주고 대체 표시를 두지 않는 것은 금지다.

---

## 8. 콘텐츠 데이터

실제 문구와 이미지는 아직 없다. 그래서 자리표시 데이터를 넣되, 나중에 교체하기 쉽게 만든다.

### 8.1 타입

```ts
// content/works.ts
export type Work = {
  slug: string          // 파일명과 key로 쓸 영문 소문자·하이픈
  title: string         // 프로젝트명
  client: string        // 클라이언트 또는 브랜드명
  category: '브랜드 로고' | '패키지 디자인' | '브랜드 리뉴얼'
  year: number
  image: string         // '/works/<slug>.jpg', 4:3 비율
  alt: string           // 이미지 설명. 빈 문자열로 두지 않는다
}
export const works: Work[] = [ /* 정확히 12개 */ ]
```

### 8.2 자리표시 규칙

- 12개는 카테고리를 섞는다. 브랜드 로고 5개, 패키지 디자인 5개, 브랜드 리뉴얼 2개 정도가 균형이 맞는다.
- 클라이언트는 카페, 코스메틱, 소규모 식품 브랜드가 고루 섞이게 짓는다. 실제 회사명을 쓰지 않는다.
- 연도는 최근 3년 안에서 흩뿌린다.
- 모든 자리표시 항목 위에 `// TODO: 실제 작업물로 교체` 주석을 단다. 이 주석이 나중에 교체 대상을 찾는 유일한 단서다.
- 이미지 파일이 없으면 `image` 값은 그대로 두고, 컴포넌트가 파일 없음을 처리해 자리표시 블록을 그린다.

---

## 9. 금지 사항

아래는 요청자가 명시한 금지 사항이므로 예외가 없다.

### 9.1 이모지를 쓰지 않는다

화면 문구, 코드, 주석, 커밋 메시지, README 어디에도 이모지를 넣지 않는다. 아이콘이 꼭 필요하면 단색 SVG를 쓰되, 아이콘 없이 글자만으로 해결되는 자리에는 넣지 않는다.

### 9.2 과하게 큰 텍스트를 쓰지 않는다

데스크톱에서 64px가 상한이고, 그 크기를 쓰는 것은 히어로의 이름 하나뿐이다. 섹션 제목을 48px 이상으로 키우거나, 프로세스 번호를 배경처럼 거대하게 깔거나, 후기 인용문을 화면 절반 높이로 키우는 배치는 모두 금지다. `clamp()`를 쓰더라도 상한은 64px를 넘지 않는다.

### 9.3 좌우 2단 배치를 반복하지 않는다

왼쪽에 이미지, 오른쪽에 글을 놓고 다음 섹션에서 좌우를 뒤집는 지그재그 배치는 쓰지 않는다. 이 페이지에서 50:50 좌우 분할은 어떤 섹션에서도 쓰지 않으며, §6 표에 적힌 일곱 개의 서로 다른 형태를 그대로 따른다.

2번 소개 섹션의 "220px 라벨 열 + 넓은 본문 열"은 비율이 크게 다른 정의 목록이므로 이 금지에 걸리지 않는다. 다만 이 형태도 페이지에서 한 번만 쓴다.

### 9.4 권장하지 않는 것 (요청자의 금지가 아니라 이 문서의 판단)

아래는 위의 세 가지와 성격이 다르다. 요청자가 금지한 것이 아니라, 위 규격을 지키려면 자연히 따라오는 판단이다. 강한 이유가 있으면 바꿔도 된다.

- 스크롤에 따라 요소가 하나씩 나타나는 등장 애니메이션. 12개 카드에 걸면 스크롤이 답답해진다.
- 그라디언트 배경, 큰 그림자, 유리 질감 효과. 작업물 이미지와 경쟁한다.
- 캐러셀. 후기 3개는 세로로 쌓는 편이 전부 읽힌다.
- 쿠키 배너, 채팅 위젯, 방문자 추적 스크립트. 요청 범위 밖이다.

---

## 10. 접근성과 성능 기준

- 본문과 배경의 명도 대비는 4.5:1 이상, 큰 글자와 UI 요소는 3:1 이상을 지킨다. 반전 배경인 6번 섹션에서 특히 확인한다.
- 제목 태그는 h1이 하나(히어로의 이름), 각 섹션 제목이 h2, 그 아래가 h3 순서로 건너뛰지 않는다.
- 모든 이미지에 의미 있는 `alt`를 준다. 장식 이미지가 아니므로 빈 `alt`를 쓰지 않는다.
- 키보드 탭 순서가 화면에 보이는 순서와 같아야 하고, 포커스 표시가 보여야 한다.
- 이미지는 `next/image`로 넣고 폭·높이를 지정해 레이아웃 이동(CLS)을 0에 가깝게 만든다.
- `prefers-reduced-motion`을 존중해 애니메이션을 끈다.
- 메타데이터는 `app/layout.tsx`의 `metadata`에 넣는다. 제목, 설명, Open Graph 이미지, `lang="ko"`를 포함한다.

---

## 11. 작업 규칙

- 한 번에 한 섹션씩 만들고, 만들 때마다 `npm run dev`로 실제 화면을 확인한다. 코드만 쓰고 화면을 보지 않은 채 다음으로 넘어가지 않는다.
- 화면 폭 360px, 768px, 1280px, 1920px 네 곳에서 확인한다. 특히 360px에서 가로 스크롤이 생기지 않는지 본다.
- 규격 수치(여백, 최대 폭, 글자 크기)는 토큰이나 `Container`에만 두고 개별 컴포넌트에 흩뿌리지 않는다.
- 커밋 메시지는 한국어 한 줄로 무엇을 했는지 적는다. 이모지를 넣지 않는다.
- 요청 범위 밖의 개선 거리를 발견하면 코드로 만들지 말고 §12 아래에 목록으로만 남긴다.

---

## 12. 완료 판정 체크리스트

아래를 전부 확인한 뒤에야 작업이 끝났다고 말한다. 하나라도 확인하지 못했으면 그 사실을 함께 보고한다.

**빌드와 검사**

- [ ] `npm run build`가 통과한다
- [ ] `npm run lint`가 통과한다
- [ ] `npm run typecheck`가 통과한다

**구조**

- [ ] 일곱 개 섹션이 명세된 순서대로 한 페이지에 있다
- [ ] 작업물이 정확히 12개다
- [ ] 프로세스가 정확히 4단계다

**규격**

- [ ] 컨테이너 최대 폭이 1280px이고 1280px 이상 화면에서 좌우 여백이 80px이다
- [ ] 360px, 768px, 1280px, 1920px에서 레이아웃이 깨지지 않고 가로 스크롤이 없다
- [ ] 데스크톱에서 64px를 넘는 글자가 없다

**목표 행동**

- [ ] 헤더 버튼, 히어로 버튼, 모바일 하단 바가 모두 `#inquiry`로 이동한다
- [ ] 폼의 네 항목이 모두 있고, 빈 값과 잘못된 형식에 한국어 오류 메시지가 뜬다
- [ ] 정상 입력으로 제출하면 성공 메시지가 뜨고, 서버 로그나 웹훅에 내용이 도착한다
- [ ] 키보드만으로 폼을 처음부터 제출까지 완료할 수 있다

**금지 사항**

- [ ] 이모지가 코드베이스 전체에 한 개도 없다 (`grep`으로 확인한다)
- [ ] 50:50 좌우 2단 배치가 어떤 섹션에도 없다

### 작업 중 발견했지만 손대지 않은 것

작업 범위 밖의 개선 거리를 여기에 적는다. 지시 없이 착수하지 않는다.

- (없음)

---

## 부록 A. 이 문서가 임의로 정한 기본값

요청서에 명시되지 않아 이 문서가 정한 것들이다. 사이트 주인이 다르게 원하면 여기만 고치면 된다.

| 정한 것 | 채택한 기본값 | 근거 | 되돌리기 |
|---|---|---|---|
| 컨테이너 1280px과 여백 80px의 관계 | 1280px은 여백을 포함한 바깥 폭이며 실제 콘텐츠 폭은 1120px | 두 수치를 함께 지키는 해석은 이것뿐이다 | 쉬움 |
| 연락처 입력 방식 | 이메일과 전화를 한 칸으로 받고 형식을 자동 판별 | 요청서의 "연락처"가 한 항목이므로 칸을 나누지 않았다 | 쉬움 |
| 예산 구간 | 300 / 600 / 1000만원을 경계로 한 다섯 구간 | 소규모 브랜드 로고·패키지 작업의 통상 범위 | 쉬움 |
| 문의 접수처 | 어댑터 + 웹훅 환경 변수, 없으면 콘솔 기록 | 실제 받을 곳이 정해지지 않았다 | 쉬움 |
| 작업물 상세 페이지 | 만들지 않는다 | 요청 범위에 없다 | 쉬움 |
| 강조색 | 딥 그린 `#2E4B3F` 하나 | 카페·코스메틱 작업물 이미지와 충돌하지 않는 채도 | 쉬움 |
