# 화면 명세 (06-d) — mobile-rosary V1 · 제2판

> 서식 정본: [`docs/prototype-vocabulary.md` §7-2](../../docs/prototype-vocabulary.md).
> 입력: [`06-prd.md`](./06-prd.md) 제3판의 FR-## · [`06-design-system.md`](./06-design-system.md) · [`06-service-design.md`](./06-service-design.md)의 정보 구조.

**이 판을 다시 쓴 이유 (2026-09-05).** 공방장 정정으로 홈이 여정 목록이 되고 로그인 · 새 기도 시작 · 여정 상세 · 설정 · 여정 완주 화면이 생겼다. 화면은 여덟, 시트는 일곱이다. **기도 화면(B)의 규칙은 제1판 그대로다** — 실제 묵주, 알의 다섯 상태, 기도문 영역 고정, 얼굴 가리지 않기, 비화면 채널의 상수. 바뀐 것은 그 화면의 머리 한 줄(며칠째)과 낭송 방식이 셋이 된 점, 손 없이 조작 채널이 더해진 점이다.

화면 사이의 연결과 시나리오 검증은 [`06-screen-map.md`](./06-screen-map.md)(06-e)가 갖고 있으며, 그 검증이 이 판에서 잡아낸 결함의 처방이 **[06-e 보강 2026-09-05]** 표시로 들어와 있다. 제1판의 보강 표시 **[06-e 보강 2026-09-01]** 도 그대로 두었다.

---

## 화면 L — 로그인

**목적 한 줄**: "어느 계정으로 들어가나"에 단추 둘로 답한다. 그 밖에는 아무것도 묻지 않는다.

**진입·이탈**: 앱을 처음 열었을 때, 또는 로그아웃 뒤. 이탈은 홈(A)뿐. 뒤로 가기 = 앱 종료.

**요소 표**

| 요소 | 상태 | 각 상태에서 | FR |
|---|---|---|---|
| 앱 이름과 한 줄 | 항상 | 세리프. "바람 하나를 위해 54일을 함께 바칩니다" 같은 한 줄. 마케팅 문구 금지 | — |
| 구글로 계속 | 기본 / 진행 중 | 제공자 인증 창을 연다. 진행 중에는 단추가 로딩 상태 | FR-32 |
| Apple로 계속 | 기본 / 진행 중 | 같다. iOS 필수(PRD §11-3) | FR-32 |
| 오류 한 줄 | 숨김(기본) / 표시 | 실패·취소 시 치자색 한 줄. 붉은색 금지 | 오류표 |

**문구 표**

| 자리 | 확정 문구 |
|---|---|
| 단추 | `구글로 계속` / `Apple로 계속` |
| 실패 | `로그인이 되지 않았습니다. 다시 시도해 주세요.` |
| 아래 작은 글 | `계정은 여정과 자리를 기기 사이에 맞추는 데만 씁니다.` |

**빈·오류 상태**: 연결 없음 = 단추 비활성 + `연결이 필요합니다` 한 줄.

**명세 밖**: 이메일 가입, 비밀번호, 게스트 모드("계정 없이 기도하기" — 정정 9로 없다), 온보딩 캐러셀.

---

## 화면 A — 홈 (여정 목록)

**목적 한 줄**: "내가 바치고 있는 기도가 무엇이고 오늘 어디까지 왔나"에 카드를 훑는 것으로 답한다. 카드를 누르면 곧 기도다.

**진입·이탈**: 로그인 뒤 유일한 뿌리. 이탈 — 여정 카드 탭(→ 기도 B) · 새 기도(→ N) · 카드 "자세히"(→ D) · 설정(→ E) · 카드 길게 누르기(→ S6). 뒤로 가기 = 앱 종료.

**요소 표**

| 요소 | 상태 | 각 상태에서 | FR |
|---|---|---|---|
| **여정 카드** | 진행 중 · 오늘 아직 / 진행 중 · 오늘 이어갈 자리 있음 / 진행 중 · 오늘 바쳤음 / **시작 전**(시작일이 미래) / 마침 / 조 기도 | 첫 줄 **바람**(세리프). 둘째 줄 **며칠째** — 54일이면 `23일째 · 청원` 또는 `41일째 · 감사`, 9일이면 `4일째`, 날마다면 `N번째 날`. 셋째 줄 **오늘 자리** — `아직` / `제3단 4번째 알부터 이어서 · 어제 저녁` / `오늘 바쳤습니다`. 리본(하루 한 칸, 빈 날은 빈 칸). 조 기도면 넷째 줄 `내 몫 제2단 · 오늘 다섯 중 셋`. **탭 = 바로 기도.** 이어갈 자리가 있으면 그 자리부터, 없으면 오늘 것의 처음부터. 시작 전 카드는 흐리게, 탭하면 `N일 뒤에 시작합니다` 한 줄 **[06-e 보강 2026-09-05]** (결함 8). 마친 카드는 목록 아래, 탭하면 여정 상세 | FR-02·03·04·35·36·40·42 |
| 카드의 "자세히" | 기본 | 카드 우측 작은 글. 탭 = 여정 상세 D | FR-37 |
| 카드 길게 누르기 | — | 여정 삭제 확인 시트 S6. 조 기도면 문구가 다르다 — 만든 사람은 `조를 해산합니다`, 조원은 `조에서 나옵니다` **[06-e 보강 2026-09-05]** (결함 9) | FR-05·40 |
| **새 기도** | 기본 | 목록 아래 큰 단추. 탭 = 새 기도 시작 N | FR-33 |
| **초대 코드로 들어가기** | 기본 | 새 기도 아래 작은 글. 탭 = 코드 여섯 자리 입력 → 맞으면 조 여정 카드가 홈에 생긴다. 만든 사람은 코드를 나누고 받은 사람은 여기서 넣는다 **[06-e 보강 2026-09-05]** (결함 12) | FR-40 |
| 설정 | 기본 | 우상단 하나. 탭 = 설정 E | — |
| 연결 없음 한 줄 | 숨김(기본) / 표시 | 상단 작은 글. 치자색 아님 — 회색(정보) | §8 |
| 음성 없음 안내 | 숨김(기본) / 표시 | 한국어 음성 부재 시에만. 치자색 | 오류표 |
| 소개 (i) | 기본 | 설정 옆 작은 원. 탭 = 소개 시트 S7. 08 검증 참가자용 | — |

**문구 표**

| 자리 | 확정 문구 |
|---|---|
| 며칠째 | `{N}일째 · 청원` / `{N}일째 · 감사` / `{N}일째` / `{N}번째 날` |
| 오늘 자리 | `아직` / `{신비} · 제{N}단 {M}번째 알부터 이어서 · {시각}` / `오늘 바쳤습니다` |
| 시각 | `방금` · `{N}분 전` · `어제 저녁` 처럼 상대 표기 |
| 시작 전 | `{N}일 뒤에 시작합니다 · {시작일}` |
| 조 기도 줄 | `내 몫 제{N}단 · 오늘 다섯 중 {K}` |
| 마침 | `{첫날} ~ {마지막 날} · {바친 날}일` |
| 새 기도 | `새 기도` |
| 초대 코드 | `초대 코드로 들어가기` / 입력 자리 `여섯 자리 코드` / 틀림 `이 코드의 기도를 찾지 못했습니다.` / 정원 초과 `이 기도는 다섯 명이 다 모였습니다.` |
| 빈 홈 | `아직 바치는 기도가 없습니다.` / `바람 하나를 적고 시작해 보세요. 54일이든 하루든, 끊겨도 그 자리가 남습니다.` |
| 연결 없음 | `연결되면 맞춰 둡니다.` |
| 음성 없음 | `이 기기에 한국어 음성이 없어 소리 없이 진행됩니다. 진동과 받는 사이는 그대로 동작합니다.` |

**빈·오류 상태**: 로그인 직후 = 여정 없음. 위 빈 홈 문구 두 줄과 "새 기도" 단추만. **이것이 정상 상태다.** 저장 실패 시 안내 1회.

**명세 밖 (일부러 없음)**: 탭 바, 추천 · 카탈로그 · 캐러셀, 오늘의 신비 머리(여정마다 다르므로 카드 안 자리 줄에 들어간다), 연속 기록 · 순위 · 불꽃, 조원별 완료 여부(조 카드는 "다섯 중 셋"까지만).

---

## 화면 N — 새 기도 시작 (세 걸음)

**목적 한 줄**: "무엇을 위해, 어떤 형식으로, 언제부터 바치나"를 걸음마다 하나씩 묻는다.

**진입·이탈**: 홈의 "새 기도"에서만. 이탈 — 마지막 걸음의 `오늘 바로 시작`(→ 기도 B) · `홈으로`(→ A, 카드가 생긴다) · 닫기(→ A, 만들지 않음). 뒤로 가기 = 앞 걸음.

**요소 표**

| 걸음 | 요소 | 상태 | 각 상태에서 | FR |
|---|---|---|---|---|
| 1/3 | 바람 입력 | 비어 있음 / 입력 중 / 24자 도달 | 세리프로 크게. 자리 글 `누구를, 무엇을 위해 바치나요`. 비어 있으면 다음으로 못 간다 | FR-33 |
| 2/3 | 형식 | 54일 기도(기본) / 9일 기도 / 날마다 | 셋 중 하나. 각 항목 아래 한 줄 설명 | FR-33 |
| 2/3 | 혼자 / 함께 | 혼자(기본) / 함께 바치기 | 함께를 고르면 `다섯이 한 단씩 나눕니다` 한 줄. 정원과 초대 방식은 만든 뒤 상세에서 | FR-40 |
| 3/3 | 시작일 | 오늘(기본) / 다른 날 | 날짜 고르기. 지난 날도 허용 — `지난 {N}일은 비어 있는 채로 시작합니다` | FR-33 |
| 3/3 | 기도문 판본 | 설정의 기본값 | 탭 = 판본 시트 S4 | FR-34 |
| 3/3 | **완료 예정일** | 계산됨 | `{완료 예정일}에 마칩니다` — 54일은 시작일 + 53일. 날마다면 이 줄이 없다 | FR-33 |
| 3/3 | 첫날의 신비 미리 보기 | 계산됨 | `첫날은 {신비}입니다` | FR-43 |
| 3/3 | 단추 둘 | 기본 | `오늘 바로 시작` / `홈으로`. 시작일이 미래면 첫 단추 대신 `만들기` | FR-33 |

**문구 표**

| 자리 | 확정 문구 |
|---|---|
| 걸음 표시 | `1 / 3` `2 / 3` `3 / 3` |
| 54일 설명 | `27일 청원, 27일 감사. 하루에 다섯 단.` |
| 9일 설명 | `아홉 날, 하루에 다섯 단.` |
| 날마다 설명 | `끝나는 날을 정하지 않습니다.` |
| 함께 바치기 설명 | `다섯이 한 단씩 나눕니다. 초대 코드로 모입니다.` |
| 완료 예정일 | `{M}월 {D}일에 마칩니다.` |
| 바람 비어 있음 | `바람을 한 줄 적어 주세요.` |

**빈·오류 상태**: 바람 비어 있음(위). 연결 없음에서 함께 바치기를 고르면 `조는 연결된 뒤 만들 수 있습니다` — 혼자로만 만들 수 있다 **[06-e 보강 2026-09-05]** (결함 10).

**명세 밖**: 사진 · 색 고르기, 알림 시각, 기간 임의 입력(형식 셋 외).

---

## 화면 B — 기도

**목적 한 줄**: "지금 내 차례인가, 어디쯤인가"를 화면을 응시하지 않고도 알린다.

**진입·이탈**: 홈의 여정 카드 · 새 기도의 `오늘 바로 시작` · 여정 상세의 `오늘 처음부터`(확인 뒤)에서만. 이탈 셋 — 잠시 멈춤(자리 저장 → 홈) / 여기서 끝내기(오늘 자리 삭제 → 홈) / 완주(→ C). 앱 이탈(전화·홈 버튼) = 자동으로 자리 저장 후 **멈춤 상태**로 정지하고, 돌아오면 그 상태의 기도 화면이 보인다 **[06-e 보강 2026-09-01]**.

**요소 표**

| 요소 | 상태 | 각 상태에서 | FR |
|---|---|---|---|
| **가운데 알 (시그니처)** | 읽는 중 / 내 차례 / 진동 진행 / 단 전환 / 멈춤 | 읽는 중 = 테두리가 읽는 절 길이에 맞춰 차오름 · 내 차례 = 4초 호흡 · 진동 진행 = 진동에 맞춰 한 번 반짝 · 단 전환 = 확장 맥동 · 멈춤 = 호흡이 멎고 흐려진다. **묵주는 설정에서 고른 것**으로 그려지고, 어느 묵주든 이 다섯 상태를 표현한다 | FR-15·21·39 |
| 멈춤 상태 단추 둘 | 멈춤일 때만 | `이어서 바치기` · `홈으로`(자리는 남는다) | FR-17·18 |
| 알 속 숫자 | 성모송 중 / 그 외 | `N / 10` (시작 기도는 `/ 3`) · 그 외는 `·` | FR-16 |
| 5단 막대 | 지남 / 지금 / 남음 | 조작 불가. 조 기도면 내 단 하나만 강조되고 나머지 넷은 조원 몫으로 흐리게 | FR-16·40 |
| 머리 두 줄 | 항상 | 첫 줄 **바람**(말줄임) + `· {N}일째`. 둘째 줄 단계 이름 | FR-31·35 |
| 기도문 앞 절 | 읽는 중 강조 / 내 몫(읽지 않기) | 세리프 `prayer-lg`. 여정의 판본으로 | FR-11·34 |
| 기도문 뒷 절 | 대기(회) / 내 차례(치자) / 앱이 읽는 중(전부 읽기) | 색 전환 0.5s | FR-11 |
| 배경 성화 | 신비마다 | 원본 해상도. 인물의 이목구비 영역을 묵주가 가리지 않는다 | FR-22·23 |
| 기도문 영역 | 고정 높이 | 긴 기도문(사도신경)도 이 안에서 처리. 배경이 잘리는 양이 변하지 않는다 | FR-24 |
| 잠시 멈춤 | 기본 | 부제 `자리가 남습니다` | FR-18 |
| 여기서 끝내기 | 기본 | 부제 `오늘 처음부터` | FR-18 |

**비화면 채널 명세 (§7-3) — 이 화면의 절반은 소리와 진동이다**

| 채널 | 규칙 |
|---|---|
| 음성 | 낭송 방식이 **교대**면 앞 절만, **전부 읽기**면 앞뒤 절 다, **읽지 않기**면 없음(FR-06·11). 속도 0.92. 신비 선포 뒤 해설은 읽지 않는다 |
| 받는 사이 | 사용자가 바칠 절의 글자수를 L이라 하면 `(900ms + L × 132ms) ÷ 속도계수(0.75/1/1.35)`. 교대 = 뒷절 글자수 · 읽지 않기 = 앞절 + 뒷절 글자수 · 전부 읽기 = L을 0으로 두어 900ms만. 신비 선포는 고정 2200ms |
| 진동 사전 | 알 넘어감 `18ms` 1회 · 단 전환 `28-60-28-60-28` · 읽지 않기 켬(설정에서) `20-50-20` · 완주 `200ms` 1회 |
| **손 없이 조작** (설정에서 켠 경우) | 흔들기 1회 = 다음 알(사이가 남아도 즉시). 이어폰 리모트 — 재생/일시정지 = 잠시 멈춤/이어서, 다음 = 다음 알, 이전 = 이전 알. **음량 버튼은 Android에서만** — iOS 심사 지침(PRD §11-3). 한 번의 조작은 **한 알**만 움직인다. 되돌아간 알은 자리 저장이 그 알로 되돌아간다 |
| 무음·무진동·무센서 기기 | 사이만으로 진행. 오류 없음. 센서가 없으면 설정에서 흔들기 항목을 감춘다 |

**문구 표**: 조작 문구는 위 버튼 둘(멈춤 상태에서는 다른 둘)뿐. **"받아 바치세요" 같은 지시문을 두지 않는다** — 알의 호흡이 그 역할이다.

**빈·오류 상태**: 해당 없음(홈을 거쳐야만 진입).

**명세 밖**: 배속, 건너뛰기 단추(손 없이 조작의 앞/뒤는 **한 알**이고 단추가 아니다), 낭송 방식 · 묵주 · 판본 · 사이 조절(전부 밖에 있다 — 06-b §9 23번), 화면 내 텍스트 스크롤.

---

## 화면 C — 완주 · C′ — 여정 완주

**목적 한 줄**: 성취 축하가 아니라 **마침**을 확인한다. 조용히. 하루의 마침(C)과 여정의 마침(C′)은 다른 화면이다.

**진입·이탈**: C는 77단계(조 기도면 내 단 14단계) 완료 시에만. 이탈 = 돌아가기(홈, 오늘 자리 삭제됨, 리본에 오늘이 채워짐). **오늘이 여정의 마지막 날이면 C의 돌아가기가 C′로 간다.** C′의 이탈 = 돌아가기(홈, 여정이 "마침"으로 목록 아래로).

**요소 표 — C**

| 요소 | 상태 | 각 상태에서 | FR |
|---|---|---|---|
| 십자가 심볼 | 항상 | SVG 세선 | — |
| 머리 문구 | 항상 | `다 바쳤습니다` — "완료·달성·축하" 금지 | FR-19 |
| 바람 줄 | 항상 | `{바람}을 위하여 · {N}일째` | FR-31·35 |
| 통계 셋 | 항상 | 성모송 50(조 기도면 10) · 걸린 분(기도 화면에 머문 시간의 합, 멈춘 시간 제외) · 이어서 N번(0이어도 숨기지 않는다) | FR-19 |
| 조 기도 줄 | 조 기도일 때만 | `오늘 다섯 중 {K}가 바쳐졌습니다` — 누가 안 했는지는 적지 않는다 | FR-40 |
| 돌아가기 | 기본 | 홈으로. 마지막 날이면 C′로 | — |

**요소 표 — C′**

| 요소 | 상태 | 각 상태에서 | FR |
|---|---|---|---|
| 머리 문구 | 항상 | `{N}일을 다 바쳤습니다` (54 또는 9) | FR-19 |
| 바람 줄 | 항상 | `{바람}을 위하여` — 첫날에 적은 그대로 | FR-19 |
| 여정 셋 | 항상 | 첫날 · 마지막 날 · 바친 날 수(`54일 중 51일`처럼 — 빈 날을 숨기지 않고 벌하지도 않는다) | FR-35·37 |
| 리본 | 항상 | 하루 한 칸 54칸. 빈 칸은 빈 채로 | FR-35 |
| 돌아가기 | 기본 | 홈으로 | — |

**명세 밖**: 공유 카드(SNS), 다음 기도 예약, 연속 기록, 조원 이름별 완료 표.

---

## 화면 D — 여정 상세

**목적 한 줄**: "이 기도는 어떻게 왔고 언제 끝나나"에 기록으로 답한다. 기도로 들어가는 자리는 아니다(그것은 홈 카드다).

**진입·이탈**: 홈 카드의 "자세히", 마친 카드 탭. 이탈 — 돌아가기(홈) · `오늘 처음부터`(S3 확인 → 기도 B).

**요소 표**

| 요소 | 상태 | 각 상태에서 | FR |
|---|---|---|---|
| 머리 | 항상 | 바람(세리프) · 형식 · 첫날 ~ 완료 예정일 · 판본 이름 | FR-33·34 |
| 진척 | 진행 중 / 마침 | `{N}일째 · 청원/감사 · 남은 {K}일`. 리본 54칸(9칸) — 바친 날 채움, 빈 날 빈 칸, 오늘 강조 | FR-35 |
| 날짜별 기록 | 항상 | 리본의 칸을 누르면 그날 — 바쳤는가, 어느 신비였는가, 걸린 분. 조 기도면 그날 내 몫이 몇 단이었는지도 | FR-37 |
| 오늘 처음부터 | 오늘 자리가 있을 때만 | 탭 = S3 확인 시트 → 자리 삭제 → 기도 | FR-18 |
| **조 영역** | 조 기도일 때만 | 초대 코드(복사 · 나누기) · 조원 목록(표시 이름, 정원 5 중 N) · 오늘 배정(누가 몇 단 — 이름 옆에 완료 표시는 두지 않는다) · 내가 만든 조면 `조 해산`, 조원이면 `조에서 나가기`(각각 S6로 확인) **[06-e 보강 2026-09-05]** (결함 9) | FR-40·44 |
| 여정 지우기 | 진행 중 / 마침 | 하단 작은 글. S6 확인 | FR-05 |

**문구 표**

| 자리 | 확정 문구 |
|---|---|
| 남은 날 | `남은 {K}일` / 마지막 날은 `오늘이 마지막 날입니다` |
| 조원 수 | `조원 {N} / 5` |
| 초대 | `초대 코드 {코드}` · 단추 `나누기` |
| 조 해산 확인 | `조를 해산합니다. 조원 모두의 카드에서 이 기도가 사라집니다.` / `해산` · `두기` |
| 조 나가기 확인 | `조에서 나옵니다. 남은 조원에게 단이 다시 배정됩니다.` / `나가기` · `두기` |

**명세 밖**: 바람 고치기(첫날의 마음을 그대로 두는 것이 이 화면의 뜻이다 — 고치려면 새 여정), 형식 바꾸기, 기록 내보내기.

---

## 화면 E — 설정

**목적 한 줄**: "이 앱이 나를 어떻게 대하게 할 것인가"를 한 화면에 위계 있게 둔다(06-b §9 15번 — 평평한 목록이 아니다).

**진입·이탈**: 홈 우상단. 이탈 = 돌아가기(홈). 시트 넷(S2·S4·S5·계정 삭제 확인)은 이 화면 위에 뜬다.

**요소 표** (묶음 셋)

| 묶음 | 요소 | 상태 | 각 상태에서 | FR |
|---|---|---|---|---|
| **바치는 방식** | 낭송 방식 | 전부 읽기 / **교대**(기본) / 읽지 않기 | 셋 중 하나. 각 항목 아래 한 줄 설명. 읽지 않기를 고르면 진동 3연타로 예고 | FR-06 |
| | 받는 사이 | 느리게 / 보통 / 빠르게 | 탭 = S2 | FR-07 |
| | 손 없이 조작 | 꺼짐(기본) / 켜짐 | 켜면 아래에 `흔들기 · 이어폰 버튼`(iOS) 또는 `흔들기 · 이어폰 · 음량 버튼`(Android) 설명 한 줄. 센서 없는 기기는 흔들기 항목을 감춘다 | FR-38 |
| **보이는 것** | 묵주 | 셋 이상 중 하나 | 탭 = S5. 현재 묵주의 작은 미리보기 | FR-39 |
| | 기본 기도문 판본 | 목록 중 하나 | 탭 = S4. `새로 만드는 기도에 적용됩니다` 한 줄 | FR-34 |
| | 밤 / 낮 | 밤(기본) / 낮 / 기기 따라 | — | 06-b §2-2 |
| **계정** | 계정 | 표시 이름 · 제공자 | 정보. `연결 상태 · 마지막 맞춤 {시각}` 한 줄 | FR-41 |
| | 로그아웃 | 기본 | 한 번 확인. **이 기기의 자리와 여정 사본을 지운다**(다른 사람이 같은 기기에 로그인할 수 있으므로) **[06-e 보강 2026-09-05]** (결함 11) | FR-45 |
| | 계정 삭제 | 기본 | 확인 시트. `서버의 모든 기도와 기록이 지워집니다. 조에 있었으면 조에서 빠집니다.` / `지우기` · `두기` | FR-45 |
| | 소개 (i) | 기본 | S7 | — |

**문구 표**

| 자리 | 확정 문구 |
|---|---|
| 전부 읽기 | `전부 읽기 — 앱이 처음부터 끝까지 읽습니다` |
| 교대 | `교대 — 앞 절은 앱이, 뒷 절은 직접 바칩니다` |
| 읽지 않기 | `읽지 않기 — 소리 없이 진동으로만 넘어갑니다` |
| 손 없이 조작 | `손 없이 조작 — 폰을 흔들면 다음 알로, 이어폰 버튼으로 앞뒤로` |
| 로그아웃 확인 | `이 기기에서 나갑니다. 기도와 기록은 계정에 남습니다.` / `나가기` · `두기` |

**명세 밖**: 알림, 언어, 글자 크기(시스템 설정을 따른다), 통계, 구매 복원(V1은 무료 검증 단계).

---

## 시트 일곱 (요약)

| 시트 | 목적 한 줄 | 확정 문구 핵심 | 어디서 | FR |
|---|---|---|---|---|
| S2 받는 사이 | 세 단계에서 고른다 | `느리게 — 천천히 바침` / `보통 — 기본` / `빠르게 — 익숙한 분` | 설정 | FR-07 |
| S3 오늘 처음부터 확인 | 오늘 자리를 지우기 전에 한 번 묻는다 | `오늘 자리를 지우고 처음부터 바칩니다.` / `처음부터` · `아니요, 이어서` | 여정 상세 | FR-18 |
| S4 기도문 판본 | 어느 문구로 바치나 | 판본마다 이름과 **성모송 첫 줄 미리 보기**. 목업은 둘로 시늉 | 새 기도 3/3 · 설정 | FR-34 |
| S5 묵주 고르기 | 어느 묵주로 바치나 | 셋 이상. 고르면 즉시 큰 미리보기. 이름은 재질로(`나무` `진주` `유리`) | 설정 | FR-39 |
| S6 여정 삭제 · 조 해산 · 조 나가기 확인 | 되돌릴 수 없는 일 전에 한 번 묻는다 | 혼자: `이 기도를 지웁니다. 기록도 함께 지워집니다.` / `지우기` · `두기`. 조: 여정 상세 문구 표 | 홈 길게 누르기 · 여정 상세 | FR-05·40 |
| S7 소개 (i) | 이 시제품이 무엇을 검증하는지 | 검증 항목과 "아직 아닌 것"을 함께 | 홈 · 설정 | — |
| 계정 삭제 확인 | 서버의 모든 것을 지우기 전에 | 설정 문구 표 | 설정 | FR-45 |

---

## 이 명세가 잡아낸 것 — 제1판의 3건과 이 판의 5건

제1판(2026-08-29)이 잡은 셋 — 완주 진동 없음 · "알 부터" 띄어쓰기 · 명세 밖 목록 부재 — 은 전부 반영돼 있다.

이 판을 쓰면서 걸린 다섯은 [`06-screen-map.md`](./06-screen-map.md) §4의 결함 8~12다 — 시작일이 미래인 카드를 누르면 무엇이 되나(8) · 조 기도에서 "지우기"가 해산인가 나가기인가(9) · 연결 없이 조를 만들 수 있나(10) · 로그아웃 뒤 기기에 남은 자리는 누구 것인가(11) · 초대 코드를 받은 사람은 어디에 넣나(12). 처방은 위 표에 **[06-e 보강 2026-09-05]** 로 들어와 있다.
