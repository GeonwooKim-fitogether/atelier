# 07 브리프 — mobile-rosary V1

> 서식의 정본은 [`docs/stage-07-procedure.md`](../../../docs/stage-07-procedure.md) §3이다. 이 문서의 내용은 새로 쓴 것이 아니라 01·03·05·06 산출물에서 옮긴 것이다.
>
> **이 브리프의 가장 중요한 발견은 채운 칸이 아니라 빈 칸이다.** 아래에서 `[디자인 토큰]`과 `[레퍼런스]` 두 절이 비어 있으며, 그 둘이 비어 있는 것이 지금 07-b(목업)가 시작되지 못하는 이유다. 서식의 목적 절반이 이 발견이다.

---

## [제품 한 줄]

출처: `05-prioritization.md` §3 방향성 문장.

> 매일 묵주기도하는 한국 가톨릭 신자들이, 잠자기 전과 대중교통에서 손이 자유롭지 않고 화면을 보기 어려운 상황에서, 묵주와 기도문 없이는 몇 알째인지 놓치고 졸음과 잡생각으로 5단을 끝까지 바치지 못하는 문제를 겪고 있기 때문에, **앞 절을 읽어 주고 뒷 절을 받게 하며, 소리와 진동만으로 진행되고, 끊기면 그 자리를 저장해 이어 주는** 기능들이 필요하다.

## [이 판이 답하려는 질문 하나]

출처: `06-service-design.md` DQ-05 판정.

> **화면을 보지 않고 손을 쓰지 않고도 5단을 끝까지 갈 수 있을까?**

## [누가 쓰나]

출처: `03-analysis.md` §1 Persona 그룹. 다섯 중 둘만 옮긴다.

| | 핵심 (Persona A) | 두 번째 (Persona B) |
|---|---|---|
| 누구 | 30대 직장인 신자. 세례명 마리아 | 60대 은퇴 신자. 세례명 안나 |
| 언제·어디서 | 매일 1회. 잠자기 전 또는 퇴근길 대중교통. AirPods | 아침. 집과 본당. 종이 기도문책을 보며 |
| 무엇이 불편한가 | 묵주·기도문을 안 가지고 다님. 신비 묵상·청원·감사 기도문을 못 외움. 졸음과 잡생각. 54일 기도를 빼먹으면 처음부터. 여러 지향(2~3개) 관리 어려움. 기존 앱 UX 투박. 화면이 남에게 보이는 것을 의식 | 작은 글씨. 복잡한 앱 조작. 기도문책이 무거움 |
| V1에서 어디까지 챕기나 | 전부 | **글씨 크기 확대 상태에서 화면이 깨지지 않는 것까지만.** 큰 글씨 모드는 V1.5 |

## [지금 쓰는 대안과 그 장점]

출처: `01-intake.md`, `03-analysis.md` §2.

Marcello Pietrelli의 "성 묵주 기도와 하느님의 자비심 기도". 장점 셋은 **단순함, 무료, 광고 없음**이고, 묵주 넘기는 화면이 직관적이다.

**우리가 깨지 말아야 할 것은 단순함이다.** `03-analysis.md` 반증 3번이 "경쟁자의 1등 가치가 단순함이므로 기능을 더하면 그 가치를 우리 손으로 깬다"고 적었고, `05-prioritization.md`가 이를 지키는 방식으로 **"기능은 두고 화면에서 감춘다"** 를 택했다. 이 문장이 아래 `[금지]`의 첫 줄이 되는 근거다.

## [화면 목록]

출처: `06-service-design.md` §2 정보 구조. **탭 바가 없다.** 화면 셋과 시트 둘이다.

| 화면 | 무엇이 놓이나 |
|---|---|
| **홈** (유일한 진입점) | 위에서부터 — 이어가기 카드(저장된 자리가 있을 때만) · 큰 시작 버튼 · 지향 선택(작게) · 침묵 진행 토글 · 받는 사이 설정 열기 |
| **기도 화면** | 현재 단과 신비 이름 · 현재 알 번호(1~10) · 앞 절(앱이 읽는 부분) · 뒷 절(사용자가 받는 부분, 받을 차례에 강조색으로 바뀌고 "받아 바치세요"가 나타난다) · 하단 버튼 둘("잠시 멈춤·자리 저장", "여기서 끝내기") |
| **완주 화면** | "5단을 마쳤습니다" 한 줄과 홈으로 돌아가기. 저장된 자리를 지운다 |
| 지향 시트 | 지향 목록·추가·선택. 지향마다 진행 자리가 따로 저장된다 |
| 받는 사이 시트 | 느리게 · 보통 · 빠르게 세 단계 |

화면 전환 규칙: 기도 화면에서 "잠시 멈춤"은 홈으로 가며 이어가기 카드를 만들고, "여기서 끝내기"는 홈으로 가며 자리를 지운다.

## [핵심 동작]

출처: `05-prioritization.md` DQ-01·DQ-02 판정, `06-service-design.md` US-01~US-04. **이 셋이 V1의 전부다.**

1. **교대 낭송** — 앱이 성모송 앞 절을 읽고 멈춘다. 받는 사이 동안 기다렸다가 다음 알로 넘어간다. 받는 사이는 **뒷 절 글자 수 × 132ms + 900ms**이고, 느리게는 ÷0.75, 빠르게는 ÷1.35. **음성 인식을 쓰지 않으므로 사용자가 실제로 받았는지 앱은 모른다.** 이 사이가 실제 호흡에 맞는지가 07-c가 답할 첫 질문이다.
2. **침묵 진행** — 켜면 소리를 전혀 내지 않고, 알마다 짧은 진동 한 번, 단이 바뀔 때 진동 세 번. 앱이 읽어 주지 않으므로 받는 사이는 앞 절 몫까지 길어진다. 진동을 지원하지 않는 기기에서는 사이만으로 진행하고 오류를 내지 않는다.
3. **끊기면 이어 준다** — 어떤 이유로든(앱 종료·전화·졸음) 중단되면 자리가 저장되고, 다음에 열면 홈 맨 위에 이어가기 카드가 뜬다. 5단을 마치면 지운다. 지향별로 따로 저장한다.

세 번째의 성격을 못 박아 둔다. `01-intake.md` 2026-08-28 정정에 따르면 통증은 끊김 자체가 아니라 **"한 번에 해야 한다"는 규범 때문에 끊긴 기도를 재개하지 못해 그날을 통째로 잃는 것**이다. 그래서 이것은 편의 기능이 아니라 **완주율을 지키는 기능**이다.

## [디자인 토큰] — 아직 비어 있음 (이제 채울 수 있다)

**레퍼런스가 들어왔으므로 이 절을 막는 것은 하나로 줄었다** — 밝은 바탕이냐 어두운 바탕이냐의 판정이다. 레퍼런스가 `03-analysis.md` §5의 "다크 모드 기본" 가설과 어긋나며, 그 어긋남과 판정에 필요한 사실은 [`refs/README.md`](./refs/README.md) §5·§6에 정리돼 있다. 판정은 공방장 몫이다.

**06단계에 디자인 시스템 절이 없다.** `06-service-design.md`는 유저 스토리·정보 구조·백오피스·톤 넷으로 끝난다. `prototype-vocabulary.md` §4가 "디자인 시스템을 06으로 올린다"고 정했으나 아직 이행되지 않았다.

**그래서 07-b(목업)를 시작할 수 없다.** 07-b의 넘어가는 조건이 "06의 디자인 시스템을 실제로 적용했다"인데 적용할 대상이 없다.

참고로, `07-prototype/v1/index.html`이 이미 쓰고 있는 색이 있다. **확정이 아니라 후보이며, 레퍼런스를 보고 06에서 정할 때의 출발점으로만 쓴다.**

| 자리 | v1이 쓰는 값 | 성격 |
|---|---|---|
| 바탕 (어두움) | `#0b0b0d` · `#141418` · `#1c1c22` | 잠자기 전 사용이므로 어두운 바탕이 기본이라는 `03-analysis.md` §5 가설과 맞는다 |
| 강조 (촛불색) | `#d9c08a` · `#1a1508` | 따뜻한 금빛 하나만 강조색으로 쓴다 |
| 보조 | `#a99bd6`(보라) · `#7fd1a8`(초록) · `#8e8e99`(회색) | 절기 색 가설과 관련될 수 있으나 근거가 문서에 없다 |
| 글꼴 | Pretendard (없으면 Apple SD Gothic Neo) | 한글 자형이 정리된 폰트 |

06에서 정해야 하는 것은 위 색 값이 아니라 다음 다섯이다 — 색(라이트 모드 포함), 글자 크기 단계(5개 이내), 간격 단위(4의 배수), 컴포넌트별 상태(기본·눌림·비활성·빈 상태), 모션 속도.

## [레퍼런스] — 채워졌다 (2026-09-05)

공방장이 33장을 골라 넣었다. 폴더별 역할과 실제로 읽어 낸 색 값은 [`refs/README.md`](./refs/README.md)에 있다.

| 폴더 | 장수 | 무엇의 기준인가 |
|---|---|---|
| `refs/01-sacred-images/` | 17 | 앱 안에 실릴 그림의 톤. **열일곱 중 열둘이 같은 가족** — 두껍게 칠한 물감·석고 부조 질감에 따뜻한 백색 바탕과 낮은 채도의 금빛(색상 40도, 채도 23~34) |
| `refs/02-captures/webflow/` | 10 | 화면의 분위기와 배치. 세 사이트가 섞여 있고 방향이 서로 다르다 — Ovelle Holm(밝은 종이, 세리프 제목, 자간 넓은 소문자 라벨) · Focused(검은 바탕, 채도 71의 형광 금색) · Liore(어두운 포트폴리오) |
| `refs/02-captures/awwwards/` | 4 | 무채색 극단 대비와 큰 글씨 |
| `refs/02-captures/savee/` | 2 | 글자 자체를 재료로 쓰는 조판 |

**가장 직접 쓰이는 것은 `webflow/04·05·06`(Ovelle Holm)이다.** 휴대폰 폭(약 460×910)으로 잡혀 있고, 조판 규칙이 이 앱의 화면에 그대로 대응한다 — 세리프 제목은 단·신비 이름에, 자간을 넓게 벌린 작은 대문자 라벨은 알 번호에, 테두리만 있는 알약 단추는 시작 단추에, 따뜻한 오프화이트 판은 이어가기 카드에.

**빠진 것 하나**: 지금 쓰는 Marcello 앱의 캡처가 없다. 우리가 깨지 않기로 한 "단순함"의 기준선이 그림으로 없다는 뜻이며, 07-b를 막지는 않는다.

## [전체 규격]

출처: `03-analysis.md` §5 UX 톤 가설, `06-service-design.md`.

- 기준 화면은 논리 픽셀 390×844(iPhone 15). 360×640(보급형 Android)에서도 깨지지 않는다.
- iOS 글씨 크기 확대(접근성 최대 단계)에서 글자가 잘리거나 버튼이 겹치지 않는다. Persona B를 위해 V1에서 챕기는 유일한 항목이다.
- 세이프 에어리어(노치와 홈 인디케이터 영역)를 비운다. 세로 모드만.
- 다크 모드가 기본이고 라이트 모드도 지원한다.
- 터치 대상 최소 44pt. 홈의 시작 버튼과 기도 화면의 두 버튼은 **80pt 이상** (졸린 상태에서 누른다 — `03-analysis.md` §5의 Headspace 참조).
- 화면 전환 400~600ms. **느린 것이 이 제품에서는 정서다.**
- 기도문은 한국 천주교 공식 기도문(주교회의 판본)을 글자 그대로 쓴다. 임의로 다듬거나 줄이지 않는다.

## [문구 규칙]

출처: `06-service-design.md` §4. 원본이 이미 표로 정해 두었다.

| 쓰지 않는다 | 대신 쓴다 |
|---|---|
| 연속 기록이 끊겼습니다 · 실패 · 놓쳤습니다 | 이어서 바치기 · 그 자리부터 |
| 오늘도 안 하셨네요 같은 알림 | 알림을 보내지 않는다 (V1) |
| 순위 · 비교 · 달성률 | 바친 횟수만 조용히 센다 |

## [금지]

세 출처에서 왔다. 첫 두 줄은 03·04단계 결정이고, 나머지는 AI 기본값이다.

- **탭 바를 만들지 않는다.** 경쟁자의 1등 가치인 단순함을 우리 손으로 깨지 않는다 (`03-analysis.md` 반증 3번).
- **기도가 끊긴 것을 벌주는 표현을 쓰지 않는다.** 04단계 "빌리지 않을 것" 목록에서 왔다.
- 온보딩 튜토리얼, 계정 가입, 결제 화면, 서버 호출을 만들지 않는다. V1에는 서버가 없다 (`06-service-design.md` §3).
- 음성 인식을 쓰지 않는다 (DQ-02 판정).
- 이모지를 아이콘 대신 쓰지 않는다.
- 목록의 모든 항목을 같은 회색 테두리 카드로 감싸지 않는다.
- 보라색 그라데이션, 장식용 무한 반복 애니메이션을 쓰지 않는다.
- 07-a에서 정한 요소 위치를 07-b에서 바꾸지 않는다. 바꾸려면 07-a로 돌아간다.

## [판정 기준]

절차 문서 §4의 판정 증명을 이 제품에 맞춰 적은 것이다.

| 단계 | 통과 조건 | 어디서 · 무엇을 하면 · 무엇이 보이나 |
|---|---|---|
| **07-a** 와이어프레임 | 화면 목록이 닫혔다. US-01~US-04의 모든 Given·When·Then이 어느 화면에서 일어나는지 지목된다 | `07-prototype/a-wireframe.html`을 열어 · 유저 플로와 화면들을 위에서 아래로 읽으면 · 화면 셋과 시트 둘, 그 사이 이동이 한눈에 보인다 |
| **07-b** 목업 | 06 디자인 토큰을 참조했다. 공방장이 "이 화면을 쓰고 싶다"고 판정했다 | `07-prototype/b-mockup.html`을 열어 · 화면들을 나란히 보고 글씨를 최대로 키워 다시 보면 · 최종 제품 같은 화면이 보이고 글씨가 커져도 겹치지 않는다 |
| **07-c** 기능 프로토타입 | 받는 사이가 실제 호흡에 맞는지 사람이 겪어 판정했다 | 실제 기기에서 AirPods를 끼고 · 5단을 한 번 끝까지 바쳐 보면 · 사이가 짧거나 긴지에 대한 판단이 나온다 |
| **07-d** 통합 시제품 | 07-b의 화면 + 07-c의 동작. 08 검증에 넘길 수 있다 | 실제 사용 자리(잠자기 전 침대, 퇴근길)에서 · 화면을 끄고 5단을 바쳐 보면 · 완주율을 실제로 잴 수 있는 상태가 된다 |

## 지금 상태와 다음 한 걸음

| 단계 | 상태 |
|---|---|
| 07-a 와이어프레임 | **작업완료** (2026-09-05) — [`a-wireframe.html`](./a-wireframe.html). 유저 플로 하나와 화면 일곱 장, 유저 스토리 대조표(19 조건 중 18 지목), 그리고 **06이 정하지 않은 것 여섯 개**를 담았다 |
| 07-b 목업 | **한 칸 남았다** — `[레퍼런스]`는 2026-09-05에 채워졌고, `[디자인 토큰]`만 남았다. 그것을 막는 것은 밝은 바탕이냐 어두운 바탕이냐의 판정 하나다 |
| 07-c 기능 프로토타입 | `07-prototype/v1/index.html`이 이에 해당한다. 동작은 확인됐으나 **사람이 실제로 바쳐 본 판정이 없다** |
| 07-d 통합 시제품 | 07-b가 없으므로 불가 |

**07-a가 06으로 돌려보낸 질문 여섯.** 상세는 `a-wireframe.html` §4에 있다. 순서는 판돈이 큰 것부터다.

1. 침묵 모드에서 화면에 무엇을 보이나 (앱이 읽지 않으면 앞 절·뒷 절을 나눌 근거가 사라진다)
2. 이어가기 카드가 몇 개까지 보이나 (지향별 저장이면 자리가 여러 개일 수 있다)
3. 5단 앞뒤의 시작 기도·마침 기도가 있나 (06의 화면 목록에는 5단 진행만 있다)
4. "여기서 끝내기"에 확인을 두나 (자리를 지우는 되돌릴 수 없는 동작이다)
5. 첫 실행의 기본 지향 이름과 지향 추가 항목
6. 기도 도중에 지금 어느 지향인지 보이나

**다음 한 걸음은 둘이며 서로 독립이다.** 하나는 위 여섯 질문을 06에서 확정하는 것이다. 다른 하나는 바탕의 밝고 어두움을 판정해 06에 디자인 토큰 절을 채우는 것이며, 그것이 끝나면 07-b로 간다. 레퍼런스는 2026-09-05에 들어왔다.
