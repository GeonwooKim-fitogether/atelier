# Handoff: 묵주 (mobile-rosary) — 한지 낮 (Hanji Day)

## Overview
54일 묵주 기도 앱의 화면 열 벌. 이 앱이 파는 것은 "오늘 한 번 기도"가 아니라 **하나의 지향을 54일 동안 바쳐 완주하는 것**이다. 홈은 오늘의 신비가 아니라 **여정 목록**이고, 카드를 한 번 탭하면 곧바로 어제 멈춘 자리에서 기도가 이어진다.

근거 문서: `GeonwooKim-fitogether/atelier` 리포의 `idea/mobile-rosary/` — PRD 제3판(`06-prd.md`), 디자인 시스템(`06-design-system.md`), 화면 명세(`06-screen-spec.md`), 화면 지도(`06-screen-map.md`). 구현 전에 PRD를 함께 읽을 것.

## About the Design Files
이 폴더의 `screens-day.html`은 **HTML로 만든 디자인 참조물**이다. 의도한 모양과 배치를 보여주는 시안이며, 그대로 가져다 쓰는 프로덕션 코드가 아니다. 할 일은 **이 화면들을 대상 코드베이스의 기존 환경에서 다시 만드는 것**이다 — React Native / Flutter / SwiftUI / Kotlin 등 이미 쓰는 프레임워크와 그 관례·라이브러리를 따른다. 아직 환경이 없다면, 이 앱에 가장 맞는 프레임워크를 골라 거기서 구현한다.

브라우저로 `screens-day.html`을 열면 열 화면이 실제 크기(390 × 844)로 나란히 렌더된다. CSS 값은 개발자 도구로 직접 읽어도 되고, 아래 표에 정리된 값을 써도 된다.

## Fidelity
**High-fidelity.** 색·타이포·간격·터치 영역이 모두 확정값이다. 픽셀 단위로 다시 만들되, 스크롤·입력·키보드·햅틱 같은 플랫폼 동작은 그 플랫폼의 표준 컴포넌트를 쓴다.

두 가지 예외:
- **성화 이미지는 목업 자산**이다. PRD D-1(저작권)이 미확정이므로 최종 이미지로 교체될 수 있다. 이미지는 교체 가능한 슬롯으로 구현할 것.
- **묵주 그림**은 SVG로 그렸다. 현재 단의 알 열 개와 큰 알만 실제 크기로 그리고, 나머지 고리는 얇은 실선으로 암시한다(59알을 모두 8% 크기로 그릴 수 없기 때문). 이 타협은 제품팀 확인이 필요하다.

## 이 벌의 정체성
전례서·기도서의 조판을 그대로 가져온 벌. 한지빛 바탕(#EDE7D8)에 먹빛 잉크, 구역을 나누는 것은 카드가 아니라 **얇은 괘선**(1px rgba(31,37,48,.16))이다. 라벨은 10.5px에 letter-spacing .24em으로 넓게 벌린다. 성화는 배경으로 깔지 않고 **고정 높이 도판**(96×124 썸네일, 148–340px 띠)으로 얹는다 — 그래서 글자 길이가 달라져도 그림이 잘리지 않는다. 강조는 치자-d(#82600F) 하나. 라디우스 0, 그림자 0. 20–35 세대와 시니어를 함께 받는 쪽.

## Screens / Views
파일 안의 순서 = 실제 흐름 순서다.

### 1. L 로그인
- **목적**: 계정 만들기. PRD 제3판 정정 9에 따라 **계정은 필수**다(조 기도·기기 간 이어가기·서버 저장이 여기서 파생).
- **레이아웃**: 위 340px 성화 도판(01_Mary_Single, background-position 50% 16%) → 아래 텍스트+조작 영역(padding 34px 24px 0).
- **조작**: `Google로 계속하기`(주 단추, 80px) · `Apple로 계속하기`(외곽선, 80px) · `초대 코드로 들어가기`(텍스트, 탭 영역 80px). 하단에 약관 2줄 11px/1.7.
- **문안**: "묵주" / "하나의 지향을 쉰네 날 동안 바칩니다. / 오늘 어디까지 바쳤는지는 앱이 기억합니다." / "계속하면 이용약관과 개인정보 처리방침에 동의합니다. 바람 문구는 서버에서 암호화해 보관합니다."

### 2. 초대 코드 입력
- **목적**: 함께 바치기 여정에 합류. **PRD 제3판 §5-2 화면표에 행이 없는 누락분** — 흐름도(§5-1)에는 있으나 화면 정의가 없어 새로 세웠다. PRD 갱신이 필요하다.
- **레이아웃**: 머리(라벨 + 닫기) → 제목 2줄 → **여섯 칸 코드 입력**(각 칸 flex:1, 높이 76px, gap 8px, 28px 500 숫자/글자) → 안내 문구 → **들어갈 여정 미리보기 카드**(라벨 10.5px·.2em / 여정명 22–24px serif / 참여자 줄) → 하단 `들어가기`(80px).
- **상태**: 입력 중인 칸은 강조 테두리(1px #82600F + 배경 rgba(130,96,15,.06)), 채워진 칸은 기본 테두리, 빈 칸은 옅은 테두리. 대소문자 구분 없음, 붙여넣기 허용.
- **문안**: "받은 여섯 자리를 / 넣어 주세요" / "대소문자를 가리지 않습니다. 붙여넣어도 됩니다." / "6일째 · 다섯 사람 중 넷이 들어와 있습니다"

### 3. A 홈 — 여정 목록
- **목적**: 진행 중인 여정을 보고, 한 번 탭으로 오늘 기도를 이어간다(FR-42).
- **레이아웃**: 머리 괘선 → 여정 블록 둘(각 padding 26px 0 24px, 아래 괘선). 블록 = 96×124 성화 썸네일 + 오른쪽 텍스트(gap 16px), 그 아래 54칸 리본(높이 22px, gap 2px)과 시작·마침 날짜. 하단 \`새 기도\`(먹빛 채움) + \`초대 코드로 들어가기\`.
- **여정 하나에 들어가는 것**: 지향 한 줄(serif 22–44px) · `N일째` + 성격(청원/감사) + 함께 바치기 여부 · **54칸 리본** · 이어갈 자리("고통의 신비 · 제3단 4번째 알부터 이어서") · 마지막 기도 시각.
- **조작**: 여정 줄 전체가 탭 대상(최소 80px) · `새 기도`(80px) · `초대 코드로 들어가기`(80px).
- **금지**: 홈에 탭 바 없음. 오늘 바친 여정은 "오늘 바쳤습니다"로 표시하되 축하 어휘·붉은 경고색을 쓰지 않는다.

### 4. N 새 기도
- **목적**: 여정 하나를 세운다. 여기서 정한 것은 54일간 바뀌지 않는다.
- **구역 넷**: ① **무엇을 위하여** — 지향 한 줄 입력(serif 23–25px, 밑줄/괘선). 보조문 "쉰네 날 동안 · 나만 봅니다". ② **청원인가 감사인가** — 두 칸(각 80px). 청원 = "27일 + 27일", 감사 = "쉰네 날 내내". ③ **낭송** — 세 줄(각 80px): `교대로`(기본, "앞은 소리가 읽고 뒤는 내가 받습니다") / `전부 소리로` / `소리 없이`. **차별점 #2이므로 인라인으로 노출한다 — 시트로 숨기지 말 것.** ④ **묵주** — 현재 선택만 한 줄로 보이고(`묵주 · 나무 · 고르기 →`, 80px) 실제 고르기는 별도 시트.
- **스크롤**: 구역 목록은 `overflow-y:auto`, 하단 `시작하기`(80px)는 고정.

### 5. 함께 바치기 배정
- **목적**: 오늘 내가 바칠 단을 알려준다. 다섯 사람이 한 단씩 나누어 하루를 채운다.
- **레이아웃**: 제목 "오늘 내 몫은 / 제2단입니다"(serif 26–34px) → 맥락 2줄 → **다섯 줄 표**(단 번호 26px 고정폭 / 이름 / 상태). 내 줄은 옅은 강조 배경 + 강조색 단 번호. → 합계 줄 → `내 몫 바치기`(80px) → `다섯 단 모두 혼자 바치기`(80px).
- **미정 규칙 ⚠**: 다섯 중 셋만 바친 날을 조 여정의 며칠째로 셀지 PRD에 규칙이 없다(FR-19·35·40 사이 공백). 시안에는 임시 문안 "남은 두 단은 자정까지 기다립니다"를 넣었다. **구현 전 규칙 확정 필요.**

### 6. B 기도 — 실제 기도 화면
가장 중요한 화면. 지켜야 할 규칙:
- **화면 안에 고르는 조작이 0개**(§9-23). 신비·단·판본을 여기서 바꿀 수 없다.
- **진행은 자동**. 받는 사이가 끝나면 다음 단계로 넘어간다(FR-12) — **넘기는 단추가 없다**. 하루는 77단계(시작 7 + 각 단 14 × 5).
- **나가는 길은 둘뿐**: `잠시 멈춤`("자리가 남습니다") · `여기서 끝내기`("오늘 처음부터"). 각 80px 이상.
- **현재 알은 화면 너비의 8% 이상**(FR-26) = 390 기준 최소 31.2px. 현재 구현 32–34px.
- **알 배치는 등호장(equal arc length)** 이어야 한다. 등각으로 배치하면 납작한 타원에서 끝쪽 알이 뭉친다 — 실제로 한 번 그렇게 만들었다가 고쳤다. 중심 간 거리 ≥ 알 지름.
- **알의 숨 애니메이션**: `@keyframes bre` — 0%/100% opacity .42 scale(1), 50% opacity 1 scale(1.12), 4초 ease-in-out 무한. SVG `<circle>`에 걸 때는 `transform-box:fill-box; transform-origin:center`가 **반드시** 필요하다(없으면 뷰박스 원점 기준으로 커져서 알이 실 밖으로 튄다).
- **기도문 영역은 고정 높이 + `overflow:hidden`**(FR-24: 글자 길이가 달라져도 그림이 잘리거나 배치가 흔들리지 않는다). 이 벌에서는 210px.
- **레이아웃**: 머리 괘선(지향·일째 / 제3단·성모송) → **148px 성화 도판**(06_Side_Jesus, flex:none) → 묵주 SVG 영역(flex:1, min-height:0, overflow:hidden, 340×245) → 기도문 210px(위 괘선) → 나가는 길 둘(각 flex:1, 82px, 외곽선).
- **문안 예**: 앞 구절 "은총이 가득하신 마리아님, 기뻐하소서. 주님께서 함께 계시니" / 받는 구절 "여인 중에 복되시며 태중의 아들 예수님 또한 복되십니다."(강조색)

### 7. C 하루 완주
- **목적**: 오늘 몫이 끝났음을 알리고 여정 안의 위치를 보여준다.
- **레이아웃**: 머리 라벨("9월 5일 · 스물세 번째 날") → "다 바쳤습니다" 34px serif → 맥락 2줄 → 54칸 리본(높이 26px) + "54일 중 21일 바쳤습니다 · 남은 31일" → 통계 세 줄(괘선) → \`돌아가기\`(먹빛 채움).
- **문안**: "다 바쳤습니다" — **축하 어휘를 쓰지 않는다.** 통계 세 줄: 성모송 50번 / 걸린 시간 18분 / 이어서 2번.
- **조작**: `돌아가기`(80px).

### 8. D 여정 상세
- **목적**: 54일 전체를 한눈에.
- **레이아웃**: 머리 → 지향 제목 → `23일째 · 청원 · 7월 14일 시작` → **54칸 9열 격자**(`grid-template-columns:repeat(9,1fr); gap:5px; aspect-ratio:1`) → 격자 설명 2줄 → 통계 다섯 줄(바친 날 21일 / 못 바친 날 2일 / 성모송 1,050번 / 청원에서 감사로 27일째부터 / 마치는 날 11월 6일) → `오늘 이어서 바치기`(80px) → `이 여정 그만두기`(80px).
- **주의**: 하단 두 조작에 `flex:none; min-height:80px`를 반드시 걸 것. 안 걸면 내용이 많은 날 flex 축소로 버튼 높이가 먹힌다(실제로 겪은 버그).

### 9. C′ 여정 완주 (54일째)
- **레이아웃**: 위 248px 성화 도판(02_Mary_and_Child) → 라벨 → "쉰네 날을 / 다 바쳤습니다" 36px serif → 맥락 2줄 → 54칸 리본(높이 30px) → 통계 → 조작 둘.
- **문안**: "쉰네 날을 / 다 바쳤습니다" / "청원 스물일곱 날, 감사 스물일곱 날 · 성모송 2,700번" / "못 바친 날은 뒤로 밀려 채워졌습니다".
- **리본**: 54칸 전부 채운 색. 조작: `이 지향으로 다시 시작하기`(80px) · `여정 목록으로`(80px).

### 10. E 설정
- **세 묶음**: **기도** (기도문 판본 → 가톨릭 기도서 / 낭송 기본값 → 교대로 / 소리 속도 → 보통) · **보기** (낮과 밤 → 기기 설정 따름 / 글자 크기 → 보통 / 기도 시각 알림 → 밤 9시) · **계정** (로그아웃).
- 줄 높이 전부 80px, 묶음 라벨은 10–10.5px에 letter-spacing .2em.
- **로그아웃 ⚠**: FR-45는 로그아웃 시 기기 사본을 지우는데, 오프라인에서 아직 올리지 못한 오늘 자리가 있으면 사라진다. 시안은 "올리지 못한 기록은 먼저 보냅니다"로 대응 문구를 달았다. **PRD 예외 표에 처리가 없다 — 확정 필요.**

## Interactions & Behavior
- **홈 → 기도**: 여정 카드 탭 한 번(중간 화면 없음). 어제 멈춘 단계에서 재개.
- **기도 진행**: 자동. 낭송 방식이 `교대로`면 앞 구절은 음성이 읽고, 받는 구절 구간이 끝나면 다음 단계로 자동 전환. `소리 없이`면 탭으로 진행.
- **알 전환**: 현재 알 4초 호흡(위 `bre`). 단이 바뀔 때는 조용한 크로스페이드만 — 튀거나 미끄러지는 모션 없음.
- **잠시 멈춤**: 자리를 남기고 나간다. **여기서 끝내기**: 오늘 몫을 버리고 처음부터.
- **오프라인**: 기도는 오프라인에서 되어야 한다. 조 배정을 못 받으면 어제와 같은 단(§8). ⚠ **조 여정 첫날에 연결이 없으면 어제가 없다 — PRD 공백.**
- **금지**: 붉은 경고색, 축하 애니메이션·문구, 배지, 연속 기록 압박("streak를 잃었습니다" 류).

## State Management
- `journeys[]`: {id, intention(암호화), kind: 청원|감사, startDate, dayIndex, days[54]: 바침|빈날|미래, recitation: 교대로|전부|없음, rosary, isGroup, groupMembers[], myDecade}
- `session`: {journeyId, step: 0..76, mysterySet, decade, beadIndex, startedAt, resumePoint}
- `settings`: {version, recitationDefault, speechRate, theme: 기기|낮|밤, textSize, reminderAt}
- 서버 필요(계정 필수). 지향·바람 문구는 저장 전 암호화. 오늘 자리는 로컬 우선 기록 후 동기화.

## Design Tokens
### 색 — 한지 낮 (Hanji Day)
| 토큰 | 값 | 쓰임 |
|---|---|---|
| 한지 바탕 | `#EDE7D8` | 화면 바탕 |
| 밝은 한지 | `#F5F1E6` | 어두운 면 위 글자 |
| 먹빛 잉크 | `#1F2530` | 제목·본문·주 단추 채움 |
| 부제 회색 | `#5C6272` | 라벨·부제·비활성 (4.9:1) |
| 치자-d 강조 | `#82600F` | 일째·현재 알·선택 테두리 (4.7:1) |
| 괘선 | `rgba(31,37,48,.16)` | 구역 구분선 |
| 옅은 칸 | `rgba(31,37,48,.10)` | 리본 빈 날 |
| 전례색 자색 | `#63507F` | 시기 라벨 (5.6:1) |
| 어두운 면 글자 | `#C9C2B0` | 먹빛 단추 안 보조문 |

두 벌 공통 규칙: **강조색은 한 벌에 하나**(치자 계열). 전례색 자색은 시기 표시에만. **리본 채움·알 색은 그래픽**이므로 원 토큰을 쓰되, **본문 크기 글자에는 대비 4.5:1을 통과하는 값만** 쓴다(치자-d를 본문에 그대로 쓰면 3.9:1로 떨어진다 — 실제로 겪은 문제).

### 타이포
- 제목·기도문·지향: **Noto Serif KR 400** — 17 / 21 / 22 / 24 / 26 / 27 / 28 / 30 / 34 / 38 / 42 / 44 / 46px
- 본문·라벨·조작: **Noto Sans KR 400 / 500 / 700** — 10 / 10.5 / 11 / 11.5 / 12 / 12.5 / 13 / 13.5 / 14 / 15px
- 기도문 행간 1.65–1.7, 본문 1.6–1.8, 라벨 1.0
- 구역 라벨: 10–10.5px, letter-spacing .2–.24em
- 숫자 강조(일째): Noto Sans KR 700, 30–40px

### 간격 · 형태
- 화면 여백: 좌우 22–24px, 위 56px(상태 바 아래), 아래 26px
- 리듬: 4 / 5 / 8 / 10 / 12 / 14 / 16 / 18 / 22 / 26 / 30 / 34px
- 라디우스: **0** (기기 프레임의 42px는 시안용 목업이며 앱 UI가 아니다)
- 그림자: 없음 (시안의 프레임 그림자는 목업용)
- 리본 칸: 높이 16 / 22 / 26 / 30 / 34 / 36px, gap 1–2px, flex:1 · 격자 칸: aspect-ratio 1, gap 5px

### 접근성
- **모든 탭 대상 최소 80px** (실측 기준). PRD §6은 80을 적었지만 iOS 44pt ≈ 88px이므로 **88px로 올리는 쪽이 안전**하다.
- 본문 글자 대비 4.5:1 이상. 큰 제목은 3:1 허용.
- 성화가 이목구비를 가리지 않도록 그라디언트 정지점을 지킬 것(FR-23).

## Assets
`assets/` — 성화 넷장(JPG). 사용자가 `01_Approved_Sacred_Images`로 제공한 이미지. **PRD D-1(저작권) 미확정이라 목업 자산이다.** 최종본으로 교체 가능한 슬롯으로 구현할 것.

- `01_Mary_Single.jpg` — L 로그인 상단 도판
- `02_Mary_and_Child.jpg` — A 홈 첫 여정 썸네일, C′ 완주 상단 도판
- `06_Side_Jesus.jpg` — B 기도 도판
- `07_Relief_Holy_Family.jpg` — A 홈 둘째 여정 썸네일

폰트는 Google Fonts CDN에서 Noto Serif KR · Noto Sans KR을 불러온다. 앱에서는 번들 폰트로 바꿀 것.

## Files
- `screens-day.html` — 화면 열 벌, 흐름 순서대로. 브라우저에서 바로 열린다(런타임 없음, 리본 54칸은 리터럴로 펼쳐 둠).
- `assets/` — 성화 이미지.
- 원본 시안(세 방향 전체 · 21개 프레임): 이 프로젝트의 `Rosary App.dc.html`.

## PRD 제3판에서 확정이 필요한 것 (구현 전)
1. **화면 수 불일치** — §5-1은 "시트 여섯", §10은 "시트 일곱"(S7 포함 여부). 시트 번호가 S2부터 시작한다.
2. **초대 코드 입력 화면이 무소속** — 흐름도에는 있고 화면표에는 행이 없다. §5-3의 "어느 화면에도 없는 FR이 0개" 게이트와 충돌.
3. **조 여정의 진척 정의** — 다섯 중 셋만 바친 날을 며칠째로 셀지 규칙 없음.
4. **조 오프라인 첫날** — "배정을 못 받으면 어제와 같은 단"인데 첫날에는 어제가 없다.
5. **FR-45 로그아웃 데이터 손실** — 오프라인에서 못 올린 오늘 자리의 처리가 예외 표에 없다.
6. 작은 것 둘 — FR-38 음량 버튼은 §11-3에서 iOS 거절 가능성이 높다고 적혀 있어 FR 본문과 상충(Android V1.5로 미루는 편이 명확). §6 접근성 "터치 영역 최소 80픽셀"은 iOS 44pt ≈ 88px과 어긋난다.
