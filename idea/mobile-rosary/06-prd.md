# 제품 요구사항 (PRD) — mobile-rosary V1 · 제3판

> 한 줄 요지: **이 앱이 파는 것은 "오늘 한 번"이 아니라 "내가 세운 바람을 위해 54일을 끝까지 바쳤다"는 한 여정의 완주다.** 그 완주를 이루는 데 필요한 것을 이 문서가 전부 적는다 — 제품의 정의, 경쟁자와의 실화면 대조, 우리 것으로 남는 차별점, 기능 요구사항, 그 요구사항이 놓이는 화면, 그리고 만들지 않기로 한 것.

이 문서는 처음 읽는 사람을 위해 자족적으로 쓴다. 묵주기도가 무엇인지 모르는 사람도 §1을 읽으면 이 제품을 이해할 수 있게 했다.

## 이 문서의 자리 — 06단계 다섯 문서 중 첫째

06단계(설계)는 다섯 문서로 이뤄지고, 다섯은 서로 다른 질문에 답한다. 같은 내용을 두 곳에 적지 않는 것이 규칙이므로, **이 문서는 자매 문서의 영역을 가리키기만 하고 옮겨 적지 않는다.**

| | 문서 | 답하는 질문 |
|---|---|---|
| **06-a** | **이 문서 (PRD)** | 무엇을 만들고 왜 이기는가 — 제품 정의 · 경쟁 대조 · 차별점 판정 · 기능 요구사항 · 범위 밖 |
| 06-b | [`06-design-system.md`](./06-design-system.md) | 어떻게 보이는가 — 색 · 서체 · 컴포넌트 상태 · 모션 · 접근성 · 톤 |
| 06-c | [`06-service-design.md`](./06-service-design.md) | 어떻게 흐르는가 — 유저 스토리 · 정보 구조 · 백 오피스 판단 |
| 06-d | [`06-screen-spec.md`](./06-screen-spec.md) | 화면마다 무엇이 놓이는가 — 요소 · 문구 · 빈 상태 · 소리와 진동의 상수 |
| 06-e | [`06-screen-map.md`](./06-screen-map.md) | 화면이 왜 이만큼이고, 사용자의 길을 걸어 보면 어디서 막히는가 |

그래서 이 문서에는 색값도, 확정 문구도, 진동 밀리초도 없다. 그것들이 필요하면 위 표의 해당 문서를 연다.

## 이 판을 다시 쓴 이유 (2026-09-05) — 공방장 검토가 제품 정의 아홉 곳을 뒤집었다

제2판(2026-08-30)은 실화면 조사를 근거로 다시 세운 판이었다. 그 판을 공방장이 검토하고 **아홉 항목을 정정**했다. 정정은 표현의 손질이 아니라 **제품의 골격을 바꾸는 것**이었다 — 앱을 열면 도착하는 곳, 누가 완주하는가, 계정이 있는가, 무엇이 차별점인가가 전부 달라진다. 그래서 문장을 고치는 대신 문서를 다시 세웠다.

| # | 공방장의 정정 | 제2판이 어떻게 적었었나 | 이 판에서 무엇이 되나 |
|---|---|---|---|
| 1 | 둥근 얼굴의 **한복 성모는 삭제** | 차별점 2순위 "한국 성화의 성모님" | 배경 성화는 두되 도상의 출처를 정하지 않는다. 차별점에서 뺀다 (§1-5·§3) |
| 2 | 조 기도는 **기능으로 추가**하라 — 기획해서 넣어라 | 서버가 없어 포기했다 (§1-6) | **함께 바치기**를 V1 기능으로 기획했다 (§1-6·FR-40) |
| 3 | 앱을 열면 **기도 목록**이어야 한다. 기도가 여럿일 수 있다. 처음 시작이면 바람 · 시작일을 받아 **완료일을 자동으로** 정하고, **기도문도 고를 수** 있게. 다음부터는 첫 화면이 지난 기록 · 진척률 · 처음의 바람을 다시 보여 주는 목록이다 | "앱을 열면 곧바로 기도 화면"을 기본 요건으로 세웠다 | 홈 = 기도 목록. 새 기도 시작 마법사. 기도문 판본 선택 (§1-4·§5·FR-33~37) |
| 4 | 오늘 무엇을 바칠지는 **우리가 정해 둔 스케줄**로 | 요일 규칙만 있었다 | 요일 규칙에 **54일 기도의 순환 규칙**을 더한다. 사용자가 고르지 않는다 (FR-01·FR-43) |
| 5 | 누가 읽나는 **셋 중 사용자가 고른다** — 전부 읽기 · 읽지 않음 · 앞 절만 읽고 뒷 절 받기 | 교대 낭송 하나였다 | 낭송 방식 셋. 교대는 그중 하나이자 기본값 (FR-06·FR-11) |
| 6 | 배경은 성화 한 장을 쓰되 **한국 성화 조건은 삭제** | "한국 성화를 쓴다" | 성화 한 장, 신비마다 바뀐다. 출처는 미정 (FR-22·D-1) |
| 7 | 손을 쓰지 않는 것은 **선택 항목** — 폰을 흔들거나 음량 버튼으로 앞뒤 | 자동 진행만 있었다 | 손 없이 조작 켬/끔. 흔들기 · 음량 버튼 · 이어폰 버튼 (FR-38, 플랫폼 제약은 §11-3) |
| 8 | 완주하는 주체 — **기능을 추가**하면 된다 | 개인만 | 2번과 같다. 개인이 기본, 조는 선택 (§1-6) |
| 9 | **개인 계정은 반드시** — 구글 같은 쉬운 로그인 | 계정도 서버도 없다 | 계정 필수. 구글 · 애플 로그인. 서버가 생긴다 (FR-32·FR-41·§6·§7) |

함께 못 박은 것 둘 — **묵주를 고르는 기능**(FR-39)과 **54일 중 진척률**(FR-35)은 있으면 좋은 것이 아니라 **반드시 있어야 하는 기능**이다.

정정 9번이 가장 멀리 미친다. 제2판의 여러 결정 — 조 기도 포기, 서버 없음, 비용 0, 오프라인만 — 은 전부 "계정을 두지 않는다"라는 한 전제에서 나왔다. 그 전제가 바뀌었으므로 그 결정들도 함께 다시 썼다. 무엇을 살리고 무엇을 버렸는지는 문서 끝 §11-4에 적었다.

---

## 1. 이 제품은 무엇인가

### 1-1. 한 문장으로

> **바람 하나를 정하고 54일(또는 정한 기간) 동안 날마다 묵주기도를 바치되, 묵주와 기도문 책 없이, 화면을 보지 않고도, 끊겨도 이어서, 혼자서든 다섯이 나눠서든 끝까지 갈 수 있게 하는 한국어 묵주기도 앱.**

파는 것은 기능 목록이 아니라 **한 여정의 완주**다. 묵주기도는 하루 한 번의 기도이기도 하지만, 한국 신자들이 실제로 하는 것은 **바람을 세우고 54일을 채우는 일**이다(01단계 — "27일 청원 뒤 27일 감사, 빼먹으면 처음부터라고 들어서 너무 힘들다"). 그래서 이 제품의 성공은 "오늘 다섯 단을 마쳤다"가 아니라 **"내가 세운 바람을 위한 여정을 끝까지 갔다"** 로 잰다. 하루의 완주는 그 여정의 한 칸이다.

### 1-2. 누가 쓰나 — 첫 판이 만족시켜야 하는 사람은 둘이다

03단계가 다섯 퍼소나(사용자 유형)를 세웠고, 그중 둘이 V1의 대상이다. 나머지 셋은 V1.5 이후로 미뤘다.

| 퍼소나 | 어떤 사람인가 | 이 사람에게 무엇이 걸려 있나 |
|---|---|---|
| **A 핵심 신자** (최우선) | 30~50대, 매일 묵주기도를 다섯 단 통째로 바친다. 잠자기 전 누워서, 또는 퇴근길 대중교통에서. 이어폰을 자주 쓴다. **54일 기도를 여러 번 시도했고, 두세 가지 바람을 동시에 바친다** | 도구(묵주·기도문 책)가 없고, 졸음과 잡생각으로 집중이 깨지고, 사람 많은 곳에서 화면이 남에게 보이는 것을 의식한다. **며칠째인지 세다가 놓치면 그만둔다** |
| **B 시니어** | 60대 이상, 신앙 생활이 하루의 중심이다. 디지털 기기에 익숙하지 않다 | 작은 글씨를 읽기 어렵고, 조작이 많은 앱은 진입 자체가 벽이다 |

퍼소나 A는 이 제품을 **매일** 쓰는 사람이므로 기능의 기준이고, 퍼소나 B는 **읽기와 조작의 기준**이다. 접근성 요구(§6)가 협상 대상이 아닌 이유가 여기 있다. 퍼소나 B가 있기 때문에, 화면이 늘어난 이 판에서도 **한 화면에서 할 일은 하나**라는 원칙을 지킨다(§5).

### 1-3. 핵심 루프 — 한 번의 묵주기도는 정해진 순서의 반복이다

묵주기도를 모르는 사람을 위해 먼저 적는다. **묵주기도는 구슬(알)을 하나씩 넘기며 정해진 기도를 정해진 횟수만큼 반복하는 가톨릭 기도다.** 한 번(다섯 단)은 아래 순서로 이뤄진다.

**시작 기도 — 일곱 단계**

1. 성호경
2. 사도신경
3. 주님의 기도
4. 성모송 (세 번 — 각각이 한 단계다)
5. 영광송

**제1단부터 제5단까지 — 각 단이 열네 단계, 다섯 단이니 일흔 단계**

1. 그 단의 신비를 선포한다 (무엇을 묵상할지 알리는 한 줄)
2. 주님의 기도
3. 성모송 (열 번 — 각각이 한 단계다)
4. 영광송
5. 구원을 비는 기도

그래서 **한 번의 묵주기도는 일흔일곱 단계**다(시작 기도 7 + 각 단 14 × 5단 = 77). 이 일흔일곱 단계를 순서대로 넘기는 것이 이 앱의 **핵심 루프**이며, 기도 화면의 소리와 진동과 그림이 전부 이 루프를 위해 존재한다.

**마침 기도는 V1에 넣지 않는다**(D-3, 기본값 확정). 완주 화면이 마침을 대신한다.

### 1-4. 여정 — 하루의 기도가 놓이는 더 큰 단위

제2판에는 없던 개념이고, 이 판의 중심이다. **한 번의 묵주기도(77단계)는 "여정" 안의 하루다.** 여정은 아래 다섯 가지로 이뤄진다.

| 여정을 이루는 것 | 뜻 | 누가 정하나 |
|---|---|---|
| **바람** (지향) | 이 기도를 누구를 위해, 무엇을 위해 바치는가. "어머니 병환 회복"처럼 한 줄 | 사용자가 시작할 때 적는다 |
| **형식** | 54일 기도(27일 청원 + 27일 감사) · 9일 기도 · 기간 없이 날마다 | 사용자가 시작할 때 고른다. 기본값은 54일 |
| **시작일** | 첫날 | 사용자가 정한다. 기본값은 오늘 |
| **완료 예정일** | 형식과 시작일에서 **앱이 계산한다.** 54일 기도면 시작일 + 53일 | 앱 |
| **기도문 판본** | 성모송 · 주님의 기도 등의 문구를 어느 판본으로 쓰는가 | 사용자가 시작할 때 고른다. 기본값은 설정의 판본 |

여정마다 **진척**이 붙는다. 54일 기도면 "오늘이 며칠째인가"(N/54)와 "청원 중인가 감사 중인가", 그리고 오늘 안에서 "몇 단 몇 번째 알까지 바쳤는가"다. **홈은 이 여정들의 목록**이고, 여정 하나를 열면 그날의 77단계로 들어간다.

이 문서에서 **"지향"이라는 말은 여정 하나를 가리킨다.** 제2판까지 지향은 이름 한 줄이었고 진행 자리 하나가 붙어 있었다. 이 판에서 지향은 위 다섯 가지와 진척을 가진 단위로 넓어졌다. 요구사항 번호(FR-04·05)는 그대로 두고 뜻을 넓혔다.

### 1-5. 제품의 뼈대 — 없으면 묵주기도 앱이 아닌 것

아래는 "있으면 좋은 것"이 아니라 **없으면 이 앱이 묵주기도 앱이 아니게 되는 것**이다. 경쟁자도 대부분 갖고 있으므로 차별점이 아니지만, 이것이 제품의 본체다.

| 뼈대 | 왜 제품 그 자체인가 |
|---|---|
| **오늘의 신비** | 묵주기도는 날마다 묵상하는 대상(신비)이 다르다. 이것이 정해지지 않으면 무엇을 바칠지 정할 수 없다. **앱이 정해 알려 준다** — 사용자가 고르지 않는다 |
| **정해진 순서의 진행** | 위 77단계가 기도의 형식 자체다. 순서를 바꾸면 다른 기도가 된다 |
| **지금 어디인가** | 성모송을 쉰 번 바치는 기도이므로, 위치를 잃으면 기도가 끊긴다 |
| **마침** | 시작이 있으면 끝이 있어야 한다. 하루의 끝(완주 화면)과 여정의 끝(54일째)이 둘 다 있어야 한다 |
| **바람** (지향) | 묵주기도는 누군가를 위해 바치는 청원 기도다. 대상 없는 묵주기도는 형식만 남는다 |
| **여정의 진척** | 54일 기도는 "며칠째인가"를 잃으면 그만두게 되는 기도다(01단계 마찰 5번). 진척이 보이지 않는 54일 기도 앱은 반쪽이다 |

### 1-6. 차별점은 뼈대의 어느 자리에 얹히나

| 뼈대의 자리 | 여기 얹히는 우리 차별점 |
|---|---|
| 단계를 넘기는 방식 | **낭송 방식을 사용자가 고른다** — 전부 읽어 주기 · 읽지 않기 · **교대 낭송**(앱이 앞 절, 내가 뒷 절). 교대가 기본값이다 |
| 손을 쓰지 않는 방식 | **손 없이 조작**을 켤 수 있다 — 폰을 흔들면 다음 알, 음량 버튼과 이어폰 버튼으로 앞뒤 |
| 지금 어디인지 보이는 방식 | **실제 묵주의 형태** 위에서 지금 알 하나가 숨 쉬듯 밝아졌다 어두워진다. **그 묵주를 사용자가 고른다** |
| 여정의 진척 | **54일 중 며칠째, 청원인가 감사인가**가 홈 카드에 늘 보인다. 처음 세운 바람을 함께 다시 보여 준다 |
| 완주하는 주체 | 혼자가 기본이되, **다섯이 한 조로 나눠 바칠 수 있다** |
| 화면의 색 | **전례 시기를 따라 바뀐다** — 대림·사순은 자색, 연중은 녹색처럼 |
| 끊겼을 때 | **자동으로 저장되고 그 자리부터 이어진다.** 기기를 바꿔도 계정으로 이어진다 |

**이 표가 이 문서 전체의 설계 원리다.** 아래 모든 기능 요구사항은 §1-5의 뼈대이거나 이 표의 차별점이며, 둘 다 아닌 것은 V1에 넣지 않는다.

제2판의 차별점 표에 있던 **"한국 성화의 성모님"은 이 표에서 빠졌다**(정정 1·6). 배경 성화는 남는다 — Marcello가 이미 하고 있는 구조이므로 기본 요건이다. 다만 어떤 성화인가는 정하지 않았고, 저작권과 함께 D-1에서 정한다.

### 1-7. 갈림길을 다시 지난다 — 완주하는 주체는 개인이 기본이고 조는 선택이다

01단계가 잡은 통증은 **"혼자서 다섯 단을 끝까지 못 간다"** 였다. 여기서 답이 두 갈래로 갈린다.

| | 혼자 완주 | 다섯이 나눠 완주 (Rosario의 답) |
|---|---|---|
| 해법 | 끊겨도 그날 안에 이어 붙여 혼자 마친다 | 다섯 사람이 한 단씩 맡아 조 전체가 하루에 한 바퀴를 채운다 |
| 하루 분량 | 다섯 단, 약 20분 | 한 단, 약 5분 |
| 필요한 것 | 기기 하나 | 계정 · 서버 · 조 편성 · 진행 동기화 |

제2판은 **왼쪽만** 택했다. 이유는 취향이 아니라 "계정을 두지 않는다"는 전제였다 — 오른쪽 길은 서버 없이 성립하지 않았다. 공방장이 그 전제를 뒤집었다(정정 9). 계정이 생기면 오른쪽 길의 비용이 사라지므로, **둘 다 한다.** 혼자 바치는 것이 기본이고, 여정을 만들 때 "함께 바치기"를 고르면 조가 된다(FR-40).

무엇을 대가로 지불하는지도 적어 둔다. **화면 수가 늘고, 서버와 계정이 생기고, 개인정보를 다루게 된다.** 이 셋은 §5·§6·§7이 받는다.

---

## 2. 경쟁자는 같은 일을 어떻게 하고 있나

### 2-1. 무엇을 근거로 대조했나

**이 절의 근거는 경쟁사 앱의 실제 화면이다.** 2026-08-30에 공방장이 실기기에서 촬영한 스크린샷 36장을 직접 분석했다 — Hallow 12장, Marcello 14장(묵주기도 진행 화면 전체와 메뉴 포함), Rosario 10장(온보딩 전체). 분석 원문은 [`02b-design-research.md`](./02b-design-research.md)에 있다.

**아직 보지 못한 것도 함께 적는다.** 한국 앱 두 종(굿뉴스 묵주기도, 성모님과 함께)은 화면을 확보하지 못했다. 아래 표에서 그 둘에 관한 칸은 **"화면 미확인"** 으로 표시하고, 리뷰에서 확인된 것만 근거로 쓴다. 추측을 사실처럼 적지 않는다.

### 2-2. 세 경쟁자의 성격을 한눈에

| | Hallow (미국) | Marcello (이탈리아) | Rosario (프랑스) |
|---|---|---|---|
| 무엇인가 | 가톨릭 기도 콘텐츠 구독 서비스. 시장 1위 | 묵주기도 전용 앱. **공방장이 지금 쓰는 앱이고 우리의 기준선** | "살아 있는 묵주" — 다섯이 한 조로 나눠 바치는 공동 기도 앱 |
| 값 | 월 9.99달러부터. 가족·학생·본당 요금까지 갖췄다 | 무료, 광고 없음 | 무료 |
| 첫인상 | 어두운 바탕에 밝고 채도 높은 콘텐츠 카드. 프리미엄하지만 무겁다 | 짙은 파랑 위에 성화. 전통적이지만 낡았다 | 거의 흰 바탕에 부드러운 색 번짐. **셋 중 가장 현대적으로 보인다** |
| 우리에게 무엇인가 | 이겨야 하는 상대가 아니라 **피해야 하는 방향**이다. 콘텐츠 매대 구조는 구독 모델이 강제한 것이라 우리가 따라갈 이유가 없다 | **지지 않아야 하는 기준선.** 기도 화면의 단순함은 그대로 지킨다 | 제품 정의를 건드린 상대. **조 기도의 구조를 여기서 배운다**(§1-7) |

### 2-3. 기능 축으로 갈라 본 대조

#### (가) 기도를 바치는 동안

| 기능 축 | Hallow | Marcello | Rosario | 우리 V1 |
|---|---|---|---|---|
| **앱을 열면 도착하는 곳** | 일곱 장짜리 프로모션 캐러셀. 묵주기도는 하단 탭에도 없고 그 안의 카드 하나다 | **곧바로 기도 화면.** 고르는 단계가 없다 | 온보딩 네 화면과 로그인, 이름 입력, 조 선택을 지나야 기도에 닿는다 | **내 기도(여정) 목록.** 카드마다 바람 · 며칠째 · 이어갈 자리가 있고, 카드를 누르면 바로 기도다. 목록에서 기도까지 조작 한 번 |
| **오늘 무엇을 바칠지 정하는 주체** | 사용자가 카탈로그에서 고른다 | **앱이 요일로 정해 알려 준다** | 조가 정한 단을 배정받는다 | **앱이 정해 알려 준다** — 자유 기도는 요일 규칙, 54일 기도는 환희·고통·영광 순환 규칙 |
| **누가 기도문을 읽나** | 앱이 전부 읽는다. 유명인 목소리가 강점 | 읽어 주지 않는다. 사용자가 전부 바친다 | 읽어 주지 않는다 | **셋 중 사용자가 고른다** — 전부 읽기 · 읽지 않기 · 교대(앞 절은 앱, 뒷 절은 나). 기본값은 교대 |
| **어느 기도문을 쓰나** | 영어 한 판본 | 이탈리아어 한 판본 | 프랑스어 한 판본 | **판본을 고른다.** 여정마다 다르게 둘 수 있다 |
| **지금 어디인지 알리는 방법** | 오디오 재생 막대 | **실제 묵주의 형태.** 지나간 알이 차오르고 주님의 기도 알은 크게 튀어나온다. 다만 알 지름이 화면 폭의 약 5%로 작다 | 신비 목록과 단 표시 | 실제 묵주의 형태. **알을 더 크게 그리고**(화면 폭의 8% 이상), **묵주의 모양과 재질을 사용자가 고른다** |
| **무엇을 묵상하는지 알리는 방법** | 거룩한 인물을 그리지 않는다. 손·구·별 같은 상징으로 우회한다 | **배경 전체가 성화 한 장.** 신비가 바뀌면 그림이 바뀐다. 다만 해상도가 낮아 뭉개진다 | 얼굴 없는 플랫 일러스트로 우회한다 | 배경 전체가 성화 한 장. 신비가 바뀌면 바뀐다. **원본 해상도를 확보한 그림만 쓴다.** 출처는 D-1 |
| **손을 쓰지 않고 진행하나** | 오디오 자동 재생이므로 된다. **다만 영어다** | 안 된다. 알을 손으로 눌러 넘긴다 | 안 된다 | **된다, 그리고 방법을 고른다.** 자동 진행(사이·진동)이 기본이고, 켜면 폰 흔들기 · 음량 버튼 · 이어폰 버튼으로 앞뒤. **한국어다** |

#### (나) 기도 밖에서

| 기능 축 | Hallow | Marcello | Rosario | 우리 V1 |
|---|---|---|---|---|
| **끊겼을 때** | 오디오 재생 위치는 복원되는 것이 관행이나, 묵주기도 문맥의 재개인지는 **미확인** | "Go to…" 메뉴로 특정 알로 건너뛸 수 있다. 자동 저장인지는 **미확인** | 하루 한 단 단위라 끊김의 개념이 다르다 | **알을 넘길 때마다 자동 저장**되고, 홈 카드에서 그 자리부터 이어진다. **기기를 바꿔도** 계정으로 이어진다 |
| **54일 기도** | 없다 | 없다 | 없다 (조 단위 연속 기도가 있다) | **여정의 기본 형식이다.** 며칠째 · 청원/감사 · 완료 예정일이 카드에 있다. 하루를 건너뛰어도 벌주지 않는다 |
| **여러 바람을 함께 바칠 때** | 지향을 동시에 관리하는 기능이 없다 | 없다 | 조의 지향이 공개 카탈로그로 있다 | **여정마다 카드 하나.** 진척과 자리가 따로 기억된다 |
| **완주하는 주체** | 개인 | 개인 | **다섯 명이 한 조** | **개인이 기본, 조는 선택.** 여정을 만들 때 "함께 바치기"를 고르면 다섯이 한 단씩 나눈다 |
| **값을 받는 방법** | 월·연·가족·학생·본당 구독. 모든 구간을 덮었다 | 무료 | 무료 | 창립 멤버 평생권 9~19달러 1회. 매년 걷지 않는다 |
| **계정** | 필수 | 없음 | 사실상 필수 (조 편성 때문) | **필수.** 구글 · 애플 로그인. 계정이 있어야 기기를 바꿔도 여정이 따라오고 조 기도가 된다 |

**한국 앱 두 종에 관해 지금 말할 수 있는 것.** 굿뉴스 묵주기도(서울대교구 공식)와 성모님과 함께는 화면을 보지 못했다. 리뷰에서 확인된 결함 하나만 근거로 쓸 수 있다 — **굿뉴스는 기도 글자 크기를 키우면 줄 간격이 따라 늘지 않아 글자가 붙어 읽기 힘들다.** 시니어를 위해 만든 기능이 시니어에게 못 쓰게 되는 자리이고, 우리 퍼소나 B의 요구와 정확히 겹친다. 이것이 FR-28의 근거다.

### 2-4. 각 축의 판정 — 다른 것이 전부 차별점은 아니다

**판정 규칙을 먼저 못 박는다.** 우리가 남과 다르다는 사실만으로는 차별점(USP, 사용자가 우리를 고르는 이유)이 되지 않는다. **사용자가 그 차이 때문에 우리를 고를 때만** 차별점이다.

| 기능 축 | 우리의 차이 | 판정 | 왜 그렇게 판정했나 |
|---|---|---|---|
| 54일 기도 | 여정의 기본 형식. 진척이 늘 보인다 | **차별점 1순위** | 세 앱 모두 없다. 01단계가 잡은 통증 중 가장 크고 오래된 것이고(마찰 5번), 한국 신자의 실제 관습이다. 완주율로 바로 드러난다 |
| 누가 기도문을 읽나 | 셋 중 고른다. 교대 낭송이 그중 하나 | **차별점 2순위** | 세 앱 모두 한 방향만 있다. 교대 낭송은 "내 차례"라는 상태를 만들어 잡생각을 막고, 전부 읽기는 시니어와 초심자를, 읽지 않기는 익숙한 사람을 받는다. 한 앱이 세 사용자를 다 받는다 |
| 완주하는 주체 | 개인 기본, 조는 선택 | **차별점 3순위** | Rosario만 조가 있고 그쪽은 조 전용이다. 우리는 혼자 바치던 사람이 같은 앱에서 조로 옮겨 갈 수 있다 |
| 손을 쓰지 않고 진행하나 | 한국어로 되고, 조작 방법을 고른다 | **차별점 4순위** | 한국어 시장이 비어 있다. 흔들기·버튼 조작은 눈을 감은 사용자에게 자동 진행보다 확실하다 |
| 어느 기도문을 쓰나 | 판본을 고른다 | **차별점 5순위** | 세 앱 모두 한 판본이다. 01단계에서 공방장이 "내가 보던 기도문하고 다르다"를 첫 불만으로 말했다. 다만 신자만 알아본다 |
| 지금 어디인지 | 묵주를 고른다 | **차별점 6순위 (정서적)** | 실제 묵주 형태는 Marcello가 이미 한다. 고르기는 우리만 하지만, 기능이 아니라 애착의 문제다 |
| 화면의 색 | 전례 시기를 따라 바뀐다 | **차별점 7순위** | 어느 앱도 하지 않는다. 신자만 알아본다 |
| 끊겼을 때 | 자동 저장, 그 자리부터, 기기 넘어서 | **차별점 8순위 (눈에 띄지 않는 종류)** | 써 본 사람만 안다. 재방문율로 드러난다 |
| 앱을 열면 도착하는 곳 | 여정 목록 | **차별점 아님 — 기본 요건** | 여정이 여럿이면 목록은 당연하다. 지킬 것은 "목록에서 기도까지 조작 한 번" |
| 무엇을 묵상하는지 | 배경 성화 한 장 | **차별점 아님 — 기본 요건** | Marcello가 이미 한다. 얼굴을 가리지 않고 해상도를 지키는 것이 우리가 더 잘하는 부분이다 |
| 값을 받는 방법 | 평생 1회 결제 | **차별점 아님 — 오히려 불리하다** | Marcello와 Rosario가 완전 무료다. 틀리면 죽는다. 그래서 05단계가 마켓 핏 지표를 따로 뒀다 |

---

## 3. 그래서 무엇이 우리 것인가

### 3-1. 기본 요건 — 지키면 본전이고 어기면 진다

아래는 차별점이 아니다. **경쟁자가 이미 하고 있어서 안 하면 지는 것**이다. 07-b 목업의 통과 조건이기도 하다.

| 기본 요건 | 어기면 무엇을 잃나 | 대응 FR |
|---|---|---|
| 목록에서 기도 시작까지 조작이 한 번이다. 카드를 누르면 기도다 | Marcello의 단순함을 잃는다. 홈이 목록이 된 대가로 이 한 줄을 반드시 지킨다 | FR-42 |
| 오늘의 신비를 앱이 정해 알려 준다 | Marcello가 이미 한다 | FR-01 · FR-43 |
| 알을 실제 묵주의 형태로 그린다 | Marcello가 우리보다 낫던 자리다 | FR-21 |
| 신비가 바뀌면 배경 성화가 바뀐다 | Marcello가 이미 한다 | FR-22 |
| 기도문은 세리프(명조 계열), 조작은 산세리프(고딕 계열) | Hallow가 이미 한다 | 06-b §3 |
| 광고·팝업·순위·업셀이 하나도 없다 | Marcello가 이미 무료·무광고다 | §9 |
| 기도 화면 안에서는 고를 것이 없다 | 기도는 탐색하는 것이 아니다. 고르는 일은 전부 기도 화면 **밖**(여정 만들기 · 설정)에서 끝낸다 | §5 |

한 문장으로 옮기면 이렇다. **기도 화면은 Marcello처럼 단순하게 지키고, 그 바깥에 여정과 계정과 조를 둔다.**

### 3-2. 차별점 — 순위와 방어 가능성

| 순위 | 차별점 | 누구를 이기나 | 얼마나 방어되나 | 사용자가 언제 겪나 |
|---|---|---|---|---|
| **1** | **54일 기도의 진척** — 며칠째 · 청원/감사 · 처음의 바람 | 셋 다 | 높다. 한국 신자의 관습을 알아야 나온다 | 둘째 날부터 매일. 홈을 열 때마다 |
| **2** | **낭송 방식 셋** — 전부 · 교대 · 없음 | 셋 다 | 높다. 교대 낭송은 전례의 응답 구조를 이해해야 나온다 | 첫 성모송에서 바로 |
| **3** | **함께 바치기** — 다섯이 한 단씩 | Hallow · Marcello | 중간. Rosario가 이미 하나 그쪽은 조 전용이다 | 조를 만든 순간부터 |
| **4** | **한국어로 손 없이** — 자동 진행 + 흔들기 · 버튼 | 셋 다 | 중간. 영어권 앱이 한국어를 붙이면 좁아진다 | 첫 사용 30초 안에 |
| **5** | **기도문 판본 선택** | 셋 다 | 중간. 판본 확보(D-4)에 달려 있다 | 여정을 만들 때 |
| **6** | **묵주 고르기** | 셋 다 | 낮다. 베끼기 쉽다 | 설정에서 한 번 |
| **7** | **전례 시기를 따라 바뀌는 색** | 셋 다 | 높다. 가톨릭 내부 지식이 필요하다 | 시기가 바뀔 때 |
| **8** | **끊겨도 이어짐, 기기를 넘어서** | Marcello의 수동 건너뛰기보다 자동 | 중간 | 두 번째 사용부터 |

### 3-3. 실화면 확인으로 무너진 판단 둘 (제2판) — 그대로 남긴다

| 처음의 판단 | 실화면이 밝힌 것 | 어떻게 처리했나 |
|---|---|---|
| "앱을 열면 바로 기도 안에 있다"를 우리 차별점으로 세웠다 | **Marcello가 이미 지키고 있었다** | 제2판에서 기본 요건으로 강등했고, 이 판에서는 홈이 목록이 되면서 **"목록에서 조작 한 번"** 으로 바뀌었다 |
| "성모님이 있는 화면"이 비어 있는 자리라고 판단했다 | **Marcello는 성화를 화면 전체에 쓴다** | 제2판은 "어떤 성모님인가"로 재정의해 한국 성화를 차별점으로 세웠다. 이 판에서 그 차별점은 **공방장 정정으로 빠졌다**(§3-4) |

### 3-4. 공방장 검토로 뒤집힌 판단 — 제2판의 결함이 무엇이었나

정정 아홉 개(문서 머리의 표)를 한 걸음 물러서서 보면 제2판의 결함은 둘이었다.

**첫째, 하루의 기도만 보고 여정을 보지 못했다.** 01단계가 54일 기도의 통증을 적어 두었고 05단계가 "54일 리본"을 V1.5로 미루면서 "만드는 값은 0인데 단순함 때문에 미룬다"고 적었다. 그 판단이 틀렸다. 54일 기도는 장식(리본)이 아니라 **이 기도의 단위**이고, 그것을 빼면 홈이 텅 비어 "곧바로 기도 화면"밖에 놓을 것이 없어진다. 홈이 목록이어야 한다는 정정 3번은 여정을 되찾으면 자연히 따라온다.

**둘째, "계정을 두지 않는다"를 요구사항이 아니라 전제로 삼았다.** 그 전제 하나가 조 기도 포기 · 서버 없음 · 기기 넘어 이어가기 없음 · 비용 0을 한 줄로 결정했는데, 어디에도 그 전제를 사용자에게 물은 기록이 없다. 정정 9번이 그 전제를 지웠다.

**셋째(작은 것), 조사가 찾은 것을 차별점으로 승격했다.** 한국 성화의 어휘는 02b 조사의 성과였고, 성과이기 때문에 차별점 표에 올렸다. 조사가 찾은 사실과 제품이 택할 방향은 다른 것인데 그 둘을 붙였다. 정정 1·6번이 그것을 떼어 냈다.

### 3-5. 이 판정이 화면 설계에 주는 지시

1. **차별점 1(진척)은 홈 카드에서, 차별점 2(낭송)는 첫 성모송에서 겪게 만든다.** 둘 다 첫날 안에 겪는다.
2. **기도 화면에는 고르는 것을 두지 않는다.** 낭송 방식 · 묵주 · 판본 · 손 없이 조작은 전부 설정이나 여정 만들기에서 끝낸다.
3. **홈 카드 하나가 여정 하나를 다 말한다** — 바람 · 며칠째 · 청원/감사 · 이어갈 자리. 카드를 열어 상세로 가야 알 수 있는 것을 카드에 남기지 않는다.
4. **차별점 8(끊겨도 이어짐)은 겪을 때까지 설명하지 않는다.**

---

## 4. 기능 요구사항

### 4-1. 읽는 법

각 요구사항에 `FR-##` 번호가 붙는다. 06-d 화면 명세와 07 목업, 08 테스트 케이스가 **이 번호를 참조**하므로, 이미 부여된 번호는 다시 매기지 않는다. FR-08과 FR-09는 처음부터 비어 있으며 재사용하지 않는다. **FR-29는 이 판에서 폐기했다**(내용이 정정 3번과 정면으로 어긋난다). 번호는 남기고 내용을 "폐기"로 표시한다.

이 판에서 **뜻이 바뀐 기존 요구사항**에는 `[제3판 개정]` 표시를 붙였고, **새 요구사항은 FR-32부터** 매겼다.

**모든 요구사항에 출처를 단다.** 출처는 아래 여섯 갈래 중 하나여야 하고, 어디에도 해당하지 않으면 그것은 이 자리에서 발명된 것이므로 **넣기 전에 §1-5의 뼈대인지부터 판정한다.**

| 출처 표기 | 뜻 |
|---|---|
| **뼈대** | §1-5의 여섯 항목. 없으면 묵주기도 앱이 아닌 것 |
| **공방장 정정** | 2026-09-05 검토에서 공방장이 직접 정한 것 (문서 머리 표의 번호) |
| **05 차별** | 05단계가 V1으로 확정해 물려준 차별 기능 (교대 낭송 · 침묵 진행 · 끊기면 이어 주기) |
| **조사(02b)** | [`02b-design-research.md`](./02b-design-research.md)에서 나온 것 |
| **제약** | 접근성 요구 또는 기기·플랫폼·스토어 심사의 제약에서 나온 것 |
| **파생** | 위 항목을 만들면 자동으로 필요해지는 것. 그 자체가 목적인 요구사항은 아니다 |

### 4-2. 계정과 동기화

| # | 요구사항 | 출처 |
|---|---|---|
| FR-32 | 앱은 **계정으로 시작한다.** 구글 로그인과 애플 로그인을 제공하고, 둘 중 하나로 들어와야 홈에 닿는다. 이메일·비밀번호 가입은 두지 않는다 | 공방장 정정 9 · 제약 (iOS는 제3자 로그인을 두면 애플 로그인을 함께 두어야 한다 — §11-3) |
| FR-41 | 여정 목록 · 진척 · 진행 자리 · 설정이 **계정에 저장**되어 기기를 바꾸거나 다시 설치해도 그대로 돌아온다. 연결이 없을 때 바친 것은 연결되면 올라간다 | 파생 (FR-32) · 05 차별 (끊기면 이어 주기의 확장) |
| FR-45 | 설정에서 **로그아웃**과 **계정 삭제**를 할 수 있다. **로그아웃은 이 기기의 여정 사본과 자리를 지운다**(서버에는 남는다 — 같은 기기에 다른 사람이 로그인할 수 있으므로). 계정을 삭제하면 서버의 모든 데이터가 지워지고, 조에 있었으면 조에서 빠진다 `[06-e 결함 11]` | 제약 (스토어 심사 — 계정을 만들 수 있으면 지울 수도 있어야 한다) · §6 개인정보 · 06-e 결함 11 |

### 4-3. 여정 — 시작하고, 목록에서 보고, 이어가기

| # | 요구사항 | 출처 |
|---|---|---|
| FR-36 | **홈은 여정(지향) 목록이다.** 카드 하나가 여정 하나이고, 카드에 **바람** · **며칠째**(54일 기도면 N/54와 청원/감사) · **오늘 안의 자리**(바쳤음 / 제N단 M번째 알부터 / 아직) · 마지막으로 바친 때가 적힌다. 진행 중인 여정이 위, 마친 여정이 아래다. **시작일이 미래인 여정**은 흐린 카드로 보이고 누르면 며칠 뒤 시작인지만 알린다 — 기도로 들어가지 않는다 `[06-e 결함 8]` | 공방장 정정 3 · 뼈대 (여정의 진척) · 06-e 결함 8 |
| FR-42 | **카드를 누르면 바로 기도 화면이다.** 이어갈 자리가 있으면 그 자리부터, 없으면 오늘 것의 처음부터. 목록에서 기도까지 조작이 한 번이다 | 조사(02b) — Marcello의 단순함. 기본 요건 |
| FR-33 | 홈의 **"새 기도"** 는 세 걸음짜리 마법사를 연다 — ① 바람(한 줄, 최대 24자) ② 형식(54일 기도 · 9일 기도 · 날마다) ③ 시작일(기본값 오늘)과 기도문 판본(기본값은 설정). 마치면 **완료 예정일을 앱이 계산해 보여 준다**(54일이면 시작일 + 53일). 함께 바칠지(FR-40)는 ②에서 고른다 | 공방장 정정 3 |
| FR-35 | 54일 기도의 **진척률**을 홈 카드와 여정 상세에 표시한다 — 며칠째(N/54), 청원인가 감사인가(1~27일 청원, 28~54일 감사), 남은 날. 하루를 건너뛰면 그날은 비어 있는 채로 남고 여정은 끊기지 않는다 — **처음부터 다시 하라고 강요하지 않는다**(사용자가 원하면 여정 상세에서 다시 시작할 수 있다) | 공방장 정정 3 (필수 명시) · 01단계 마찰 5 · 톤 규칙 |
| FR-37 | 여정 상세에서 **날짜별 기록**을 볼 수 있다 — 어느 날 바쳤고 어느 날 비었는가. 마친 여정도 목록 아래에 남아 다시 볼 수 있다 | 공방장 정정 3 ("지난 묵주기도의 기록") |
| FR-04 | 여정마다 진행 자리와 진척이 **따로** 기억된다 `[제3판 개정 — 지향이 여정으로 넓어졌다]` | 뼈대 |
| FR-05 | 여정을 지울 수 있다(카드 길게 누르기 → 확인). 진행이 있는 여정을 지우면 그 기록도 지워진다. 여정이 하나도 없는 상태는 허용된다(첫 실행이 그 상태다). **조 여정은 만든 사람에게는 "조 해산", 조원에게는 "조에서 나가기"** 이고 확인 문구가 각각 다르다 `[제3판 개정 — "마지막 하나는 지울 수 없다"를 뺐다 · 06-e 결함 9]` | 파생 (FR-33) · 06-e 결함 9 |
| FR-02 | 카드에 이어갈 자리가 있으면 **그 자리부터**가 기본 행동이다. 카드 안에 "처음부터"는 따로 두지 않고 여정 상세에 둔다 `[제3판 개정 — 이어가기 카드가 여정 카드에 흡수됐다]` | 05 차별 |
| FR-03 | 이어갈 자리에는 어디서 멈췄는지(제N단 M번째 알)와 언제 멈췼는지와 **어느 신비였는지**가 함께 저장되고, 이어가기는 **저장된 신비**로 진행한다 | 05 차별 · 06-e 결함 2 |
| FR-30 | 첫 실행(로그인 직후)에는 여정이 없다. 홈은 **"새 기도" 하나와 이 앱이 무엇인지 두 줄**만 보인다. 비어 보이는 것이 정상이며, 채우라고 재촉하지 않는다 `[제3판 개정]` | 파생 (06-c 백 오피스 판단) |
| FR-29 | **폐기 (2026-09-05).** "무엇을 바칠지 고르는 화면을 중간에 두지 않는다"는 정정 3과 어긋난다. 지키려던 것(조작 한 번)은 FR-42가 잇는다 | — |

### 4-4. 오늘의 신비 — 앱이 정한다

| # | 요구사항 | 출처 |
|---|---|---|
| FR-01 | 오늘의 신비는 **앱이 정해 알려 준다.** 사용자가 고르는 자리를 두지 않는다 `[제3판 개정 — 규칙이 둘로 늘었다, FR-43]` | 뼈대 · 공방장 정정 4 |
| FR-43 | 규칙은 여정의 형식에 따른다. **날마다(자유) 기도**는 요일 규칙 — 월·토 환희, 화·금 고통, 수·일 영광, 목 빛. **54일 기도**는 첫날부터 환희 → 고통 → 영광을 하루씩 돌려 27일을 채우고, 28일째부터 같은 순환으로 27일을 채운다(빛의 신비는 54일 기도의 전통적 순환에 들어가지 않는다). 9일 기도는 요일 규칙을 따른다 | 공방장 정정 4 · 뼈대 |

### 4-5. 정해진 순서로 진행하기

| # | 요구사항 | 출처 |
|---|---|---|
| FR-10 | 한 번의 기도를 **77단계**(시작 기도 7 + 각 단 14 × 5단) 순서대로 진행한다. 구성은 §1-3과 같다 | 뼈대 |
| FR-16 | 지금 몇 단째이고 그 단의 몇 번째 알인지를 화면에서 볼 수 있다 | 뼈대 |
| FR-31 | 기도 화면에 지금 바치고 있는 여정의 바람과 현재 단계의 이름을 표시한다. 54일 기도면 며칠째도 한 줄로 | 파생 (FR-04·FR-16·FR-35) |
| FR-17 | **알을 넘길 때마다** 진행 자리를 저장한다. 기기에 먼저 저장하고 계정에는 연결될 때 올린다 | 05 차별 · 파생 (FR-41) |
| FR-18 | 기도 화면에서 나가는 길이 둘이다. **잠시 멈춤**은 자리를 남기고 홈으로 가고, **여기서 끝내기**는 오늘 자리를 지우고 홈으로 간다. 여정 상세의 **오늘 처음부터**는 자리가 있을 때 한 번 확인한 뒤 지운다 `[제3판 개정 — 확인 시트의 자리가 홈에서 여정 상세로]` | 파생 (FR-17) · 06-e 결함 3 |
| FR-20 | 기도가 진행되는 동안 화면이 저절로 꺼지지 않게 한다 | 제약 |
| FR-34 | 기도문은 여정에 정해진 **판본**으로 표시하고 읽는다. 판본은 여정마다 다를 수 있고, 설정의 기본 판본을 바꾸면 **새로 만드는 여정**에만 적용된다(진행 중인 여정의 문구가 도중에 바뀌지 않는다) | 공방장 정정 3 · 01단계 ("내가 보던 기도문하고 다르다") |

### 4-6. 낭송과 진행 — 이 앱의 절반은 소리와 진동이다

**이 앱의 주 사용 맥락은 화면을 보지 않는 것이므로, 소리와 진동이 화면만큼 중요한 인터페이스다.** 정확한 상수(몇 밀리초, 어떤 진동 패턴)는 [`06-screen-spec.md`](./06-screen-spec.md)의 비화면 채널 명세가 정본이다.

| # | 요구사항 | 출처 |
|---|---|---|
| FR-06 | **낭송 방식을 셋 중 고른다** — ① 전부 읽기(앱이 기도문 전체를 읽는다) ② 교대(앱이 앞 절, 사용자가 뒷 절) ③ 읽지 않기(앱은 소리를 내지 않는다). 기본값은 ②. 설정에서 바꾸며 다음 실행까지 유지된다 `[제3판 개정 — "침묵 진행 켬/끔"이 셋 중 하나로 넓어졌다]` | 공방장 정정 5 · 05 차별 |
| FR-11 | 교대 방식이면 앱이 기도문의 **앞 절만** 읽고 뒷 절은 읽지 않는다 — 그것이 사용자의 차례다. 전부 읽기면 앞뒤를 다 읽고, 읽지 않기면 아무것도 읽지 않는다 `[제3판 개정]` | 05 차별 |
| FR-12 | 앱이 읽기를 마치면 **받는 사이**만큼 기다린 뒤 자동으로 다음 단계로 넘어간다. 사용자가 실제로 응답했는지는 판별하지 않는다(음성 인식을 쓰지 않는다 — DQ-02) | 05 차별 |
| FR-13 | 받는 사이는 **사용자가 바칠 절의 길이에 비례해 계산**하고 FR-07의 세 단계로 조절한다. 교대면 뒷 절, 읽지 않기면 앞뒤 절 전체, 전부 읽기면 짧은 숨 하나가 사용자 몫이다 `[제3판 개정]` | 05 차별 |
| FR-14 | 읽지 않기 방식이면 진동으로 진행을 알린다. 알이 넘어갈 때 짧게 한 번, 단이 바뀔 때 세 번 | 05 차별 |
| FR-07 | 받는 사이를 느리게·보통·빠르게 셋 중에서 고를 수 있고 유지된다 | 제약 (호흡 속도는 사람마다 다르다) |
| FR-38 | **손 없이 조작**을 설정에서 켤 수 있다. 켜면 **폰을 흔들면 다음 알**로 넘어가고, **음량 버튼 위/아래와 이어폰의 재생·앞·뒤 버튼**이 다음/이전 알이 된다. 자동 진행과 함께 쓸 수 있다 — 사이가 끝나기 전에 흔들면 그 순간 넘어간다. 기본값은 꺼짐. **음량 버튼은 플랫폼이 허용하는 범위에서만 쓴다**(§11-3) | 공방장 정정 7 |
| FR-27 | 하루의 완주 순간에 **긴 진동을 한 번** 울린다 | 파생 + 제약 (눈을 감은 사용자가 끝을 알아야 한다) |

### 4-7. 화면에 보이는 것

색·서체·모션의 값은 [`06-design-system.md`](./06-design-system.md)가 정한다. 여기 적는 것은 **그 시스템이 반드시 만족시켜야 하는 요구**다.

| # | 요구사항 | 출처 |
|---|---|---|
| FR-15 | 화면 가운데의 현재 알이 다섯 가지 상태를 구분해 보인다 — 앱이 읽는 중, 내가 받을 차례, 진동만으로 진행 중, 단이 바뀌는 중, 멈춤(앱을 벗어났다 돌아왔을 때) | 파생 (FR-06·FR-11·FR-14) · 06-e 결함 5 |
| FR-21 | 알을 **실제 묵주의 형태**로 그린다. 위가 벌어진 고리에 아래로 십자가와 알 몇 개가 늘어진 모양이며, 정원 위 균등 배치를 쓰지 않는다 | 조사(02b) |
| FR-39 | 화면에 그리는 **묵주를 사용자가 고른다.** V1은 서로 확실히 다른 셋 이상(예: 나무 · 진주 · 유리)을 제공하고, 설정에서 바꾸면 모든 여정의 기도 화면에 적용된다. 고른 묵주는 알의 상태 표시(FR-15)를 그대로 지원해야 한다 | 공방장 정정 (필수 명시) |
| FR-22 | 신비가 바뀌면 **배경 성화가 바뀐다.** 무엇을 묵상하는지가 글자가 아니라 그림으로 전달된다. **성화의 출처와 화풍은 이 문서가 정하지 않는다**(D-1) `[제3판 개정 — "한국 성화" 조건 삭제]` | 조사(02b) — Marcello의 구조 · 공방장 정정 6 |
| FR-23 | **묵주가 성화 인물의 이목구비를 가리지 않는다.** 눈·코·입 영역에 알이나 십자가가 겹치지 않는다 | 조사(02b) — Marcello의 결함 |
| FR-24 | **기도문 영역의 높이가 고정**이다. 기도문 길이가 달라져도 배경 그림이 잘리는 양이 변하지 않는다 | 조사(02b) — Marcello의 결함 |
| FR-25 | 전례 시기(대림·사순 / 연중 / 부활·성탄·성모 축일 / 성령강림·수난)에 따라 **부 강조색이 바뀐다** | 조사(02b) — 전례색 |
| FR-26 | 알의 지름이 화면 폭의 **8% 이상**이다 | 조사(02b) + 제약 |
| FR-28 | 기도문의 글자 크기를 키우면 **줄 간격과 자간이 함께 커진다.** 시스템 글자 크기 200% 확대에서도 글자가 붙거나 잘리지 않는다 | 조사(02b) + 제약 — 굿뉴스의 실제 실패 지점 |

### 4-8. 함께 바치기 — 조

| # | 요구사항 | 출처 |
|---|---|---|
| FR-40 | 여정을 만들 때 **"함께 바치기"** 를 고르면 조가 만들어진다. 만든 사람이 **초대 코드**(또는 링크)를 받아 나누고, 코드로 들어온 사람이 조원이 된다. 조는 **다섯 명**이 정원이며 바람과 형식(54일 등)을 공유한다. 날마다 앱이 조원 다섯에게 **제1단부터 제5단을 하나씩 배정**해 조 전체가 하루에 한 바퀴(다섯 단)를 채운다. 조원이 다섯이 안 되면 남은 단은 있는 사람들에게 돌아가며 배정된다. 홈 카드에는 내 몫(오늘 제N단)과 조 전체의 오늘 진척(다섯 중 몇 단이 바쳐졌나)이 함께 보인다. **받은 사람은 홈의 "초대 코드로 들어가기"에서 코드를 넣는다.** 조는 **연결이 있을 때만** 만들 수 있다(코드는 서버가 발급한다). 조원이 나가면 남은 사람에게 단이 다시 배정된다 `[06-e 결함 10·12]` | 공방장 정정 2·8 · 조사(02b) — Rosario의 구조 · 06-e 결함 10·12 |
| FR-44 | 조원의 이름은 **로그인 계정의 이름**을 쓰고, 조 안에서만 보인다. 조는 초대 코드를 아는 사람만 들어올 수 있고, 공개 목록·검색·추천을 두지 않는다 | 파생 (FR-40) · 제약 (개인정보) |

### 4-9. 마침

| # | 요구사항 | 출처 |
|---|---|---|
| FR-19 | 하루의 다섯 단(조 기도면 내 몫의 한 단)을 마치면 **완주 화면**을 보이고 오늘 자리를 지운다. 54일 기도의 마지막 날이면 **여정 완주 화면**이 이어서 나온다 — 바람과 첫날 · 마지막 날 · 바친 날 수를 조용히 적는다 `[제3판 개정]` | 뼈대 (마침 — 하루와 여정) |

**출처가 "이 문서에서 처음 발명됨"인 요구사항은 0개다.** 요구사항 마흔둘(FR-01~45 중 FR-08·09 결번, FR-29 폐기) 전부가 여섯 갈래 중 하나에 속한다.

---

## 5. 화면별 정리 — 무엇을 만들어야 하나

### 5-1. 화면 여덟과 시트 여섯

**이 절이 정하는 것은 화면의 목록과 각 화면이 책임지는 요구사항까지다.** 화면 안에 무엇이 어떤 상태로 놓이고 어떤 문구가 쓰이는지는 [`06-screen-spec.md`](./06-screen-spec.md)가 정한다.

제2판은 화면이 셋이었다. 여정 · 계정 · 조가 들어오면서 여덟이 됐다. 늘어난 만큼 **한 화면에서 할 일은 하나**라는 원칙을 더 세게 지킨다 — 퍼소나 B(시니어)가 기준이다.

```
로그인 (L)  ── 구글 · 애플 ──▶  홈 (A)  = 여정 목록
                               ├─ 여정 카드 탭 ────────────────▶ 기도 (B)   [조작 한 번]
                               ├─ 새 기도 ─▶ 새 기도 시작 (N) 세 걸음 ─▶ 기도 (B) 또는 홈
                               ├─ 초대 코드로 들어가기 ─▶ 코드 입력 ─▶ 홈에 조 여정 카드
                               ├─ 카드 길게 누르기 ─▶ 여정 삭제 확인 시트 (S6)
                               ├─ 카드의 "자세히" ─▶ 여정 상세 (D)
                               │        ├─ 날짜별 기록 · 완료 예정일 · 오늘 처음부터(S3 확인)
                               │        └─ 조 기도면: 조원과 오늘 배정 · 초대 코드
                               └─ 설정 (E)
                                        ├─ 낭송 방식 (전부 · 교대 · 없음)
                                        ├─ 받는 사이 시트 (S2)
                                        ├─ 손 없이 조작 켬/끔
                                        ├─ 묵주 고르기 시트 (S5)
                                        ├─ 기본 기도문 판본 시트 (S4)
                                        ├─ 밤/낮
                                        └─ 로그아웃 · 계정 삭제

기도 (B)
 ├─ 잠시 멈춤 ─▶ 홈 (자리 남음)
 ├─ 여기서 끝내기 ─▶ 홈 (오늘 자리 삭제)
 ├─ 앱 이탈 ─▶ 멈춤 상태 (돌아오면 이어서 바치기 / 홈으로)
 └─ 77단계(조 기도면 내 단) 완료 ─▶ 완주 (C) ─▶ [여정 마지막 날이면] 여정 완주 (C′) ─▶ 홈
```

**탭 바를 두지 않는다.** 화면이 늘었지만 **모든 길이 홈에서 출발해 홈으로 돌아온다.** 설정은 홈 우상단 하나, 새 기도는 홈 아래 하나다. 퍼소나 B에게 "지금 어디 있나"를 묻게 만드는 구조를 피한다.

### 5-2. 화면마다 답해야 하는 질문과 담기는 요구사항

| 화면 | 이 화면이 사용자에게 답하는 질문 | 담기는 FR |
|---|---|---|
| **L 로그인** | "어느 계정으로 들어가나" — 단추 둘, 그 밖에 없다 | FR-32 |
| **A 홈 (여정 목록)** | "내가 바치고 있는 기도가 무엇이고 오늘 어디까지 왔나" — 카드를 훑으면 답이 나온다 | FR-02 · FR-03 · FR-04 · FR-05 · FR-30 · FR-35 · FR-36 · FR-40(조 카드) · FR-42 |
| **N 새 기도 시작** | "무엇을 위해, 어떤 형식으로, 언제부터 바치나" — 세 걸음, 걸음마다 질문 하나 | FR-33 · FR-34 · FR-40(함께 바치기 선택) · FR-43(완료 예정일과 첫날 신비 미리 보기) |
| **B 기도** | "지금 내 차례인가, 그리고 어디쯤인가" — 화면을 응시하지 않고도 답한다 | FR-01 · FR-10 · FR-11 · FR-12 · FR-13 · FR-14 · FR-15 · FR-16 · FR-17 · FR-18 · FR-20 · FR-21 · FR-22 · FR-23 · FR-24 · FR-26 · FR-28 · FR-31 · FR-34 · FR-38 · FR-39 · FR-43 |
| **C 완주 / C′ 여정 완주** | "오늘이 끝났는가 / 여정이 끝났는가" — 축하하지 않고 마침을 조용히 확인한다 | FR-19 · FR-27 |
| **D 여정 상세** | "이 기도는 어떻게 왔고 언제 끝나나" — 날짜별 기록, 완료 예정일, 조원과 배정 | FR-18(오늘 처음부터) · FR-35 · FR-37 · FR-40 · FR-44 |
| **E 설정** | "이 앱이 나를 어떻게 대하게 할 것인가" — 낭송 · 사이 · 손 없이 · 묵주 · 판본 · 밤/낮 · 계정 | FR-06 · FR-07 · FR-34(기본 판본) · FR-38 · FR-39 · FR-41 · FR-45 |
| **S2 받는 사이 시트** | "내 호흡에 맞는 속도는 어느 것인가" | FR-07 |
| **S3 오늘 처음부터 확인 시트** | "정말 오늘 자리를 버리는가" | FR-18 |
| **S4 기도문 판본 시트** | "어느 문구로 바치나" — 판본마다 성모송 첫 줄을 미리 보인다 | FR-34 |
| **S5 묵주 고르기 시트** | "어느 묵주로 바치나" — 셋 이상, 고르면 즉시 미리 보인다 | FR-39 |
| **S6 여정 삭제 확인 시트** | "정말 이 기도를 지우는가" | FR-05 |
| **S7 소개 (i) 시트** | "이 시제품은 무엇을 검증하는가" — 08 검증 참가자용 | — |
| **모든 화면 공통** | — | FR-25 (전례색) · FR-41 (동기화는 화면이 아니라 배경에서 돈다) |

### 5-3. 무소속 요구사항 점검

**어느 화면에도 속하지 않는 FR은 0개다.** FR-01부터 FR-45까지(FR-08·09 결번, FR-29 폐기) 전부가 위 표의 어느 행에 들어 있다. 이 점검은 07-b 목업이 통과해야 하는 게이트이기도 하다.

---

## 6. 비기능 요구사항

| 항목 | 기준 | 왜 |
|---|---|---|
| **오프라인 동작** | **기도 자체는 연결 없이 전부 동작한다.** 오늘의 신비 계산 · 기도문 · 음성 · 진동 · 자리 저장이 기기 안에서 끝난다. 연결이 필요한 것은 로그인(첫 한 번) · 동기화 · 조의 배정과 진척뿐이며, 이것들은 연결이 돌아오면 따라잡는다 | 지하철과 성당은 신호가 약하다. 조 기도에서 오늘 배정을 받지 못하면 **어제 배정과 같은 단**을 바치고 뒤에 맞춘다 |
| **개인정보** | 계정이 있으므로 데이터가 기기 밖으로 나간다. 그래서 **최소한만 서버에 둔다** — 계정 식별자 · 표시 이름 · 여정(바람 문구 포함) · 진척 · 설정. **바람 문구는 아픈 사람의 이름이 들어가는 민감한 글이므로 서버에서 암호화해 저장하고, 조원 외에는 누구에게도 보이지 않는다.** 위치 · 연락처 · 광고 식별자는 수집하지 않는다. 계정 삭제 시 전부 지운다(FR-45) | 제2판이 서버를 두지 않은 진짜 이유가 이것이었다. 서버를 두게 됐으니 그 이유를 요구사항으로 옮겨 지킨다 |
| **배터리** | 화면 꺼짐 방지를 쓰되 기도가 끝나는 즉시 해제한다 | 잠자기 전 사용이 주 맥락이다 |
| **접근성** | 터치 영역 최소 80픽셀, 본문 대비 4.5 대 1 이상, 시스템 글자 200% 확대에서 깨지지 않는다. 상세 기준은 [`06-design-system.md`](./06-design-system.md) §7 | 퍼소나 B와, 졸음으로 손끝이 부정확해진 퍼소나 A |
| **플랫폼** | V1은 iOS. Android는 V1.5에서 판단한다 | 공방장이 직접 매일 쓰며 검증할 수 있는 쪽이 iOS다 |
| **성능** | 알이 넘어갈 때의 지연이 100밀리초 이내다. 동기화가 기도의 호흡을 막지 않는다(백그라운드) | 기도의 호흡이 끊기면 안 된다 |
| **광고와 업셀** | 하나도 두지 않는다 | §3-1의 기본 요건이자 §9의 확정 사항 |
| **서버** | 계정 · 여정 · 진척 · 조를 담는 서버가 **V1에 있다.** 이것이 제2판과 가장 크게 다른 점이며, 05단계의 "서버 0원" 비용 산정은 이 판에서 무효다(§11-2) | 공방장 정정 9 |

---

## 7. 저장하는 것과 저장하지 않는 것

기기와 서버 두 곳에 저장하고, **기기가 먼저이고 서버는 따라온다.**

| 무엇 | 담기는 값 | 어디에 |
|---|---|---|
| 계정 | 로그인 제공자(구글/애플)와 식별자, 표시 이름 | 서버 |
| 설정 | 낭송 방식(전부/교대/없음), 받는 사이(느리게/보통/빠르게), 손 없이 조작(켬/끔), 묵주, 기본 기도문 판본, 밤/낮 | 기기 + 서버 |
| 여정 목록 | 여정마다 — 식별자, 바람(최대 24자), 형식, 시작일, 완료 예정일, 기도문 판본, 혼자/조, 상태(진행 중/마침/지움) | 기기 + 서버 (바람은 서버에서 암호화) |
| 날짜별 기록 | 여정 × 날짜 — 바쳤는가, 어느 신비였는가, 걸린 시간 | 기기 + 서버 |
| 오늘의 자리 | 몇 번째 단계인지, 몇 단째인지, 몇 번째 알인지, 어느 신비인지, 언제 멈췄는지 | 기기 먼저, 서버에 따라 올림 |
| 조 | 조 식별자, 초대 코드, 조원 목록(계정 식별자와 표시 이름), 날짜별 배정(누가 몇 단), 조원별 오늘 바침 여부 | 서버 |

**저장하지 않는 것**: 위치, 연락처, 기기 정보, 기도한 내용의 음성이나 녹음, 광고 식별자, 조 밖으로 나가는 사용 통계. 08단계 검증에서 완주율을 재야 하지만, 그때도 **사용자가 직접 알려 주는 방식**을 쓴다.

---

## 8. 오류와 예외

**붉은 경고색을 쓰지 않는다.** 기도 맥락에서 붉은 경고는 벌주는 인상을 주기 때문이며, 주의가 필요하면 치자색으로 표시한다([`06-design-system.md`](./06-design-system.md) §2-4).

| 상황 | 앱이 하는 일 | 사용자에게 보이는 말 |
|---|---|---|
| 로그인이 실패했다(취소·네트워크) | 로그인 화면에 머문다 | "로그인이 되지 않았습니다. 다시 시도해 주세요." |
| 연결이 없는데 로그인이 이미 되어 있다 | 기기에 있는 여정으로 **정상 동작**한다. 동기화만 뒤로 미룬다 | 알리지 않는다 (홈 상단에 작은 "연결되면 맞춰 둡니다" 한 줄만) |
| 연결이 없는데 조 기도의 오늘 배정을 못 받았다 | 어제 배정과 같은 단을 바치게 하고, 연결되면 서버 배정과 맞춘다 | "오늘 배정을 아직 받지 못해 어제와 같은 단을 바칩니다." |
| 두 기기에서 같은 여정을 동시에 바쳤다 | **더 앞선 자리**를 남긴다(기도를 잃지 않는 쪽) | 알리지 않는다 |
| 기기에 한국어 음성이 없다 | 읽지 않기 방식으로 진행하고, 받는 사이에 앞 절 몫을 더한다 | "이 기기에 한국어 음성이 없어 소리 없이 진행됩니다. 진동과 받는 사이는 그대로 동작합니다." |
| 기기가 진동을 지원하지 않는다 | 사이만으로 진행한다 | 알리지 않는다 |
| 기기가 흔들기 감지를 지원하지 않는다 | 손 없이 조작 설정에서 흔들기 항목을 감춘다 | 알리지 않는다 |
| 기도 중에 전화가 온다 | 자리를 저장하고 **멈춤 상태**로 정지한다 | 알리지 않는다. 돌아오면 "이어서 바치기 / 홈으로" |
| 저장 공간을 쓸 수 없다 | 기도 진행은 정상이고, 자리 저장만 실패한다 | "이 기기에서는 진행 자리를 기억할 수 없습니다." — 한 번만 알린다 |
| 바람이 비어 있다 | 다음 걸음으로 넘어가지 않는다 | 입력란 아래 "바람을 한 줄 적어 주세요" |
| 시작일이 오늘보다 앞이다 | 허용한다. 지난 날은 비어 있는 채로 여정이 만들어진다 | "지난 N일은 비어 있는 채로 시작합니다." |
| 초대 코드가 틀렸거나 조가 찼다 | 들어가지 않는다 | "이 코드의 기도를 찾지 못했습니다." / "이 기도는 다섯 명이 다 모였습니다." |
| 저장된 자리가 손상됐다 | 그 자리를 버리고 오늘 처음부터 시작할 수 있게 둔다 | "이어갈 자리를 찾지 못해 오늘 처음부터 시작합니다." |

---

## 9. V1 범위 밖 — 만들지 않는 것

**명시하지 않으면 만들게 된다.** 제2판에서 범위 밖이었다가 이 판에서 **안으로 들어온 것**은 54일 진척, 여러 여정의 목록, 조 기도, 계정이다. 아래는 여전히 밖이다.

| 무엇 | 언제 | 왜 지금이 아닌가 |
|---|---|---|
| 목소리 고르기 (사제·성우) | V1.5 | 교회 승인과 초상권 협의가 앞에 있다 |
| 알림·푸시 | 미정 | 기도를 재촉하는 알림은 톤 규칙과 충돌한다. 01단계가 "알아서 알람도"를 말했으므로 **필요한지부터 08에서 검증**한다 |
| 조의 지향 공개 · 조 찾기 · 추천 | 만들지 않는다 | 조는 초대 코드로만 맺어진다(FR-44). 공개 카탈로그는 Rosario의 것이고 우리 개인정보 기준과 어긋난다 |
| 조 기도의 다른 방식(다섯이 각자 다섯 단을 바치고 진척만 나눔) | V1.5 | V1은 Rosario가 검증한 "한 단씩 나눔" 하나만 한다. 둘을 함께 두면 조를 만드는 화면이 복잡해진다 |
| 가족 계정 · 본당 라이선스 | V1.5~V2 | 결제와 관리 화면이 필요하다 |
| 다국어 | V2 | 각 권역의 기도문 감수는 사람이 해야 한다 |
| Android | V1.5 | §6 플랫폼 |
| 완주 공유 카드(SNS) · 연속 기록 배지 · 순위 | **만들지 않는다** | 기도를 성과로 만드는 표현은 톤 규칙 위반이다. 54일 진척은 성과가 아니라 **여정의 위치**다 — 백분율 배지나 불꽃 아이콘으로 그리지 않는다 |
| 광고 | **만들지 않는다** | 03·04단계에서 이미 제외를 확정했다 |
| 기도 중 배속과 건너뛰기 단추 | **만들지 않는다** | 기도는 탐색하는 것이 아니다. FR-38의 앞/뒤는 **한 알**을 되짚는 것이고 배속이 아니다 |
| 음성 인식으로 응답 판별 | **만들지 않는다** | DQ-02 확정 |

---

## 10. 07단계로 넘기는 것

07단계는 목업과 시제품을 만드는 자리다. 이 문서가 넘기는 것은 넷이다.

1. **화면 목록이 닫혔다** — 화면 여덟과 시트 일곱(§5-1). 화면 사이의 연결과 시나리오 검증은 [`06-screen-map.md`](./06-screen-map.md)가 갖고 있다.
2. **요구사항과 화면의 대응표**(§5-2). 07-b 목업은 이 대응을 만족해야 하며, 08단계 테스트 케이스도 같은 번호를 쓴다.
3. **기본 요건과 차별점의 구분**(§3-1·§3-2). 목업 채점은 [`06-design-system.md`](./06-design-system.md) §9의 항목으로 한다.
4. **아직 닫히지 않은 것**(§11) — 목업을 만들면서 임의로 정하지 말아야 할 항목들이다.

07-b 목업의 통과 조건에 이 문서가 더하는 한 줄은 이것이다.

> **위 FR 전부가 어느 화면에서 충족되는지 대응이 확인된다. 어느 화면에도 없는 FR이 0개다.**

**07 개정 목록(2026-09-05)** — 지금까지 만든 시제품과 후보 시안은 전부 **기도 화면 하나**(B)를 다룬다. 그 화면의 규칙(실제 묵주 · 알의 상태 · 기도문 영역 고정 · 얼굴 가리지 않기)은 이 판에서도 그대로이므로 **버릴 것은 없다.** 다만 아래는 이 판과 어긋난다.

- 시제품 v4의 **한복 성모 SVG** — 정정 1로 조건이 빠졌다. 배경 성화의 출처는 D-1이 정한다.
- 시제품 v1~v4와 후보 d·e 시리즈의 **홈** — 전부 "곧바로 기도 화면"형이다. 홈은 여정 목록(A)으로 다시 만든다.
- 07-b 시안 브리프 §2의 차별점 목록("한국 성화의 성모님")과 §7 장르 2("쪽빛 밤과 한복 성모").
- 07-c 프롬프트 v2는 기도 화면 한 장만 요구하므로 그대로 쓸 수 있다. 다만 "지향" 표시 줄에 며칠째를 더한다.
- 새로 만들어야 하는 화면 — L 로그인 · A 홈(목록) · N 새 기도 시작 · D 여정 상세 · E 설정 · C′ 여정 완주와 시트 S4·S5·S6.

---

## 11. 아직 확정되지 않은 것

이 문서가 확신하지 못하는 것을 숨기지 않고 적는다. **아래 항목은 07단계에서 임의로 정하지 않는다.**

### 11-1. 사람의 결정을 기다리는 것

| # | 무엇을 정해야 하나 | 지금의 기본값 (답이 없으면 이대로 간다) |
|---|---|---|
| D-1 · **범위가 바뀜 2026-09-05** | **배경 성화를 어떻게 확보하나.** 정정 6으로 "한국 성화"라는 조건이 빠졌으므로 선택지가 넓어졌다 — 저작권이 만료된 고전 성화, 사용 허락을 받은 현대 작품, 새로 그린 것. 비용과 시간이 갈린다 | 07-b 목업은 **저작권이 만료된 고전 성화**로 진행한다(확보가 확실하고 비용이 없다). 출시용 자산은 별도 결정을 받는다 |
| D-2 · 기본값 확정 2026-09-01 | 밤과 낮 중 기본값 | 밤 잠정. 08 검증에서 정한다 |
| D-3 · 기본값 확정 2026-09-01 | 마침 기도를 넣을 것인가 | 넣지 않는다 |
| D-4 · **범위가 바뀜 2026-09-05** | **기도문을 어느 판본들로 제공하고, 각 판본의 사용 권한이 있는가.** 정정 3으로 "판본을 고른다"가 기능이 됐으므로, "어느 하나로 쓰나"가 아니라 **"어느 것들을 담나"** 가 질문이다 | 한국 천주교 현행 공식 판본을 기본값으로 담고, 판본 목록과 권한 확인은 배포 전에 마친다. 07-b 목업은 판본 시트를 자리만 잡고 문구 둘로 시늉한다 |

### 11-2. 다른 문서에서 고쳐야 하는 것

- **05단계의 비용 산정** — "서버·저장 0원, 서버가 없다"는 이 판에서 무효다. 서버 비용 한 줄이 생긴다. 05 문서 끝에 정정 메모를 붙였다.
- **02b §1-4 Rosario 절의 결론** — "우리 아키텍처는 이 방향을 택할 수 없다"는 제2판의 전제였고 이 판에서 뒤집혔다. 02b 끝에 정정 메모를 붙였다.
- **07-b · 07-e · 시제품 v4** — §10 개정 목록.
- **06-b 디자인 시스템 §0·§1·§4-B·§9** — "첫 화면 = 기도 화면" 규칙과 "한국 성화 계열" 수용 기준. 이 판과 함께 고쳤다.

### 11-3. 확인하지 못한 것과 플랫폼 제약

| 무엇 | 지금 아는 것 | 이것이 바뀌면 무엇이 달라지나 |
|---|---|---|
| **iOS에서 음량 버튼을 조작에 쓸 수 있는가** | 애플은 음량 버튼을 음량 조절 외의 용도로 쓰는 앱을 심사에서 거절한다(앱스토어 심사 지침 2.5.9). **iOS V1에서 FR-38의 음량 버튼은 쓰지 못할 가능성이 높다.** 흔들기(가속도 센서)와 이어폰 재생·앞·뒤 버튼(미디어 리모트 명령)은 허용된다 | iOS V1은 흔들기 + 이어폰 버튼으로 가고, 음량 버튼은 Android V1.5에서 켠다. 공방장에게 이 제약을 알린 것이 이 줄이다 |
| **애플 로그인 의무** | 제3자 로그인(구글)을 두면 애플 로그인을 함께 두어야 한다(심사 지침 4.8) | FR-32가 둘을 함께 요구하는 이유다. 확인 사항이 아니라 확정 제약이다 |
| **54일 기도에서 하루를 건너뛰었을 때의 전통** | 전통적으로는 처음부터 다시 시작한다고 알려져 있으나, 이 앱은 강요하지 않는다(FR-35). 신앙적으로 맞는 처리인지는 사제 자문이 필요하다 | 자문 결과 "반드시 처음부터"라면 기본값을 뒤집는 대신 **선택지를 눈에 띄게** 두는 쪽으로 고친다 |
| **굿뉴스 · 성모님과 함께의 실제 화면** | 확보하지 못했다 | 한국 앱이 54일 기도를 어떻게 다루는지에 따라 차별점 1순위의 방어 가능성이 달라진다 |
| Marcello가 끊긴 자리를 자동 저장하는가 | 메뉴에 "Go to…"가 있는 것만 확인했다 | 차별점 8순위의 우위 폭 |

### 11-4. 이 판에서 버린 것과 살린 것

| 버린 것 | 왜 |
|---|---|
| "앱을 열면 곧바로 기도 화면"이라는 기본 요건과 FR-29 | 정정 3. 여정이 여럿이면 목록이 홈이다. 지키려던 단순함은 "목록에서 조작 한 번"(FR-42)으로 옮겼다 |
| 차별점 "한국 성화의 성모님"과 "한국 성화를 쓴다"는 요구 | 정정 1·6. 조사의 성과를 제품 방향으로 승격한 것이 결함이었다(§3-4) |
| "계정도 로그인도 두지 않는다 · 기기 밖으로 나가는 데이터가 없다" | 정정 9. 개인정보를 지키려던 뜻은 §6의 암호화·최소 수집·삭제 요구로 옮겼다 |
| "조 기도는 §9 범위 밖" | 정정 2·8. FR-40으로 들어왔다 |
| "54일 진행 표시는 V1.5" | 정정 3(필수 명시). 여정의 기본 형식이 됐다(§1-4) |
| "침묵 진행 켬/끔" 이분법 | 정정 5. 낭송 방식 셋으로 넓혔다(FR-06) |
| "마지막 지향 하나는 지울 수 없다" | 여정이 없는 홈이 정상 상태가 됐다(FR-30) |

| 살린 것 | 어디로 갔나 |
|---|---|
| 77단계 핵심 루프 | §1-3 그대로 |
| 제품의 뼈대 | §1-5. 다섯에서 여섯으로(여정의 진척을 더했다) |
| 경쟁 대조와 실화면 근거 | §2. 우리 V1 칸만 다시 썼다 |
| 기도 화면의 규칙 전부 — 실제 묵주 · 알의 상태 · 얼굴 가리지 않기 · 기도문 영역 고정 · 8% · 시니어 조판 | FR-15·21·23·24·26·28 그대로 |
| 교대 낭송 · 받는 사이 · 진동 · 끊기면 이어 주기 | FR-07·11·12·13·14·17·18·27. 교대는 셋 중 기본값이 됐다 |
| 무너진 판단 둘의 기록 | §3-3 |
| 오류·예외 · 저장하는 것 · 범위 밖 | §7·§8·§9. 항목을 보탰다 |
| 기존 FR-01~31의 번호 | 전부 유지. 뜻이 바뀐 것은 `[제3판 개정]` 표시 |
