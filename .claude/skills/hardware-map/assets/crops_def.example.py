# hardware-map · 블록별 크롭 정의 — 렌더러(render_crops.py)와 추출기(extract_parts.py) 공용.
# 이 파일을 crops_def.py 로 복사한 뒤 제품에 맞게 채운다(둘이 항상 같은 값을 봐야 정합).
#
# CROP: 블록 id → (sheet_page_index, x0, y0, x1, y1)
#   - sheet_page_index: 0부터. (fitz doc[idx])
#   - 좌표: 모두 0~1이면 "페이지 대비 분수"로 자동 인식(권장, 해상도 무관).
#           1보다 크면 절대 pt 좌표(A3=842×595pt 등)로 처리.
#   - 블록 id 는 hw_data.py 의 B 키와 정확히 일치해야 한다.
#   - 크롭 사각형에 "보이는" 부품이 곧 그 블록의 '부품별 역할' 목록이 된다.
#     사분면이 겹치면 이웃 블록 부품이 함께 잡히니(= 이미지에도 보임) 그대로 정합.
CROP = {
 # sheet 3 (idx2): 전원·MCU·메모리·커넥터·LED
 'mcu':    (2, 0.47, 0.00, 1.00, 0.44),
 'charger':(2, 0.00, 0.02, 0.47, 0.24),
 'gauge':  (2, 0.00, 0.22, 0.42, 0.45),
 'sw':     (2, 0.40, 0.24, 0.52, 0.45),
 'power':  (2, 0.00, 0.44, 0.47, 0.77),
 'rtc':    (2, 0.47, 0.44, 0.74, 0.66),
 'sd':     (2, 0.73, 0.44, 1.00, 0.66),
 'battery':(2, 0.00, 0.76, 0.30, 1.00),
 'pogo':   (2, 0.28, 0.76, 0.63, 1.00),
 # sheet 4 (idx3): GNSS·IMU·HR·E-Paper (사분면, 겹침 최소화)
 'gnss':   (3, 0.00, 0.03, 0.42, 0.56),
 'gnssfe': (3, 0.37, 0.03, 1.00, 0.52),
 'gnssant':(3, 0.85, 0.05, 1.00, 0.40),
 'imu':    (3, 0.00, 0.38, 0.55, 0.66),
 'mag':    (3, 0.50, 0.41, 0.87, 0.66),
 'led':    (3, 0.00, 0.66, 1.00, 1.00),
}

# SHEETS: 전체 시트 이미지 key(=B의 'sh' 값) → 페이지 인덱스.
#   '전체 회로도 시트로 보기' 버튼과 이미지 없는 블록의 폴백에 쓰인다.
SHEETS = {
 'block': 0,   # 블록다이어그램 (Sheet 1)
 'pwr':   2,   # 전원·MCU·메모리 (Sheet 3)
 'gnss':  3,   # GNSS·IMU·LED (Sheet 4)
}
